var lib =
/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./lib/index.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./lib/behaviorRunner.js":
/*!*******************************!*\
  !*** ./lib/behaviorRunner.js ***!
  \*******************************/
/*! exports provided: BehaviorRunner, initRunnableBehavior */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"BehaviorRunner\", function() { return BehaviorRunner; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"initRunnableBehavior\", function() { return initRunnableBehavior; });\n/* harmony import */ var _general__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./general */ \"./lib/general.js\");\n/* harmony import */ var _postStepFNs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./postStepFNs */ \"./lib/postStepFNs.js\");\n/* harmony import */ var _events__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./events */ \"./lib/events.js\");\n/* harmony import */ var _delays__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./delays */ \"./lib/delays.js\");\n\n\n\n\n\n/**\n * A thin wrapper around the execution of a behaviors actions\n * in order to support behavior pausing.\n *\n * A behavior is in a paused state when the property `$WBBehaviorPaused`,\n * found on the `window` object, is truthy and when that property is\n * falsy the behavior is considered in an un-paused state.\n *\n * The check for a behavior pause state transitions is done\n * before performing the behaviors next action, allowing the\n * action to be atomic.\n */\nclass BehaviorRunner {\n  /**\n   * @param {BehaviorRunnerOpts} init\n   */\n  constructor({ behaviorStepIterator, postStepFN, metadata }) {\n    /**\n     *  The behavior's action iterator\n     * @type {RawBehaviorIterator}\n     */\n    this.stepIterator = behaviorStepIterator;\n\n    /**\n     * @type {?number}\n     */\n    this.upCheckInterval = null;\n\n    /**\n     * @type {?Object}\n     */\n    this.metadata = metadata;\n\n    /**\n     * A function used to transform the return value of {@link BehaviorRunner#stepIterator}\n     * after performing an action into a value that is interpretable by Webrecorders automation\n     * system.\n     * @type {?function(result: RawBehaviorStepResults): BehaviorStepResults}\n     */\n    this.postStepFN = postStepFN;\n\n    /**\n     * @type {?Promise<void>}\n     * @private\n     */\n    this._waitP = null;\n    Object(_general__WEBPACK_IMPORTED_MODULE_0__[\"autobind\"])(this);\n  }\n\n  /**\n   * Returns T/F indicating if the behavior is currently paused\n   * @return {boolean}\n   */\n  get isPaused() {\n    return window.$WBBehaviorPaused;\n  }\n\n  /**\n   * Swaps the behavior actions to be applied to the page.\n   * @param {RawBehaviorIterator} newBehaviorIterator - a new behavior action iterator to be run\n   * @param {function(results: RawBehaviorStepResults): BehaviorStepResults} [newPostStepFN] - an optional new post step function\n   * to be used. If a previous postStepFN is in use and a new function is not supplied the old one is persisted.\n   */\n  swapBehaviorIterator(newBehaviorIterator, newPostStepFN) {\n    this.stepIterator = newBehaviorIterator;\n    if (newPostStepFN) {\n      this.postStepFN = newPostStepFN;\n    }\n  }\n\n  /**\n   * Swaps the postStepFN to be used.\n   * @param {function(results: RawBehaviorStepResults): BehaviorStepResults} newPostStepFN - The new\n   * postStepFN to be used.\n   */\n  swapPostStepFn(newPostStepFN) {\n    this.postStepFN = newPostStepFN;\n  }\n\n  /**\n   * Returns a promise that resolves once `window.$WBBehaviorPaused`\n   * is falsy, checking at 2 second intervals.\n   * @return {Promise<void>}\n   */\n  waitToBeUnpaused() {\n    if (this._waitP) return this._waitP;\n    this._waitP = new Promise(resolve => {\n      this.upCheckInterval = setInterval(() => {\n        if (!window.$WBBehaviorPaused) {\n          clearInterval(this.upCheckInterval);\n          this._waitP = null;\n          resolve();\n        }\n      }, 2000);\n    });\n    return this._waitP;\n  }\n\n  /**\n   * Calls the next function of {@link BehaviorRunner#stepIterator} and\n   * if a postStepFN was supplied it is called with the results of\n   * the performed action otherwise they are returned directly.\n   * @return {Promise<BehaviorStepResults>}\n   */\n  performStep() {\n    const resultP = this.stepIterator.next();\n    if (this.postStepFN) {\n      return resultP.then(this.postStepFN);\n    }\n    return resultP.then(_postStepFNs__WEBPACK_IMPORTED_MODULE_1__[\"doneOrWait\"]);\n  }\n\n  /**\n   * Initiates the next action of a behavior.\n   *\n   * If the behavior is transitioning into the paused state (previously not paused)\n   * the promise returned resolves with the results of performing the next\n   * action once the un-paused state has been reached.\n   *\n   * If this method is called and the behavior is currently in the paused state\n   * the promise returned is the same one returned when transitioning into the\n   * paused state.\n   *\n   * Otherwise the returned promise resolves with the state of the behavior\n   * after performing an action.\n   * @return {Promise<BehaviorStepResults>}\n   */\n  step() {\n    if (this._waitP) {\n      return this._waitP;\n    }\n    if (window.$WBBehaviorPaused) {\n      return this.waitToBeUnpaused().then(this.performStep);\n    }\n    return this.performStep();\n  }\n\n  /**\n   * Shortcut for automatically run the behavior via async generators with\n   * the ability to optionally supply\n   *\n   * @example\n   *   // in some async function\n   *   for await (const value of runner.autoRunIter()) {\n   *     console.log(value)\n   *   }\n   * @param {BehaviorRunOptions} [options] - Options for controlling\n   * the amount of delay applied after each step if any and outlink collection.\n   * @return {AsyncIterableIterator<BehaviorStepResults>}\n   */\n  async *autoRunIter(options) {\n    const opts = Object.assign({ noOutlinks: true }, options);\n    const haveDelay = typeof opts.delayAmount === 'number';\n    window.$WBNOOUTLINKS = opts.noOutlinks;\n    let next;\n    while (true) {\n      next = await this.step();\n      // if we are not collecting outlinks no need to add that property\n      yield opts.noOutlinks\n        ? next\n        : Object.assign({ outlinks: window.$wbOutlinks$ }, next);\n      if (next.done) break;\n      if (haveDelay) await Object(_delays__WEBPACK_IMPORTED_MODULE_3__[\"delay\"])(opts.delayAmount);\n    }\n  }\n\n  /**\n   * Automatically run the behavior to completion optionally supplying\n   * options for controlling logging, delay, and outlink collection.\n   *\n   * Default options are no logging, no delay, and no collection of outlinks.\n   * @param {BehaviorRunOptions} [options] - Options for controlling\n   * the amount of delay applied after each step if any and outlink collection.\n   * @return {Promise<void>}\n   */\n  async autoRun(options) {\n    const opts = Object.assign({ logging: false }, options);\n    for await (const step of this.autoRunIter(opts)) {\n      if (opts.logging) {\n        console.log(step.msg, step.state);\n      }\n    }\n    if (opts.logging) {\n      console.log('done');\n    }\n  }\n\n  /**\n   * Pauses the behavior by setting the behavior paused flag to true\n   */\n  pause() {\n    window.$WBBehaviorPaused = true;\n  }\n\n  /**\n   * Un-pauses the behavior by setting the behavior paused flag to false\n   */\n  unpause() {\n    window.$WBBehaviorPaused = false;\n  }\n\n  /**\n   * Shortcut for running a behavior from a for await of loop.\n   * @return {BehaviorRunnerAsyncIterator}\n   */\n  [Symbol.asyncIterator]() {\n    return {\n      next: () => this.step(),\n    };\n  }\n}\n\n/**\n * Performs the setup required for integration with Webrecorders automation system\n * Adds as propeties to the supplied window object\n *  - $WBBehaviorStepIter$: the supplied `behaviorStepIterator`\n *  - $WBBehaviorRunner$: the instance of the class that wraps the behaviors actions\n *  - $WRIteratorHandler$: a function that initiates a behaviors action\n * @param {BehaviorRunnerInitOpts} init - Object used to initialize the behavior for running\n * @return {BehaviorRunner}\n */\nfunction initRunnableBehavior({\n  win,\n  behaviorStepIterator,\n  postStepFN,\n  metadata,\n}) {\n  win.$WBBehaviorStepIter$ = behaviorStepIterator;\n  const runner = new BehaviorRunner({\n    behaviorStepIterator,\n    postStepFN,\n    metadata,\n  });\n  win.$WBBehaviorRunner$ = runner;\n  win.$WRIteratorHandler$ = runner.step;\n  const crect = win.document.documentElement.getBoundingClientRect();\n  const x = Math.floor(Math.random() * crect.right - 100);\n  const y = Math.floor(Math.random() * crect.bottom - 100);\n  win.document.dispatchEvent(\n    Object(_events__WEBPACK_IMPORTED_MODULE_2__[\"createMouseEvent\"])({\n      type: 'mousemove',\n      position: {\n        pageX: x,\n        pageY: y,\n        clientX: x,\n        clientY: y,\n        screenX: Math.floor(Math.random() * win.screen.width),\n        screenY: Math.floor(Math.random() * win.screen.height),\n      },\n    })\n  );\n  return runner;\n}\n\n/**\n * @typedef {Object} BehaviorRunnerOpts\n * @property {RawBehaviorIterator} behaviorStepIterator - An async iterator that\n * yields the state of the behavior\n * @property {function(results: RawBehaviorStepResults): BehaviorStepResults} [postStepFN] - An optional\n * function that is supplied the state of the behavior after performing an action.\n * It is required that this function returns the state unmodified or a transformation\n * of the state that is either an object that contains the original value of the done property\n * or a boolean that is the value of the original done property.\n * @property {Object} [metadata] - Optional metadata about the behavior currently being run\n */\n\n/**\n * @typedef {Object} BehaviorRunnerInitOpts\n * @property {Window} win\n * @property {RawBehaviorIterator} behaviorStepIterator - An async iterator that\n * yields the state of the behavior\n * @property {function(results: RawBehaviorStepResults): BehaviorStepResults} [postStepFN] - An optional\n * function that is supplied the state of the behavior after performing an action.\n * It is required that this function returns the state unmodified or a transformation\n * of the state that is either an object that contains the original value of the done property\n * or a boolean that is the value of the original done property.\n * @property {Object} [metadata] - Optional metadata about the behavior currently being run\n */\n\n/**\n * @typedef {Object} BehaviorRunnerAsyncIterator\n * @property {function(): Promise<BehaviorStepResults>} next - function used to initiate the next action of the behavior\n */\n\n/**\n * @typedef {Object} BehaviorRunOptions\n * @property {number} [delayAmount = 1000] - Time value in milliseconds representing how much time should be waited after initiating a behavior's step\n * @property {boolean} [noOutlinks = true] - Should the collection of outlinks be disabled when running the behavior\n * @property {boolean} [logging = true] - Should information the behavior sends be displayed\n */\n\n/**\n * @typedef {AsyncIterator<RawBehaviorStepResults>|AsyncIterableIterator<RawBehaviorStepResults>} RawBehaviorIterator\n */\n\n\n//# sourceURL=webpack://lib/./lib/behaviorRunner.js?");

/***/ }),

/***/ "./lib/behaviorState.js":
/*!******************************!*\
  !*** ./lib/behaviorState.js ***!
  \******************************/
/*! exports provided: stateWithMsgNoWait, stateWithMsgWait, createState, stateWithMsgWaitFromFn, stateWithMsgWaitFromAwaitable */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"stateWithMsgNoWait\", function() { return stateWithMsgNoWait; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"stateWithMsgWait\", function() { return stateWithMsgWait; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"createState\", function() { return createState; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"stateWithMsgWaitFromFn\", function() { return stateWithMsgWaitFromFn; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"stateWithMsgWaitFromAwaitable\", function() { return stateWithMsgWaitFromAwaitable; });\n/**\n * Returns a behavior state object with the supplied msg indicating\n * the behavior runner should not wait for network idle.\n * The value returned should be yield'd to the behavior\n * runner\n * @param {string} msg - A message from the behavior representing a description of the action just performed\n * @param {*} [state] - Any additional information about the full state of the behavior\n * @return {BehaviorState}\n */\nfunction stateWithMsgNoWait(msg, state = {}) {\n  return { wait: false, msg, state };\n}\n\n/**\n * Returns a behavior state object with the supplied msg indicating\n * the behavior runner should wait for network idle.\n * The value returned should be yield'd to the behavior\n * runner\n * @param {string} msg -  A message from the behavior representing a description of the action just performed\n * @param {*} [state] - Any additional information about the full state of the behavior\n * @return {BehaviorState}\n */\nfunction stateWithMsgWait(msg, state = {}) {\n  return { wait: true, msg, state };\n}\n\n/**\n * Returns a behavior state object with the supplied wait\n * and msg values. The value returned should be yield'd\n * to the behavior runner\n * @param {boolean} wait\n * @param {*} msg - A message from the behavior representing a description of the action just performed\n * @param {*} [state] - Any additional information about the full state of the behavior\n * @return {BehaviorState}\n */\nfunction createState(wait, msg, state = {}) {\n  return { wait, msg, state };\n}\n\n/**\n * Returns a behavior state object with the supplied msg with the wait\n * value being set by the return value of the supplied function.\n * The value returned should be yield'd to the behavior runner\n * @param {function(): boolean} fn\n * @param {*} msg - A message from the behavior representing a description of the action just performed\n * @param {*} [state] - Any additional information about the full state of the behavior\n * @return {BehaviorState}\n */\nfunction stateWithMsgWaitFromFn(fn, msg, state) {\n  let wait = false;\n  try {\n    wait = fn();\n  } catch (e) {\n    wait = false;\n  }\n  return createState(wait, msg);\n}\n\n/**\n * Returns a Promise that resolves to an behavior state object with\n * the supplied msg with the wait value being set by the value resolved Promise.\n * The value returned should be yield'd to the behavior runner\n * @param {Promise<boolean>} awaitable\n * @param {*} msg - A message from the behavior representing a description of the action just performed\n * @param {*} [state] - Any additional information about the full state of the behavior\n * @return {Promise<BehaviorState>}\n */\nasync function stateWithMsgWaitFromAwaitable(awaitable, msg, state) {\n  let wait = false;\n  try {\n    wait = await awaitable;\n  } catch (e) {\n    wait = false;\n  }\n  return createState(wait, msg, state);\n}\n\n/**\n * @typedef {Object} BehaviorState\n * @property {string} msg - A message from the behavior representing a description of the action just performed\n * @property {boolean} wait - Should a wait for network idle be performed if possible by the runner\n * @property {*} state  - Any additional information about the full state of the behavior\n */\n\n\n//# sourceURL=webpack://lib/./lib/behaviorState.js?");

/***/ }),

/***/ "./lib/browser.js":
/*!************************!*\
  !*** ./lib/browser.js ***!
  \************************/
/*! exports provided: waitForHistoryManipToChangeLocation, locationEquals, browserLocation, locationContains, browserHistoryGoBack, browserHistoryNav */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForHistoryManipToChangeLocation\", function() { return waitForHistoryManipToChangeLocation; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"locationEquals\", function() { return locationEquals; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"browserLocation\", function() { return browserLocation; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"locationContains\", function() { return locationContains; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"browserHistoryGoBack\", function() { return browserHistoryGoBack; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"browserHistoryNav\", function() { return browserHistoryNav; });\n/**\n * Waits for the value of `window.location.href` to differ from\n * the supplied value due to browser history manipulation. If actual navigation\n * causes a change this function is useless.  The default poll rate is 1 second\n * and the safety timeout is 5 seconds.\n * @param {string} previousLocation - The value of `window.location.href` prior\n * to the pending or already occurred history manipulation of `window.location`\n * @param {WaitForOptions} [options] - Optional options\n * for controlling the checks poll rate and the safety timeout length.\n * Supplied values for `pollRate` and `safety` in milliseconds.\n * @return {Promise<boolean>} - Returns false if no change was detected by\n * the timeout length determined by options.safety otherwise true.\n */\nfunction waitForHistoryManipToChangeLocation(previousLocation, options) {\n  const opts = Object.assign({ pollRate: 1000, max: 5000 }, options);\n  let pollInterval;\n  let safetyTimeout;\n  return new Promise(resolve => {\n    if (!locationEquals(previousLocation)) return resolve(true);\n    pollInterval = setInterval(() => {\n      if (!locationEquals(previousLocation)) {\n        if (safetyTimeout) {\n          clearTimeout(safetyTimeout);\n        }\n        clearInterval(pollInterval);\n        return resolve(true);\n      }\n    }, opts.pollRate);\n    safetyTimeout = setTimeout(() => {\n      if (pollInterval) clearInterval(pollInterval);\n      resolve(!locationEquals(previousLocation));\n    }, opts.max);\n  });\n}\n\n/**\n * Determines the strict equality of the string\n * to the value of `window.location.href`\n * @param {string} someLocation - The expect location.href value\n * @return {boolean}\n */\nfunction locationEquals(someLocation) {\n  return window.location.href === someLocation;\n}\n\n/**\n * Returns the value `window.location.href`\n * @return {string}\n */\nfunction browserLocation() {\n  return window.location.href;\n}\n\n/**\n * Determines if `window.location.href` contains\n * the supplied value as a substring.\n * @param {string} something - The value to determine if\n * it is a substring of `window.location.href`\n * @return {boolean}\n */\nfunction locationContains(something) {\n  return window.location.href.indexOf(something) !== -1;\n}\n\n/**\n * Navigate the browser back 1 entry in the history stack.\n * This may cause navigation.\n */\nfunction browserHistoryGoBack() {\n  history.go(-1);\n}\n\n/**\n */\nfunction browserHistoryNav(url, $win) {\n  if (!$win) {\n    $win = window;\n  }\n  $win.history.replaceState({}, '', url);\n  $win.dispatchEvent(new $win.PopStateEvent('popstate', { state: {} }));\n}\n\n\n\n//# sourceURL=webpack://lib/./lib/browser.js?");

/***/ }),

/***/ "./lib/clicks.js":
/*!***********************!*\
  !*** ./lib/clicks.js ***!
  \***********************/
/*! exports provided: click, clickInContext, clickWithDelay, clickInContextWithDelay, scrollIntoViewAndClick, scrollIntoViewAndClickWithDelay, scrollAllIntoViewAndClick, scrollAllIntoViewAndClickWithDelay, selectElemAndClick, selectElemAndClickWithDelay, selectElemFromAndClick, selectElemFromAndClickWithDelay, clickAndWaitFor, selectClickAndWaitFor, selectFromAndClickNTimes, selectFromAndClickNTimesWithDelay, selectFromAndClickUntilNullWithDelay, selectScrollIntoViewAndClickSelectedWhileConnected, selectScrollIntoViewAndClickWithDelayWhileSelectedConnected, selectAllAndClick, selectAllAndClickWithDelay, clickAndWaitForHistoryChange, clickSelectedElementsNextElementSibling, clickSelectedElementsPreviousElementSibling */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"click\", function() { return click; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"clickInContext\", function() { return clickInContext; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"clickWithDelay\", function() { return clickWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"clickInContextWithDelay\", function() { return clickInContextWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoViewAndClick\", function() { return scrollIntoViewAndClick; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoViewAndClickWithDelay\", function() { return scrollIntoViewAndClickWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollAllIntoViewAndClick\", function() { return scrollAllIntoViewAndClick; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollAllIntoViewAndClickWithDelay\", function() { return scrollAllIntoViewAndClickWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectElemAndClick\", function() { return selectElemAndClick; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectElemAndClickWithDelay\", function() { return selectElemAndClickWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectElemFromAndClick\", function() { return selectElemFromAndClick; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectElemFromAndClickWithDelay\", function() { return selectElemFromAndClickWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"clickAndWaitFor\", function() { return clickAndWaitFor; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectClickAndWaitFor\", function() { return selectClickAndWaitFor; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectFromAndClickNTimes\", function() { return selectFromAndClickNTimes; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectFromAndClickNTimesWithDelay\", function() { return selectFromAndClickNTimesWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectFromAndClickUntilNullWithDelay\", function() { return selectFromAndClickUntilNullWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectScrollIntoViewAndClickSelectedWhileConnected\", function() { return selectScrollIntoViewAndClickSelectedWhileConnected; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectScrollIntoViewAndClickWithDelayWhileSelectedConnected\", function() { return selectScrollIntoViewAndClickWithDelayWhileSelectedConnected; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectAllAndClick\", function() { return selectAllAndClick; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectAllAndClickWithDelay\", function() { return selectAllAndClickWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"clickAndWaitForHistoryChange\", function() { return clickAndWaitForHistoryChange; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"clickSelectedElementsNextElementSibling\", function() { return clickSelectedElementsNextElementSibling; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"clickSelectedElementsPreviousElementSibling\", function() { return clickSelectedElementsPreviousElementSibling; });\n/* harmony import */ var _delays__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./delays */ \"./lib/delays.js\");\n/* harmony import */ var _scrolls__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./scrolls */ \"./lib/scrolls.js\");\n/* harmony import */ var _dom__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./dom */ \"./lib/dom.js\");\n/* harmony import */ var _events__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./events */ \"./lib/events.js\");\n/* harmony import */ var _browser__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./browser */ \"./lib/browser.js\");\n\n\n\n\n\n\n/**\n * @type {number}\n * @private\n */\nlet __currentClickCount = 0;\n\n/**\n * @type {Array<string>}\n * @private\n */\nconst __clickPreEvents = ['mousemove', 'mouseover', 'mousedown', 'mouseup'];\n\n/**\n * Calls the click function on the supplied element if non-null/defined.\n * Returns true or false to indicate if the click happened\n * @param {?SomeElement} elem - The element to be clicked\n * @return {boolean} - T/F to indicate that click happened.\n */\nfunction click(elem) {\n  let clicked = false;\n  if (elem != null) {\n    Object(_events__WEBPACK_IMPORTED_MODULE_3__[\"fireMouseEventsOnElement\"])({\n      elem,\n      eventNames: __clickPreEvents,\n      clickCount: __currentClickCount,\n    });\n    __currentClickCount += 1;\n    elem.click();\n    elem.dispatchEvent(\n      Object(_events__WEBPACK_IMPORTED_MODULE_3__[\"createMouseEvent\"])({\n        type: 'mouseleave',\n        elem,\n        clickCount: __currentClickCount,\n      })\n    );\n    clicked = true;\n  }\n  return clicked;\n}\n\n/**\n * Calls the click function on the supplied element if non-null/defined\n * that exists in the JS context of the supplied window (cntx).\n * Returns true or false to indicate if the click happened\n * @param {SomeElement} elem - The element to be clicked\n * @param {Window} cntx - The context window\n * @return {boolean} - T/F to indicate that click happened.\n */\nfunction clickInContext(elem, cntx) {\n  let clicked = false;\n  if (elem != null) {\n    Object(_events__WEBPACK_IMPORTED_MODULE_3__[\"fireMouseEventsOnElement\"])({\n      elem,\n      eventNames: __clickPreEvents,\n      view: cntx,\n    });\n    elem.click();\n    elem.dispatchEvent(\n      Object(_events__WEBPACK_IMPORTED_MODULE_3__[\"createMouseEvent\"])({ type: 'mouseleave', view: cntx, elem })\n    );\n    clicked = true;\n  }\n  return clicked;\n}\n\n/**\n * Calls the click method of the supplied element and waits the\n * specified delayTime after clicking IFF the supplied element was\n * non-null/undefined.\n * @param {?SomeElement} elem - The element to be clicked\n * @param {number} [delayTime = 1000] - How long is the delay after click\n * @return {Promise<boolean>} - A Promise that resolves to T/F\n * indicating if the click happened\n */\nasync function clickWithDelay(elem, delayTime) {\n  let clicked = click(elem);\n  if (clicked) {\n    await Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"delay\"])(delayTime || 1000);\n  }\n  return clicked;\n}\n\n/**\n * Calls the click method of the supplied element that exists in\n * the JS context of the supplied window object (cntx) and waits the\n * specified delayTime after clicking IFF the supplied element was\n * non-null/undefined.\n * @param {HTMLElement|Element} elem - The element to be clicked\n * @param {Window} cntx - The context window\n * @param {number} [delayTime = 1000] - How long is the delay after click\n * @return {Promise<boolean>} - A Promise that resolves to T/F\n * indicating if the click happened\n */\nasync function clickInContextWithDelay(elem, cntx, delayTime) {\n  const clicked = clickInContext(elem, cntx);\n  if (clicked) {\n    await Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"delay\"])(delayTime || 1000);\n  }\n  return clicked;\n}\n\n/**\n * Scrolls the supplied element into view and clicks it if the\n * element is non-null/undefined\n * @param {HTMLElement|Element} elem - The element to be scrolled into view\n * and clicked.\n * @return {boolean} - T/F to indicate that the click happened\n */\nfunction scrollIntoViewAndClick(elem) {\n  Object(_scrolls__WEBPACK_IMPORTED_MODULE_1__[\"scrollIntoView\"])(elem);\n  return click(elem);\n}\n\n/**\n * Scrolls the supplied element into view and clicks it if the\n * element is non-null/undefined and waits for the specified delay time\n * @param {HTMLElement|Element} elem - The element to be\n * @param {number} [delayTime = 1000] - How long is the delay\n * @return {Promise<boolean>} - A Promise that resolves to T/F indicating\n * that the click happened\n */\nfunction scrollIntoViewAndClickWithDelay(elem, delayTime) {\n  Object(_scrolls__WEBPACK_IMPORTED_MODULE_1__[\"scrollIntoView\"])(elem);\n  return clickWithDelay(elem, delayTime || 1000);\n}\n\n/**\n * Scrolls into view and clicks all the elements\n * present in the supplied array.\n * @param {Array<HTMLElement>} elems - The array of elements\n * to be scrolled into view and clicked\n * @return {number} - The number of elements clicked out of the supplied\n * array of elements\n */\nfunction scrollAllIntoViewAndClick(elems) {\n  let totalClicks = 0;\n  for (var i = 0; i < elems.length; ++i) {\n    Object(_scrolls__WEBPACK_IMPORTED_MODULE_1__[\"scrollIntoView\"])(elems[i]);\n    if (click(elems[i])) totalClicks += 1;\n  }\n  return totalClicks;\n}\n\n/**\n * Scrolls into view and clicks all the elements\n * present in the supplied array waiting for the supplied\n * delay time after each click of an element in the array.\n * @param {Array<HTMLElement|Element|Node>} elems - The array of elements\n * to be scrolled into view and clicked\n * @param {number} [delayTime = 1000] - How long is the delay after click\n * @return {Promise<void>}\n */\nasync function scrollAllIntoViewAndClickWithDelay(elems, delayTime) {\n  const delayAmount = delayTime || 1000;\n  for (var i = 0; i < elems.length; ++i) {\n    Object(_scrolls__WEBPACK_IMPORTED_MODULE_1__[\"scrollIntoView\"])(elems[i]);\n    await clickWithDelay(elems[i], delayAmount);\n  }\n}\n\n/**\n * Clicks the element from executing document.querySelector using\n * the supplied selector\n * @param {string} selector - the css selector to use\n * @param {*} [cntx] - element to use rather than document for the querySelector call\n * @return {boolean} - T/F to indicate that the click happened\n */\nfunction selectElemAndClick(selector, cntx) {\n  return click(Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, cntx));\n}\n\n/**\n * Clicks the element from executing document.querySelector using\n * the supplied selector and waiting the specified delay time after clicking\n * @param {string} selector - the css selector to use\n * @param {number} [delayTime = 1000] - How long is the delay\n * @return {Promise<boolean>} - A Promise that resolves to T/F indicating\n * that the click happened\n */\nfunction selectElemAndClickWithDelay(selector, delayTime) {\n  return clickWithDelay(document.querySelector(selector), delayTime || 1000);\n}\n\n/**\n * Clicks the element from executing querySelector using\n * the supplied selector using the supplied element\n * @param {Element|Node|HTMLElement} selectFrom - element to use for the querySelector call\n * @param {string} selector - the css selector to use\n * @return {boolean} - T/F to indicate that the click happened\n */\nfunction selectElemFromAndClick(selectFrom, selector) {\n  return click(Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, selectFrom));\n}\n\n/**\n * Clicks the element from executing querySelector using\n * the supplied selector from the supplied element and waiting the specified delay time after clicking\n * @param {Element|Node|HTMLElement} selectFrom - element to use for the querySelector call\n * @param {string} selector - the css selector to use\n * @param {number} [delayTime = 1000] - How long is the delay\n * @return {Promise<boolean>} - A Promise that resolves to T/F indicating\n * that the click happened\n */\nfunction selectElemFromAndClickWithDelay(\n  selectFrom,\n  selector,\n  delayTime\n) {\n  return clickWithDelay(Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, selectFrom), delayTime || 1000);\n}\n\n/**\n * Clicks the supplied element and then waits for the supplied predicate function to return true\n * @param {Element|Node|HTMLElement} elem - the element to be clicked\n * @param {function(): boolean} predicate - function returning true or false indicating the wait condition is satisfied\n * @param {?WaitForOptions} [options] - Options controlling how the wait\n * will happen\n * @return {Promise<{predicate: boolean, maxExceeded: boolean, clicked: boolean}>} - A Promise that resolves with\n * an object indicating the results of the operation\n */\nasync function clickAndWaitFor(elem, predicate, options) {\n  const results = { clicked: false, maxExceeded: false, predicate: false };\n  results.clicked = click(elem);\n  if (results.clicked) {\n    const waitr = await Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"waitForPredicate\"])(predicate, options);\n    results.predicate = waitr.predicate;\n    results.maxExceeded = waitr.maxExceeded;\n  }\n  return results;\n}\n\n/**\n * Clicks the element returned by calling querySelector using the selector\n * on document or the supplied element (selectFrom) and then waits for the supplied\n * predicate function to return true\n * @param {string} selector - the css selector to use\n * @param {function(): boolean} predicate - function returning true or false indicating the wait condition is satisfied\n * @param {HTMLElement|Document} [selectFrom] - optional element to use for the querySelector(selector) call\n * @return {Promise<{predicate: boolean, maxExceeded: boolean, clicked: boolean}>} - A Promise that resolves with\n * an object indicating the results of the operation\n */\nfunction selectClickAndWaitFor(selector, predicate, selectFrom) {\n  return clickAndWaitFor(Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, selectFrom), predicate);\n}\n\n/**\n * Clicks the element returned from result of calling querySelector using the supplied\n * selector on the supplied element N number of times. Note that the evaluation of the querySelector\n * is done all N times.\n * @param {Element} selectFrom - element to use for the querySelector(selector) call\n * @param {string} selector - the css selector to use\n * @param {number} n - How many times to click the element returned by\n * performing selectFrom.querySelector(selector)\n * @return {number} - How many clicks occurred\n */\nfunction selectFromAndClickNTimes(selectFrom, selector, n) {\n  let totalClicks = 0;\n  let clickMe;\n  for (var i = 0; i < n; ++i) {\n    clickMe = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, selectFrom);\n    if (click(clickMe)) {\n      totalClicks += 1;\n    }\n  }\n  return totalClicks;\n}\n\n/**\n * Clicks the element returned from result of calling querySelector using the supplied\n * selector on the supplied element N number of times and waits for delay time afters each click.\n * Note that the evaluation of the querySelector is done all N times.\n * @param {Element} selectFrom - The element to use to call querySelector(selector) on\n * @param {string} selector - the css selector to use\n * @param {number} n - How many times to click the element returned by\n * performing selectFrom.querySelector(selector)\n * @param {number} [delayTime = 1000] - How long is the delay\n * @return {Promise<number>} - How many clicks occurred\n */\nasync function selectFromAndClickNTimesWithDelay(\n  selectFrom,\n  selector,\n  n,\n  delayTime\n) {\n  let totalClicks = 0;\n  const delayAmount = delayTime || 1000;\n  let clickMe;\n  for (var i = 0; i < n; ++i) {\n    clickMe = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, selectFrom);\n    if (await clickWithDelay(clickMe, delayAmount)) {\n      totalClicks += 1;\n    }\n  }\n  return totalClicks;\n}\n\n/**\n * Clicks the element returned from result of calling querySelector using the supplied\n * selector on the supplied element until the results of the evaluting the selector returns null/undefined,\n * waiting for delay time afters each click.\n * @param {Element} selectFrom - The element to use to call querySelector(selector) on\n * @param {string} selector - the css selector to use when performing selectFrom.querySelector(selector)\n * @param {{delay: number, max: ?number}} [options] - options about delayTime and safety time\n * @return {Promise<number>} - How many clicks occurred\n */\nasync function selectFromAndClickUntilNullWithDelay(\n  selectFrom,\n  selector,\n  options\n) {\n  let totalClicks = 0;\n  const opts = Object.assign({ delay: 1000 }, options);\n  let exit = false;\n  let safety;\n  if (opts.max) {\n    safety = setTimeout(() => {\n      exit = true;\n    }, opts.max);\n  }\n  let clickMe = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, selectFrom);\n  while (clickMe != null) {\n    if (!clickMe.isConnected) break;\n    if (exit) break;\n    await clickWithDelay(clickMe, opts.delay);\n    clickMe = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, selectFrom);\n    totalClicks += 1;\n  }\n  if (opts.safety) {\n    clearTimeout(safety);\n  }\n  return totalClicks;\n}\n\n/**\n * Like {@link selectFromAndClickUntilNullWithDelay} but will stop\n * selecting and clicking the selected element if the current reference\n * becomes disconnected from the dom or the optional stopPredicate\n * returns true. There is no dealy for delay you want\n * {@link selectScrollIntoViewAndClickWithDelayWhileSelectedConnected}\n * @param {string} selector - The selector to be used to select the element\n * @param {SelectScrollIntoViewClickUntilNullOptions} options\n * @return {void}\n */\nfunction selectScrollIntoViewAndClickSelectedWhileConnected(\n  selector,\n  options = {}\n) {\n  let selected = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, options.cntx);\n  const haveStopPred = typeof options.stopPredicate === 'function';\n  while (selected) {\n    if (!selected.isConnected) break;\n    scrollIntoViewAndClick(selected);\n    if (haveStopPred && options.stopPredicate(selected)) break;\n    selected = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, options.cntx);\n  }\n}\n\n/**\n * Like {@link selectFromAndClickUntilNullWithDelay} but will stop\n * selecting and clicking the selected element if the current reference\n * becomes disconnected from the dom or the optional stopPredicate\n * returns true.\n * @param {string} selector - The selector to be used to select the element\n * @param {SelectScrollIntoViewClickUntilNullOptions} [options] - Additional configuration options\n * @return {Promise<void>}\n */\nasync function selectScrollIntoViewAndClickWithDelayWhileSelectedConnected(\n  selector,\n  options = {}\n) {\n  let selected = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, options.cntx);\n  const haveStopPred = typeof options.stopPredicate === 'function';\n  while (selected) {\n    if (!selected.isConnected) break;\n    await scrollIntoViewAndClickWithDelay(selected, options.delay);\n    if (haveStopPred && options.stopPredicate(selected)) break;\n    selected = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qs\"])(selector, options.cntx);\n  }\n}\n\n/**\n * Selects all elements that match the supplied selector and clicks them\n * returning T/F indicating if a click happened on any selected element\n * @param {string} selector\n * @param {Element} [context]\n * @return {boolean}\n */\nfunction selectAllAndClick(selector, context) {\n  const selected = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qsa\"])(selector, context);\n  let anyClicked = false;\n  for (let i = 0; i < selected.length; i++) {\n    if (click(selected[i])) anyClicked = true;\n  }\n  return anyClicked;\n}\n\n/**\n * Selects all elements that match the supplied selector and clicks them\n * returning T/F indicating if a click happened on any selected element\n * @param {{selector: string, cntx: *, delayTime: *}} opts\n * @return {boolean}\n */\nasync function selectAllAndClickWithDelay({ selector, cntx, delay }) {\n  const selected = Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"qsa\"])(selector, cntx);\n  let anyClicked = false;\n  for (let i = 0; i < selected.length; i++) {\n    let wasClicked = await clickWithDelay(selected[i], delay);\n    anyClicked = wasClicked || anyClicked;\n  }\n  return anyClicked;\n}\n\n/**\n * Clicks the supplied element and waits for browser history to change (the URL is updated by JavaScript modifying the current history stack)\n * @param {Element|HTMLElement} elemToBeClicked - the Element to be click\n * @param {WaitForOptions} [waitOpts] - options controlling the wait\n * @return {Promise<{oldLocation: ?string, historyChanged: boolean, clicked: boolean, newLocation: ?string, ok: boolean}>}\n */\nasync function clickAndWaitForHistoryChange(elemToBeClicked, waitOpts) {\n  const results = {\n    clicked: false,\n    historyChanged: false,\n    ok: false,\n    oldLocation: Object(_browser__WEBPACK_IMPORTED_MODULE_4__[\"browserLocation\"])(),\n    newLocation: null,\n  };\n  results.clicked = click(elemToBeClicked);\n  if (results.clicked) {\n    results.historyChanged = await Object(_browser__WEBPACK_IMPORTED_MODULE_4__[\"waitForHistoryManipToChangeLocation\"])(\n      results.oldLocation,\n      waitOpts\n    );\n  }\n  if (results.historyChanged) {\n    results.newLocation = Object(_browser__WEBPACK_IMPORTED_MODULE_4__[\"browserLocation\"])();\n  }\n  results.ok = results.clicked && results.historyChanged;\n  return results;\n}\n\n/**\n * Clicks the next element sibling of the element the supplied selector matches\n * @param {string} selector - the css selector to use\n * @param {SomeElement|Document} [cntx] - element to use rather than document for the querySelector call\n * @return {boolean}  - T/F to indicate that click happened.\n */\nfunction clickSelectedElementsNextElementSibling(selector, cntx) {\n  return click(Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"selectedNextElementSibling\"])(selector, cntx));\n}\n\n/**\n * Clicks the previous element sibling of the element the supplied selector matches\n * @param {string} selector - the css selector to use\n * @param {SomeElement|Document} [cntx] - element to use rather than document for the querySelector call\n * @return {boolean}  - T/F to indicate that click happened.\n */\nfunction clickSelectedElementsPreviousElementSibling(selector, cntx) {\n  return click(Object(_dom__WEBPACK_IMPORTED_MODULE_2__[\"selectedPreviousElementSibling\"])(selector, cntx));\n}\n\n/**\n * @typedef {Object} SelectScrollIntoViewClickUntilNullOptions\n * @property {Document|Element|HTMLElement} [cntx] - optional element to call querySelector on using selector\n * @property {function(selected: Element): boolean} [stopPredicate] - optional function called to determine if the loop should end after each iteration\n * @property{number} [delay] - amount of time, in milliseconds, that should be waited after scrolling into view and clicking (defaults to 1 second) -- Only used if the function will apply a delay\n */\n\n\n//# sourceURL=webpack://lib/./lib/clicks.js?");

/***/ }),

/***/ "./lib/delays.js":
/*!***********************!*\
  !*** ./lib/delays.js ***!
  \***********************/
/*! exports provided: DelayAmount1Second, DelayAmount2Seconds, DelayAmount3Seconds, DelayAmount4Seconds, DelayAmount5Seconds, secondsToDelayAmount, resolveWhenBehaviorUnPaused, setIntervalP, setTimeoutP, delay, waitForPredicate, waitForAndSelectElement, domCompletePromise, waitForAdditionalElemChildren, waitForAdditionalElemChildrenMO, waitUntilElementIsRemovedFromDom, waitForSelector, waitForElementToBecomeInvisible, waitForElementToBecomeVisible */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount1Second\", function() { return DelayAmount1Second; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount2Seconds\", function() { return DelayAmount2Seconds; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount3Seconds\", function() { return DelayAmount3Seconds; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount4Seconds\", function() { return DelayAmount4Seconds; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount5Seconds\", function() { return DelayAmount5Seconds; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"secondsToDelayAmount\", function() { return secondsToDelayAmount; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"resolveWhenBehaviorUnPaused\", function() { return resolveWhenBehaviorUnPaused; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"setIntervalP\", function() { return setIntervalP; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"setTimeoutP\", function() { return setTimeoutP; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"delay\", function() { return delay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForPredicate\", function() { return waitForPredicate; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForAndSelectElement\", function() { return waitForAndSelectElement; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"domCompletePromise\", function() { return domCompletePromise; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForAdditionalElemChildren\", function() { return waitForAdditionalElemChildren; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForAdditionalElemChildrenMO\", function() { return waitForAdditionalElemChildrenMO; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitUntilElementIsRemovedFromDom\", function() { return waitUntilElementIsRemovedFromDom; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForSelector\", function() { return waitForSelector; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForElementToBecomeInvisible\", function() { return waitForElementToBecomeInvisible; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForElementToBecomeVisible\", function() { return waitForElementToBecomeVisible; });\n/* harmony import */ var _dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./dom */ \"./lib/dom.js\");\n/* harmony import */ var _general__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./general */ \"./lib/general.js\");\n\n\n\n/**\n * 1 second in milliseconds\n * @type {number}\n */\nconst DelayAmount1Second = 1000;\n\n/**\n * 2the second in milliseconds\n * @type {number}\n */\nconst DelayAmount2Seconds = 2000;\n\n/**\n * 3 second in milliseconds\n * @type {number}\n */\nconst DelayAmount3Seconds = 3000;\n\n/**\n * 4 second in milliseconds\n * @type {number}\n */\nconst DelayAmount4Seconds = 4000;\n\n/**\n * 5 second in milliseconds\n * @type {number}\n */\nconst DelayAmount5Seconds = 5000;\n\n/**\n * Converts the supplied n number of seconds into milliseconds\n * which is required for setTimeout and setInterval\n * @param {number} n - The number of seconds to be delayed\n * @return {number} - The delay amount in milliseconds\n */\nfunction secondsToDelayAmount(n) {\n  return n * 1000;\n}\n\n/**\n * Returns a Promise that resolves once\n * the behavior has been un-paused\n * @return {Promise<any>}\n */\nfunction resolveWhenBehaviorUnPaused() {\n  return new Promise(resolve => {\n    let intervalId = setInterval(() => {\n      if (!window.$WBBehaviorPaused) {\n        clearInterval(intervalId);\n        resolve();\n      }\n    }, 2000);\n  });\n}\n\n/**\n * Polls the supplied function at the timeout rate\n * only if the behavior is not paused\n * @return {number} - The id of the interval\n */\nfunction setIntervalP(callback, timeout) {\n  return setInterval(function() {\n    if (!window.$WBBehaviorPaused) {\n      callback();\n    }\n  }, timeout);\n}\n\n/**\n * Like setTimeout except that the timeout respects the pause state\n * of the behavior\n * @return {number} - The id of the timeout\n */\nfunction setTimeoutP(callback, timeout) {\n  function execIfNotPaused() {\n    if (!window.$WBBehaviorPaused) {\n      callback();\n    } else {\n      setTimeout(execIfNotPaused, 500);\n    }\n  }\n\n  return setTimeout(execIfNotPaused, timeout);\n}\n\n/**\n * Returns a Promise that resolves after waiting the amount of time\n * specified by delayTime.\n * @param {number} [delayTime = 3000] - The length of the delay in milliseconds\n * @return {Promise<void>}\n */\nfunction delay(delayTime) {\n  return new Promise(resolve => {\n    setTimeout(resolve, delayTime || 3000);\n  });\n}\n\n/**\n * Returns a promise that resolves once the supplied predicate function\n * returns a truthy value.\n * @param {function(): boolean} predicate - A predicate function that\n * returns a truthy value to indicate the wait condition was satisfied otherwise\n * a falsy value to indicate wait condition not satisfied\n * @param {?WaitForOptions} [options] - Options controlling how the wait\n * will happen\n * @return {Promise<WaitResult>}\n */\nfunction waitForPredicate(predicate, options) {\n  const opts = Object.assign({ pollRate: 1000 }, options);\n  const results = { maxExceeded: false, predicate: false };\n  let maxTo;\n  return new Promise(resolve => {\n    if (predicate()) {\n      results.predicate = true;\n      return resolve(results);\n    }\n    let int = setInterval(() => {\n      if (predicate()) {\n        results.predicate = true;\n        clearInterval(int);\n        if (maxTo) clearTimeout(maxTo);\n        resolve(results);\n      }\n    }, opts.pollRate);\n    if (opts.max && opts.max !== -1) {\n      maxTo = setTimeout(() => {\n        results.predicate = predicate();\n        results.maxExceeded = true;\n        clearInterval(int);\n        resolve(results);\n      }, opts.max);\n    }\n  });\n}\n\n/**\n * Calls `fromNode.querySelector` using the supplied selector. If the\n * return value of the first calls is null/undefined then waits for the same call\n * to return a truthy value and then returns the element the selector matches.\n * @param {SomeElement} fromNode - The element the to be selected `selector` from\n * @param {string} selector - The querySelector to use for `fromNode.querySelector`\n * call.\n * @param {?WaitForOptions} [options] - Options controlling the wait\n * @return {Promise<?SomeElement>}\n */\nasync function waitForAndSelectElement(fromNode, selector, options) {\n  const elem = Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"qs\"])(selector, fromNode);\n  if (!elem) {\n    const waitRet = await waitForPredicate(\n      () => Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"selectorExists\"])(selector, fromNode),\n      options\n    );\n    if (waitRet.predicate) return Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"qs\"])(selector, fromNode);\n    return null;\n  }\n  return elem;\n}\n\n/**\n * Returns a promise that resolves once\n * `document.readyState === 'complete'`\n * @return {Promise<void>}\n */\nfunction domCompletePromise() {\n  if (document.readyState !== 'complete') {\n    return new Promise(r => {\n      let i = setInterval(() => {\n        if (document.readyState === 'complete') {\n          clearInterval(i);\n          r();\n        }\n      }, 1000);\n    });\n  }\n  return Promise.resolve();\n}\n\n/**\n * Waits for the number children of the supplied parentElement\n * to become greater than the supplied parent elements `currentChildCount`.\n *\n * __Configuration__\n *\n * If a `max` is supplied (defaults to 15 seconds) the wait for additional children\n * to be added will not exceed the configured `max` value unless `max = -1`,\n * which in this case, the wait is infinite.\n *\n * When a `guard` function is supplied it is polled at value of `pollRate`\n * (defaults to 1 second), if and when it returns true the wait is considered\n * done regardless if the supplied parent element had additional children\n * added to it.\n *\n * @param {SomeElement} parentElement - The parent element\n * @param {WaitForOptions} [options] - Options controlling the wait\n * @return {Promise<WaitResult>}\n */\nfunction waitForAdditionalElemChildren(parentElement, options) {\n  const opts = Object.assign({ pollRate: 1000, max: 15000 }, options);\n  let int;\n  let safety;\n  const currentChildCount = parentElement.childElementCount;\n  const results = { predicate: false, maxExceeded: false };\n  return new Promise(resolve => {\n    int = setInterval(() => {\n      if (!parentElement.isConnected) {\n        clearInterval(int);\n        if (safety) clearTimeout(safety);\n        return resolve(results);\n      }\n      if (opts.guard && opts.guard()) {\n        clearInterval(int);\n        if (safety) clearTimeout(safety);\n        return resolve(results);\n      }\n      if (parentElement.childElementCount > currentChildCount) {\n        clearInterval(int);\n        if (safety) clearTimeout(safety);\n        results.predicate = true;\n        return resolve(results);\n      }\n    }, opts.pollRate);\n    if (opts.max !== -1) {\n      safety = setTimeout(() => {\n        clearInterval(int);\n        results.predicate = parentElement.childElementCount > currentChildCount;\n        results.maxExceeded = true;\n        resolve(results);\n      }, opts.max);\n    }\n  });\n}\n\n/**\n * Waits for additional child elements to be added to the supplied parentElement\n * using a mutation observer.\n *\n * __Configuration__\n *\n * If a `max` is supplied (defaults to 15 seconds) the wait for additional children\n * to be added will not exceed the configured `max` value unless `max = -1`,\n * which in this case, the wait is infinite.\n *\n * When a `guard` function is supplied it is polled at value of `pollRate`\n * (defaults to 1 second), if and when it returns true the wait is considered\n * done regardless if the supplied parent element had additional children\n * added to it.\n *\n * @param {SomeElement} parentElement - The parent element\n * @param {GuardedWaitForOptions} [options] - Options controlling the wait\n * @return {Promise<WaitResult>}\n */\nfunction waitForAdditionalElemChildrenMO(parentElement, options) {\n  const opts = Object.assign({ pollRate: 1000, max: 15000 }, options);\n  let maxTo;\n  let pollIn;\n  const results = { predicate: false, maxExceeded: false };\n  const prr = Object(_general__WEBPACK_IMPORTED_MODULE_1__[\"promiseResolveReject\"])();\n  let mutatationObz;\n  const observer = () => {\n    if (maxTo) clearTimeout(maxTo);\n    if (pollIn) clearInterval(pollIn);\n    results.predicate = true;\n    if (mutatationObz) {\n      mutatationObz.disconnect();\n    }\n    prr.resolve(results);\n  };\n  if (opts.guard && opts.guard()) {\n    results.predicate = true;\n    prr.resolve(results);\n    return prr.promise;\n  }\n  mutatationObz = new MutationObserver(observer);\n  mutatationObz.observe(parentElement, { childList: true });\n  if (opts.guard) {\n    pollIn = setInterval(() => {\n      if (opts.guard()) {\n        observer();\n      }\n    }, opts.pollRate);\n  }\n  if (opts.max !== -1) {\n    maxTo = setTimeout(() => {\n      if (pollIn !== -1) clearInterval(pollIn);\n      results.maxExceeded = true;\n      mutatationObz.disconnect();\n      prr.resolve(results);\n    }, opts.max);\n  }\n  return prr.promise;\n}\n\n/**\n * Waits for the supplied element to be removed from the document.\n * The condition, element removed from document, is polled at the configured\n * `pollRate` (defaults to 1 second) for a maximum of `max` time (defaults to 15 seconds)\n * unless `max = -1` which in this case there is no maximum.\n * @param {SomeElement} elem - The element that should be removed\n * @param {WaitForOptions} [options] - Options controlling the wait\n * @return {Promise<WaitResult>}\n */\nfunction waitUntilElementIsRemovedFromDom(elem, options) {\n  const results = { predicate: false, maxExceeded: false };\n  const opts = Object.assign({ pollRate: 1000, max: 15000 }, options);\n  return new Promise(resolve => {\n    if (!elem.isConnected) {\n      results.predicate = true;\n      return resolve(results);\n    }\n    let safety;\n    const poll = setInterval(() => {\n      if (!elem.isConnected) {\n        if (safety) clearTimeout(safety);\n        clearInterval(poll);\n        results.predicate = true;\n        return resolve(results);\n      }\n    }, opts.pollRate);\n    if (opts.max !== -1) {\n      safety = setTimeout(() => {\n        clearInterval(poll);\n        results.predicate = !elem.isConnected;\n        results.maxExceeded = true;\n        resolve(results);\n      }, opts.max);\n    }\n  });\n}\n\n/**\n * Waits for the supplied selector to exist\n * @param {WaitForSelectorOptions} config - Options controlling the wait\n * @return {Promise<WaitResult>}\n */\nfunction waitForSelector({ selector, cntx, options }) {\n  return waitForPredicate(() => Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"selectorExists\"])(selector, cntx), options);\n}\n\n/**\n * Waits for the supplied element computed style display or visibility state\n * the element is invisible.\n * The condition, element becomes invisible, is polled at the configured\n * `pollRate` (defaults to 1 second) for a maximum of `max` time (defaults to 15 seconds)\n * unless `max = -1` which in this case there is no maximum.\n * @param {SomeElement} elem - The element that should be removed\n * @param {WaitForOptions} [options] - Options controlling the wait\n * @return {Promise<WaitResult>}\n */\nfunction waitForElementToBecomeInvisible(elem, options) {\n  const results = { predicate: false, maxExceeded: false };\n  const opts = Object.assign({ pollRate: 1000, max: 15050 }, options);\n  return new Promise(resolve => {\n    if (!Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"isElemVisible\"])(elem)) {\n      results.predicate = true;\n      return resolve(results);\n    }\n    let safety;\n    const poll = setInterval(() => {\n      if (!Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"isElemVisible\"])(elem)) {\n        if (safety) clearTimeout(safety);\n        clearInterval(poll);\n        results.predicate = true;\n        return resolve(results);\n      }\n    }, opts.pollRate);\n    if (opts.max !== -1) {\n      safety = setTimeout(() => {\n        clearInterval(poll);\n        results.predicate = !Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"isElemVisible\"])(elem);\n        results.maxExceeded = true;\n        resolve(results);\n      }, opts.max);\n    }\n  });\n}\n\n/**\n * Waits for the supplied element computed style display or visibility state\n * the element is visible.\n * The condition, element becomes visible, is polled at the configured\n * `pollRate` (defaults to 1 second) for a maximum of `max` time (defaults to 15 seconds)\n * unless `max = -1` which in this case there is no maximum.\n * @param {SomeElement} elem - The element that should be removed\n * @param {WaitForOptions} [options] - Options controlling the wait\n * @return {Promise<WaitResult>}\n */\nfunction waitForElementToBecomeVisible(elem, options) {\n  const results = { predicate: false, maxExceeded: false };\n  const opts = Object.assign({ pollRate: 1000, max: 15050 }, options);\n  return new Promise(resolve => {\n    if (!Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"isElemVisible\"])(elem)) {\n      results.predicate = true;\n      return resolve(results);\n    }\n    let safety;\n    const poll = setInterval(() => {\n      if (Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"isElemVisible\"])(elem)) {\n        if (safety) clearTimeout(safety);\n        clearInterval(poll);\n        results.predicate = true;\n        return resolve(results);\n      }\n    }, opts.pollRate);\n    if (opts.max !== -1) {\n      safety = setTimeout(() => {\n        clearInterval(poll);\n        results.predicate = Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"isElemVisible\"])(elem);\n        results.maxExceeded = true;\n        resolve(results);\n      }, opts.max);\n    }\n  });\n}\n/**\n * @typedef {Object} WaitForOptions\n * @property {number} [pollRate = 1000] - The rate at which the condition will be checked\n * @property {?number} [max = 15000] - The maximum length of time the polling will happen in milliseconds\n */\n\n/**\n * @typedef {Object} GuardedWaitForOptions\n * @property {function(): boolean} [guard] - Function polled to determine if the wait\n * is considered done by additional conditions\n * @property {number} [pollRate = 1000] - The rate at which the condition will be checked\n * @property {?number} [max] - The maximum length of time the polling will happen in milliseconds\n */\n\n/**\n * @typedef {Object} WaitResult\n * @property {boolean} predicate - T/F indicating if the stopping condition(s) of a wait was met, this includes guards\n * @property {boolean} maxExceeded - T/F indicating if the configured maximum wait time was exceeded\n */\n\n/**\n * @typedef {Object} WaitForSelectorOptions\n * @property {string} selector - The selector to be waited for\n * @param {SomeElement|Document} [cntx] - element to use rather than document for the querySelector call\n * @param {WaitForOptions} [options] - Options controlling the wait\n */\n\n\n//# sourceURL=webpack://lib/./lib/delays.js?");

/***/ }),

/***/ "./lib/dom.js":
/*!********************!*\
  !*** ./lib/dom.js ***!
  \********************/
/*! exports provided: xpathNumberQuery, xpathSnapShot, xpathSnapShotArray, maybePolyfillXPG, qs, filteredQs, qsa, repeatedQSAIterator, id, maybeRemoveElem, maybeRemoveElemById, elemOffsetTopZero, markElemAsVisited, addBehaviorStyle, canAccessIf, selectorExists, idExists, findTag, attr, attrEq, nthChildNodeOf, chainNthChildNodeOf, firstChildElemOfParent, firstChildElementOf, firstChildElementOfSelector, nthPreviousSibling, nthChildElementOf, chainNthChildElementOf, chainFistChildElemOf, lastChildElementOf, lastChildElementOfSelector, chainLastChildElemOf, numElemChildren, chainQs, addClass, removeClass, hasClass, hasClasses, hasAnyClass, elemMatchesSelector, isClasslessElem, getElemSibling, getElemsParentsSibling, elemHasSibling, getElemSiblingAndRemoveElem, elemHasZeroBoundingRect, isElemVisible, getNthParentElement, elementTextContains, elementTextEqs, elementTextStartsWithAndEndsWith, elementTextStartsWith, elementTextEndsWith, splitElemInnerText, splitElemTextContents, elemInnerText, elemInnerTextMatchesRegex, innerTextOfSelected, elemInnerTextEqs, elemInnerTextEqsInsensitive, elemInnerTextEqsOneOf, elemTextContent, documentScrollPosition, getElementPositionWidthHeight, getElementClientPagePosition, getElementClientPageCenter, anySelectorExists, elementsNameEquals, nodesNameEquals, xpathOneOf, qsOneOf, qsaOneOf, selectedNextElementSibling, selectedPreviousElementSibling, docBaseURIEndsWith, docBaseURIEquals, repeatedXpathQueryIterator, repeatedXpathQueryIteratorAsync, elemDataValue, elemDataValueEqs, elemHasChildren, childElementIterator, childElementIteratorOfSelected, childNodeIterator, findDirectChildElement, waitForEventTargetToFireEvent, loadPageViaIframe */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"xpathNumberQuery\", function() { return xpathNumberQuery; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"xpathSnapShot\", function() { return xpathSnapShot; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"xpathSnapShotArray\", function() { return xpathSnapShotArray; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"maybePolyfillXPG\", function() { return maybePolyfillXPG; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"qs\", function() { return qs; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"filteredQs\", function() { return filteredQs; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"qsa\", function() { return qsa; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"repeatedQSAIterator\", function() { return repeatedQSAIterator; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"id\", function() { return id; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"maybeRemoveElem\", function() { return maybeRemoveElem; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"maybeRemoveElemById\", function() { return maybeRemoveElemById; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemOffsetTopZero\", function() { return elemOffsetTopZero; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"markElemAsVisited\", function() { return markElemAsVisited; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"addBehaviorStyle\", function() { return addBehaviorStyle; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"canAccessIf\", function() { return canAccessIf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectorExists\", function() { return selectorExists; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"idExists\", function() { return idExists; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"findTag\", function() { return findTag; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"attr\", function() { return attr; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"attrEq\", function() { return attrEq; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"nthChildNodeOf\", function() { return nthChildNodeOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"chainNthChildNodeOf\", function() { return chainNthChildNodeOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"firstChildElemOfParent\", function() { return firstChildElemOfParent; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"firstChildElementOf\", function() { return firstChildElementOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"firstChildElementOfSelector\", function() { return firstChildElementOfSelector; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"nthPreviousSibling\", function() { return nthPreviousSibling; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"nthChildElementOf\", function() { return nthChildElementOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"chainNthChildElementOf\", function() { return chainNthChildElementOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"chainFistChildElemOf\", function() { return chainFistChildElemOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"lastChildElementOf\", function() { return lastChildElementOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"lastChildElementOfSelector\", function() { return lastChildElementOfSelector; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"chainLastChildElemOf\", function() { return chainLastChildElemOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"numElemChildren\", function() { return numElemChildren; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"chainQs\", function() { return chainQs; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"addClass\", function() { return addClass; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"removeClass\", function() { return removeClass; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"hasClass\", function() { return hasClass; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"hasClasses\", function() { return hasClasses; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"hasAnyClass\", function() { return hasAnyClass; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemMatchesSelector\", function() { return elemMatchesSelector; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isClasslessElem\", function() { return isClasslessElem; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getElemSibling\", function() { return getElemSibling; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getElemsParentsSibling\", function() { return getElemsParentsSibling; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemHasSibling\", function() { return elemHasSibling; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getElemSiblingAndRemoveElem\", function() { return getElemSiblingAndRemoveElem; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemHasZeroBoundingRect\", function() { return elemHasZeroBoundingRect; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isElemVisible\", function() { return isElemVisible; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getNthParentElement\", function() { return getNthParentElement; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elementTextContains\", function() { return elementTextContains; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elementTextEqs\", function() { return elementTextEqs; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elementTextStartsWithAndEndsWith\", function() { return elementTextStartsWithAndEndsWith; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elementTextStartsWith\", function() { return elementTextStartsWith; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elementTextEndsWith\", function() { return elementTextEndsWith; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"splitElemInnerText\", function() { return splitElemInnerText; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"splitElemTextContents\", function() { return splitElemTextContents; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemInnerText\", function() { return elemInnerText; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemInnerTextMatchesRegex\", function() { return elemInnerTextMatchesRegex; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"innerTextOfSelected\", function() { return innerTextOfSelected; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemInnerTextEqs\", function() { return elemInnerTextEqs; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemInnerTextEqsInsensitive\", function() { return elemInnerTextEqsInsensitive; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemInnerTextEqsOneOf\", function() { return elemInnerTextEqsOneOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemTextContent\", function() { return elemTextContent; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"documentScrollPosition\", function() { return documentScrollPosition; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getElementPositionWidthHeight\", function() { return getElementPositionWidthHeight; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getElementClientPagePosition\", function() { return getElementClientPagePosition; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getElementClientPageCenter\", function() { return getElementClientPageCenter; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"anySelectorExists\", function() { return anySelectorExists; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elementsNameEquals\", function() { return elementsNameEquals; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"nodesNameEquals\", function() { return nodesNameEquals; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"xpathOneOf\", function() { return xpathOneOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"qsOneOf\", function() { return qsOneOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"qsaOneOf\", function() { return qsaOneOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectedNextElementSibling\", function() { return selectedNextElementSibling; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectedPreviousElementSibling\", function() { return selectedPreviousElementSibling; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"docBaseURIEndsWith\", function() { return docBaseURIEndsWith; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"docBaseURIEquals\", function() { return docBaseURIEquals; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"repeatedXpathQueryIterator\", function() { return repeatedXpathQueryIterator; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"repeatedXpathQueryIteratorAsync\", function() { return repeatedXpathQueryIteratorAsync; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemDataValue\", function() { return elemDataValue; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemDataValueEqs\", function() { return elemDataValueEqs; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"elemHasChildren\", function() { return elemHasChildren; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"childElementIterator\", function() { return childElementIterator; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"childElementIteratorOfSelected\", function() { return childElementIteratorOfSelected; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"childNodeIterator\", function() { return childNodeIterator; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"findDirectChildElement\", function() { return findDirectChildElement; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"waitForEventTargetToFireEvent\", function() { return waitForEventTargetToFireEvent; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"loadPageViaIframe\", function() { return loadPageViaIframe; });\n/* harmony import */ var _strings__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./strings */ \"./lib/strings.js\");\n/* harmony import */ var _general__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./general */ \"./lib/general.js\");\n\n\n\n/**\n * Returns the results of evaluating the supplied\n * xpath query using an optional context `contextElement`, defaults\n * to document, as XPathResult.NUMBER_TYPE\n * @param {string} xpathQuery - The xpath query to be evaluated\n * @param {SomeElement|Document} [contextElement] - Optional\n * element to be used as the context of the evaluation\n * @return {number}\n */\nfunction xpathNumberQuery(xpathQuery, contextElement) {\n  const result = document.evaluate(\n    xpathQuery,\n    contextElement || document,\n    null,\n    XPathResult.NUMBER_TYPE,\n    null\n  );\n  return result.numberValue;\n}\n\n/**\n * Returns the results of evaluating the supplied\n * xpath query using an optional context `contextElement`, defaults\n * to document, as XPathResult.ORDERED_NODE_SNAPSHOT_TYPE\n * @param {string} xpathQuery - The xpath query to be evaluated\n * @param {SomeElement|Document} [contextElement] - Optional\n * element to be used as the context of the evaluation\n * @return {XPathResult} - The results of the xpath query evaluation\n * @see https://developer.mozilla.org/en-US/docs/Web/API/Document/evaluate\n */\nfunction xpathSnapShot(xpathQuery, contextElement) {\n  return document.evaluate(\n    xpathQuery,\n    contextElement || document,\n    null,\n    XPathResult.ORDERED_NODE_SNAPSHOT_TYPE,\n    null\n  );\n}\n\n/**\n * Provides the same functionality of the chrome console utility `$x`\n * but likely less performant\n * @param {string} xpathQuery - The xpath query to be evaluated\n * @param {SomeElement|Document} [contextElement] - Optional\n * element to be used as the context of the evaluation\n * @return {Array<SomeElement>} - The results of the xpath query evaluation\n * @see https://developer.mozilla.org/en-US/docs/Web/API/Document/evaluate\n * @see https://developers.google.com/web/tools/chrome-devtools/console/utilities\n */\nfunction xpathSnapShotArray(xpathQuery, contextElement) {\n  const snapShot = xpathSnapShot(xpathQuery, contextElement);\n  const elements = [];\n  const len = snapShot.snapshotLength;\n  for (var i = 0; i < len; i++) {\n    elements.push(snapShot.snapshotItem(i));\n  }\n  return elements;\n}\n\n/**\n * Ensures that if the value of the chrome console utility $x\n * is not the actual utility (jquery is on the page) the returned\n * function behaves exactly like it.\n * @param {function(...args: *): Array<SomeElement>} cliXPG\n * @return {function(...args: *): Array<SomeElement>}\n */\nfunction maybePolyfillXPG(cliXPG) {\n  if (\n    typeof cliXPG === 'function' &&\n    cliXPG.toString().includes('[Command Line API]')\n  ) {\n    return cliXPG;\n  }\n  return xpathSnapShotArray;\n}\n\n/**\n * Utility function for `(document||element).querySelector(selector)`\n * @param {string} selector - the selector to be use\n * @param {SomeElement|Document} [context] - element to use rather than document for the querySelector call\n * @return {?SomeElement}\n */\nfunction qs(selector, context) {\n  if (context != null) return context.querySelector(selector);\n  return document.querySelector(selector);\n}\n\n/**\n * Utility function for `(document||element).querySelector(selector)`\n * @param {string} selector - the selector to be use\n * @param {function(elem: SomeElement): boolean} filterFn\n * @param {SomeElement|Document} [context] - element to use rather than document for the querySelector call\n * @return {?SomeElement}\n */\nfunction filteredQs(selector, filterFn, context) {\n  const elem = qs(selector, context);\n  if (elem == null) return null;\n  if (elem && filterFn(elem)) return elem;\n  return null;\n}\n\n/**\n * Utility function for `document.querySelectorAll(selector)`\n * @param {string} selector - the selector to be use\n * @param {SomeElement|Document} [context] - element to use rather than document for the querySelector call\n * @return {NodeList<SomeElement>}\n */\nfunction qsa(selector, context) {\n  if (context != null) return context.querySelectorAll(selector);\n  return document.querySelectorAll(selector);\n}\n\n/**\n * Returns an iterator over the results of calling {@link qsa} with the supplied\n * selector and optional context element until the call to {@link qsa} returns no more results\n * @param {string} selector - the selector to be use\n * @param {SomeElement|Document} [context] - element to use rather than document for the querySelector call\n * @return {IterableIterator<SomeElement>}\n */\nfunction* repeatedQSAIterator(selector, context) {\n  let results = qsa(selector, context);\n  while (results.length) {\n    for (let i = 0; i < results.length; i++) {\n      yield results[i];\n    }\n    results = qsa(selector, context);\n  }\n}\n\n/**\n * Utility function for `document.getElementById(id)`\n * @param {string} eid - The id of the element to get\n * @param {SomeElement|Document} [context] - Optional document element to use rather than\n * the current JS context's\n * @return {?SomeElement}\n */\nfunction id(eid, context) {\n  if (context != null) return context.getElementById(eid);\n  return document.getElementById(eid);\n}\n\n/**\n * Removes the element selected by the supplied querySelector, if it exits,\n * returning true to indicate the element was removed and false otherwise\n * @param {string} selector - the selector to be use\n * @param {SomeElement|Document} [context] - element to use rather than document for the querySelector call\n * @return {boolean}\n */\nfunction maybeRemoveElem(selector, context) {\n  const elem = qs(selector, context);\n  if (elem) {\n    elem.remove();\n    return true;\n  }\n  return false;\n}\n\n/**\n * Removes the element with the supplied id, if it exits, returning\n * true to indicate the element was removed and false otherwise\n * @param {string} eid - The id of the element to remove\n * @param {SomeElement|Document} [context] - Optional document element to use rather than\n * the current JS context's\n * @return {boolean}\n */\nfunction maybeRemoveElemById(eid, context) {\n  const elem = id(eid, context);\n  if (elem) {\n    elem.remove();\n    return true;\n  }\n  return false;\n}\n\n/**\n * Returns true if the supplied elements `offsetTop === 0`\n * @param {SomeElement} elem - The element to check its\n * offsetTop\n * @return {boolean}\n */\nfunction elemOffsetTopZero(elem) {\n  return elem.offsetTop === 0;\n}\n\n/**\n * Marks the supplied element as visited by adding the marker\n * to its classList.\n * @param {SomeElement} elem - The element to mark\n * as visited\n * @param {string} [marker = 'wrvistited'] - Optional marker to use\n * defaults to `wrvistited`\n */\nfunction markElemAsVisited(elem, marker = 'wrvistited') {\n  if (elem != null) {\n    elem.classList.add(marker);\n  }\n}\n\n/**\n * Creates a style tag if one was not created before and adds\n * the supplied `styleDef` to it. If a style tag was created before\n * this function is a no-op\n * @param {string} styleDef - The CSS rules to add\n * @return {Object} - An object containing the selectors as key value pairs\n * where the keys are the selectors in camelcase and the value is the raw selector\n */\nfunction addBehaviorStyle(styleDef) {\n  let style = document.getElementById('$wrStyle$');\n  if (style == null) {\n    style = document.createElement('style');\n    style.id = '$wrStyle$';\n    document.head.appendChild(style);\n  }\n  style.textContent = styleDef;\n  const rules = style.sheet.rules;\n  let ruleIdx = rules.length;\n  let selector;\n  const classes = {};\n  while (ruleIdx--) {\n    selector = rules[ruleIdx].selectorText.replace('.', '');\n    classes[Object(_strings__WEBPACK_IMPORTED_MODULE_0__[\"camelCase\"])(selector)] = selector;\n  }\n  return classes;\n}\n\n/**\n * Determines if the supplied iframe is accessible from this\n * origin. Test is that access the window object of the iframes contentWindow\n * does not throw an exception and the contentDocument is not falsy.\n * @param {HTMLIFrameElement} iframe - The iframe to determine accessibility\n * @return {boolean} - True if the iframe is accessible and false otherwise\n */\nfunction canAccessIf(iframe) {\n  if (iframe == null) return false;\n  try {\n    iframe.contentWindow.window;\n  } catch (e) {\n    return false;\n  }\n  return iframe.contentDocument != null;\n}\n\n/**\n * Determines if the element the supplied selector selects exists\n * @param {string} selector - The querySelector to use for testing if\n * the element it selects exists\n * @param {Document|SomeElement} [cntx] - Optional element to use rather\n * than the current JS context's document object\n * @return {boolean} - True if the element exists, false otherwise\n */\nfunction selectorExists(selector, cntx) {\n  return qs(selector, cntx) != null;\n}\n\n/**\n * Determines if the element the supplied id identifies exists\n * @param {string} eid - The id of the element\n * @param {Document} [cntx] - Optional document object to use rather\n * than the current JS context's document object\n * @return {boolean} - True if the element exists, false otherwise\n */\nfunction idExists(eid, cntx) {\n  return id(eid, cntx) != null;\n}\n\n/**\n * Attempts to find a tag using the supplied function that accepts\n * an xpath query and an optional starting element and returns\n * the element the supplied predicate function returns a truthy value for\n * @param {function(query: string, node: ?Node): Node[]} xpg - xpath execution function\n * @param {string} tag - The tag to be found\n * @param {function(elem: SomeElement): boolean} predicate - Element selecting predicate function\n * @param {Document|SomeElement} [cntx] - Optional starting element, defaults to `document`\n * @return {?SomeElement} - The desired element if it was found\n */\nfunction findTag(xpg, tag, predicate, cntx) {\n  const tags = xpg(`//${tag}`, cntx || document);\n  for (var i = 0; i < tags.length; ++i) {\n    if (predicate(tags[i])) return tags[i];\n  }\n  return null;\n}\n\n/**\n * Retrieves the value of an elements attribute if it exists\n * @param {SomeElement} elem - The element to retrieve an attribute from\n * @param {string} attr - The name of the attribute to be retrieved\n * @return {*} - The value of the retrieved attribute if it exists\n */\nfunction attr(elem, attr) {\n  if (elem) return elem.getAttribute(attr);\n  return null;\n}\n\n/**\n * Tests to determine if the value of elements attribute equals\n * the supplied value using loose equality\n * @param {SomeElement} elem - The element to retrieve an attribute from\n * @param {string} attr - The name of the attribute to be retrieved\n * @param {*} shouldEq - The value the attributes value should equal\n * @return {boolean} - T/F indicating if the attribute equals. Note\n * false can indicate the attribute does not equal expected or\n * the element was null/undefined\n */\nfunction attrEq(elem, attr, shouldEq) {\n  if (elem) return elem.getAttribute(attr) == shouldEq;\n  return false;\n}\n\n/**\n * Returns the Nth child node of the supplied element (indexing assumes start is 1)\n * @param {SomeElement|Document} elem - The element to retrieve the nth child of\n * @param {number} nth - The number of the nth child\n * @return {?SomeElement} - The nth child if it exists\n */\nfunction nthChildNodeOf(elem, nth) {\n  if (elem && elem.children && elem.children.length >= nth) {\n    return elem.childNodes[nth - 1];\n  }\n  return null;\n}\n\n/**\n * Chains {@link nthChildNodeOf} for each nth child in `nths`\n * @param {SomeElement} startingElem - The starting parent element\n * @param {...number} nths - The consecutive nth child node\n * @return {?SomeElement}\n * @example\n * // child is the parent elements 3rd child node's 4th child node's 5th child node\n * const child = chainNthChildNodeOf(parentElement, 3, 4, 5);\n */\nfunction chainNthChildNodeOf(startingElem, ...nths) {\n  if (elemHasChildren(startingElem)) {\n    let child = startingElem;\n    for (var i = 0; i < nths.length; ++i) {\n      child = nthChildNodeOf(child, nths[i]);\n      if (child == null) break;\n    }\n    return child;\n  }\n  return null;\n}\n\n/**\n * Returns the first child element of the supplied elements parent element.\n * If the supplied element or the elements parent is null/undefined null is returned\n * @param {?SomeElement} elem - The element who's parents first child is desired\n * @return {?SomeElement}\n */\nfunction firstChildElemOfParent(elem) {\n  if (elem == null || elem.parentElement == null) return null;\n  return elem.parentElement.firstElementChild;\n}\n\n/**\n * Returns the supplied elements first element child. If the element is null\n * null is returned\n * @param {?SomeElement} elem - The element who's first child is desired\n * @returns {?SomeElement}\n */\nfunction firstChildElementOf(elem) {\n  if (elem != null) return elem.firstElementChild;\n  return null;\n}\n\n/**\n * Returns the first child element of the element matching the supplied selector\n * @param {string} selector - The selector to match the parent element of the desired child\n * @param {Document|SomeElement} [cntx] - Optional starting element, defaults to `document`\n * @returns {?SomeElement}\n */\nfunction firstChildElementOfSelector(selector, cntx) {\n  return firstChildElementOf(qs(selector, cntx));\n}\n\n/**\n * Returns the nth previous sibling of the supplied element if it is not null/undefined or the supplied element is not null/undefined\n * @param {?SomeElement} elem - The element who's nth previous sibling is desired\n * @param {number} nth - The nth previous sibling\n * @return {SomeElement}\n */\nfunction nthPreviousSibling(elem, nth) {\n  let prevSibling = elem;\n  for (let i = 0; i < nth; i++) {\n    if (!prevSibling) break;\n    prevSibling = prevSibling.previousElementSibling;\n  }\n  return prevSibling;\n}\n\n/**\n * Returns the Nth child element of the supplied element (indexing assumes start is 1)\n * @param {SomeElement|Document} elem - The element to retrieve the nth child element of\n * @param {number} nth - The number of the nth child element\n * @return {?SomeElement} - The nth child if it exists\n */\nfunction nthChildElementOf(elem, nth) {\n  if (!elem || !elem.firstElementChild) return null;\n  let child = elem.firstElementChild;\n  for (let i = 1; i < nth; i++) {\n    child = child.nextElementSibling;\n    if (!child) break;\n  }\n  return child;\n}\n\n/**\n * Chains {@link nthChildElementOf} for each nth child element in `nths`\n * @param {SomeElement} elem - The starting parent element\n * @param {...number} nths - The consecutive nth child node\n * @return {?SomeElement}\n * @example\n * // child is the parent elements 3rd child element's 4th child element's 5th child element\n * const child = chainNthChildElementOf(parentElement, 3, 4, 5);\n */\nfunction chainNthChildElementOf(elem, ...nths) {\n  let child = elem;\n  for (let i = 0; i < nths.length; i++) {\n    child = nthChildElementOf(child, nths[i]);\n    if (!child) break;\n  }\n  return child;\n}\n\n/**\n * Chains {@link firstChildElementOf} on the supplied element n `times`\n * @param {SomeElement} elem - The starting element\n * @param {number} times - How many times to call {@link firstChildElementOf}\n * @returns {?SomeElement}\n */\nfunction chainFistChildElemOf(elem, times) {\n  if (elem == null) return null;\n  let child = elem;\n  for (var i = 0; i < times; ++i) {\n    child = firstChildElementOf(child);\n    if (child == null) break;\n  }\n  return child;\n}\n\n/**\n * Returns the last child element of the supplied element\n * @param {?SomeElement} elem - The element who's last child element is desired\n * @returns {?SomeElement}\n */\nfunction lastChildElementOf(elem) {\n  if (elem != null) return elem.lastElementChild;\n  return null;\n}\n\n/**\n * Returns the last child element of the element the selector selects\n * @param {string} selector - The selector to be used to select the element who's last child element is desired\n * @param {Document|SomeElement} [cntx] - Optional starting element, defaults to `document`\n * @returns {?SomeElement}\n */\nfunction lastChildElementOfSelector(selector, cntx) {\n  return lastChildElementOf(qs(selector, cntx));\n}\n\n/**\n * Chains {@link lastChildElementOf} n `times` starting with the supplied element\n * @param {SomeElement} elem - The starting parent element who\n * @param {number} times - How many times should the\n * @returns {?SomeElement}\n */\nfunction chainLastChildElemOf(elem, times) {\n  let child = elem;\n  if (elem != null && elem.children && elem.children.length) {\n    for (var i = 0; i < times; ++i) {\n      child = lastChildElementOf(child);\n      if (child == null) break;\n    }\n  }\n  return child;\n}\n\n/**\n * Returns the number of child elements the supplied element has if it is not\n * null/undefined otherwise -1\n * @param {SomeElement} elem - The element who's child element count is deired\n * @return {number}\n */\nfunction numElemChildren(elem) {\n  if (elem != null) {\n    return elem.childElementCount;\n  }\n  return -1;\n}\n\n/**\n * Consecutively calls querySelector(selector) on the element returned by the previous invocation\n * @param {SomeElement|Document} startingSelectFrom - The first element to perform querySelector(startingSelector) on\n * @param {string} startingSelector - The first selector\n * @param {...string} selectors - Additional selections\n * @return {?SomeElement} - Final selected element if it exists\n */\nfunction chainQs(startingSelectFrom, startingSelector, ...selectors) {\n  let selected = qs(startingSelector, startingSelectFrom);\n  if (selected != null) {\n    const len = selectors.length;\n    for (var i = 0; i < len; ++i) {\n      selected = qs(selectors[i], selected);\n      if (selected == null) return null;\n    }\n  }\n  return selected;\n}\n\n/**\n * Adds a CSS classname to the supplied element\n * @param {?SomeElement} elem - The element to add the classname to\n * @param {string} clazz - The classname to be added\n */\nfunction addClass(elem, clazz) {\n  if (elem) {\n    elem.classList.add(clazz);\n  }\n}\n\n/**\n * Removes a CSS classname to the supplied element\n * @param {?SomeElement} elem - The element to remove the classname to\n * @param {string} clazz - The classname to be removed\n */\nfunction removeClass(elem, clazz) {\n  if (elem) {\n    elem.classList.remove(clazz);\n  }\n}\n\n/**\n * Tests to see if the supplied element has a css class\n * @param {?SomeElement} elem - The element to be tested if it has the class\n * @param {string} clazz - The classname\n * @return {boolean} - T/F indicating if the element has the class\n */\nfunction hasClass(elem, clazz) {\n  if (elem) return elem.classList.contains(clazz);\n  return false;\n}\n\n/**\n * Tests to see if the supplied element has the supplied css classes\n * @param {?SomeElement} elem - The element to be tested if it has the classes\n * @param {...string} classes - The classes the element must have\n * @return {boolean} - T/F indicating if the element has the classes\n */\nfunction hasClasses(elem, ...classes) {\n  if (!elem) return false;\n  for (let i = 0; i < classes.length; i++) {\n    if (!elem.classList.contains(classes[i])) return false;\n  }\n  return true;\n}\n\n/**\n * Tests to see if the supplied element has any of the supplied css classes\n * @param {?SomeElement} elem - The element to be tested if it has any of the classes\n * @param {...string} classes - The classes the element can have\n * @return {boolean} - T/F indicating if the element has any of the classes\n */\nfunction hasAnyClass(elem, ...classes) {\n  if (!elem) return false;\n  for (let i = 0; i < classes.length; i++) {\n    if (elem.classList.contains(classes[i])) return true;\n  }\n  return false;\n}\n\n/**\n * Returns T/F indicating if the element matches the supplied selector\n * @param {SomeElement} elem\n * @param {string} selector\n * @return {boolean}\n */\nfunction elemMatchesSelector(elem, selector) {\n  if (!elem) return false;\n  return elem.matches(selector);\n}\n\n/**\n * Returns T/F indicating if the supplied element has not CSS classes\n * @param {SomeElement} elem\n * @return {boolean}\n */\nfunction isClasslessElem(elem) {\n  return elem.classList.length === 0;\n}\n\n/**\n * Returns the supplied elements next element sibling\n * @param {SomeElement} elem - The element to receive its sibling\n * @return {?SomeElement} - The elements sibling if it exists\n */\nfunction getElemSibling(elem) {\n  if (!elem) return null;\n  return elem.nextElementSibling;\n}\n\n/**\n * Returns the next element sibling of the parent element of the\n * supplied element\n * @param {SomeElement} elem - The element to receive its sibling\n * @return {?SomeElement} - The elements sibling if it exists\n */\nfunction getElemsParentsSibling(elem) {\n  if (!elem) return null;\n  return getElemSibling(elem.parentElement);\n}\n\n/**\n * Returns T/F indicating if the supplied element has an sibling element\n * @param {SomeElement} elem\n * @return {boolean}\n */\nfunction elemHasSibling(elem) {\n  return getElemSibling(elem) != null;\n}\n\n/**\n * Returns the supplied elements next element sibling and removes the\n * supplied element\n * @param {SomeElement} elem - The element to receive its sibling\n * @return {?SomeElement} - The elements sibling if it exists\n */\nfunction getElemSiblingAndRemoveElem(elem) {\n  const sibling = getElemSibling(elem);\n  elem.remove();\n  return sibling;\n}\n\n/**\n * Determines if the supplied elements bounding client rect's\n * x,y,width,height,top,left properties all equal zero.\n * Note this function returns true if the element is null/undefined;\n * @param {SomeElement} elem - The element to be tested\n * @return {boolean} - T/F indicating if all zero or not.\n */\nfunction elemHasZeroBoundingRect(elem) {\n  if (elem == null) return true;\n  const rect = elem.getBoundingClientRect();\n  return (\n    rect.x === 0 &&\n    rect.y === 0 &&\n    rect.width === 0 &&\n    rect.height === 0 &&\n    rect.top === 0 &&\n    rect.left === 0\n  );\n}\n\n/**\n * Returns T/F indicating if the supplied element is visible.\n *\n * If the supplied element is falsy the return value is false.\n *\n * The test checks the computed style of the supplied element\n * to determine if it's css display property is not null and\n * the visibility of the element is visible.\n *\n * @param {SomeElement} elem\n * @return {boolean}\n */\nfunction isElemVisible(elem) {\n  if (elem == null) return false;\n  const computedStyle = window.getComputedStyle(elem);\n  if (computedStyle.display === 'none') return false;\n  return computedStyle.visibility === 'visible';\n}\n\n/**\n * Returns the Nth parent element of the supplied element (indexing assumes start is 1)\n * @param {SomeElement} elem - The element to retrieve the nth parent element of\n * @param {number} nth - The number of the nth parent\n * @return {?SomeElement} - The nth parent element if it exists\n */\nfunction getNthParentElement(elem, nth) {\n  if (elem != null && elem.parentElement != null && nth >= 1) {\n    let counter = nth - 1;\n    let parent = elem.parentElement;\n    while (counter > 0 && parent != null) {\n      parent = parent.parentElement;\n      counter--;\n    }\n    return parent;\n  }\n  return null;\n}\n\n/**\n * Returns T/F indicating if the supplied element's textContents contains the supplied string\n * @param {SomeElement} elem\n * @param {string} needle - The string that the elements textContents should contain\n * @param {boolean} [caseInsensitive] - Should the compairison be case insensitive\n * @returns {boolean}\n */\nfunction elementTextContains(elem, needle, caseInsensitive) {\n  if (elem != null && elem.textContent != null) {\n    const tc = elem.textContent;\n    return (caseInsensitive ? tc.toLowerCase() : tc).includes(needle);\n  }\n  return false;\n}\n\n/**\n * Returns T/F indicating if the supplied element's textContents equals the supplied string\n * @param {SomeElement} elem\n * @param {string} shouldEqual - The string that the elements textContents should be equal to\n * @param {boolean} [caseInsensitive] - Should the compairison be case insensitive\n * @returns {boolean}\n */\nfunction elementTextEqs(elem, shouldEqual, caseInsensitive) {\n  if (elem != null) {\n    const tc = elem.textContent;\n    return (caseInsensitive ? tc.toLowerCase() : tc) == shouldEqual;\n  }\n  return false;\n}\n\n/**\n * Returns T/F indicating if the supplied element' textContent starts and ends with the supplied start and end strings\n * @param {SomeElement} elem\n * @param {string} start - The string the element's textContent should start with\n * @param {string} end - The string the element's textContent should end with\n * @returns {boolean}\n */\nfunction elementTextStartsWithAndEndsWith(elem, start, end) {\n  return elementTextStartsWith(elem, start) && elementTextEndsWith(elem, end);\n}\n\n/**\n * Returns T/F indicating if the supplied element's textContent starts with the supplied string\n * @param {SomeElement} elem\n * @param {string} start - The string the element's textContent should start with\n * @returns {boolean}\n */\nfunction elementTextStartsWith(elem, start) {\n  if (elem != null && elem.textContent != null) {\n    return elem.textContent.startsWith(start);\n  }\n  return false;\n}\n\n/**\n * Returns T/F indicating if the supplied element's textContent ends with the supplied string\n * @param {SomeElement} elem\n * @param {string} end - The string the element's textContent should end with\n * @returns {boolean}\n */\nfunction elementTextEndsWith(elem, end) {\n  if (elem != null && elem.textContent != null) {\n    return elem.textContent.endsWith(end);\n  }\n  return false;\n}\n\n/**\n * Returns the results of splitting the supplied elements innerText using the supplied splitter\n * @param {SomeElement} elem\n * @param {string|RegExp} splitter\n * @returns {?Array<string>}\n */\nfunction splitElemInnerText(elem, splitter) {\n  if (elem != null && elem.innerText != null) {\n    return elem.innerText.split(splitter);\n  }\n  return null;\n}\n\n/**\n * Returns the results of splitting the supplied elements textContent using the supplied splitter\n * @param {SomeElement} elem\n * @param {string|RegExp} splitter\n * @returns {?Array<string>}\n */\nfunction splitElemTextContents(elem, splitter) {\n  if (elem != null && elem.textContent != null) {\n    return elem.textContent.split(splitter);\n  }\n  return null;\n}\n\n/**\n * Returns the supplied elements innerText\n * @param {?SomeElement} elem\n * @param {boolean} [trim] - Should the innerText be trimmed\n * @returns {?string}\n */\nfunction elemInnerText(elem, trim) {\n  if (elem != null && elem.innerText != null) {\n    return trim ? elem.innerText.trim() : elem.innerText;\n  }\n  return null;\n}\n\n/**\n * Returns T/F if the supplied elements innerText matches the supplied regex\n * @param {SomeElement} elem\n * @param {RegExp} regex\n * @return {boolean}\n */\nfunction elemInnerTextMatchesRegex(elem, regex) {\n  if (elem == null) return false;\n  return regex.test(elem.innerText);\n}\n\n/**\n * Returns the inner text of the element the supplied selectors matches\n * @param {string} selector - the selector to be use\n * @param {SomeElement|Document} [cntx] - element to use rather than document for the querySelector call\n * @return {?string}\n */\nfunction innerTextOfSelected(selector, cntx) {\n  return elemInnerText(qs(selector, cntx));\n}\n\n/**\n * Returns T/F if the supplied elements innerText equals the supplied string case sensitive\n * @param {SomeElement} elem\n * @param {string} shouldEqual - The string the elements inner text should equal\n * @param {boolean} [trim = false] - Should the innerText be trimmed before comparison\n * @return {boolean}\n */\nfunction elemInnerTextEqs(elem, shouldEqual, trim = false) {\n  if (elem == null || !elem.innerText) return false;\n  const innerText = trim ? elem.innerText.trim() : elem.innerText;\n  return innerText === shouldEqual;\n}\n\n/**\n * Returns T/F if the supplied elements innerText equals the supplied string case in-sensitive\n * @param {SomeElement} elem\n * @param {string} shouldEqual\n * @param {boolean} [trim = false]\n * @return {boolean}\n */\nfunction elemInnerTextEqsInsensitive(elem, shouldEqual, trim = false) {\n  if (elem == null || !elem.innerText) return false;\n  const innerText = trim ? elem.innerText.trim() : elem.innerText;\n  return innerText.toLowerCase() === shouldEqual;\n}\n\n/**\n * Returns T/F if the supplied elements innerText equals one of the supplied string case sensitive\n * @param {SomeElement} elem\n * @param {...string} shouldEquals\n * @return {boolean}\n */\nfunction elemInnerTextEqsOneOf(elem, ...shouldEquals) {\n  if (elem != null && elem.innerText != null) return false;\n  const innertText = elem.innerText;\n  for (var i = 0; i < shouldEquals.length; ++i) {\n    if (innertText === shouldEquals[i]) return true;\n  }\n  return false;\n}\n\n/**\n * Returns the textContent of the supplied element\n * @param {SomeElement} elem\n * @returns {?string}\n */\nfunction elemTextContent(elem) {\n  if (elem != null && elem.textContent != null) return elem.textContent;\n  return null;\n}\n\n/**\n * Returns the scrollTop and scrollLeft values of the supplied document if one\n * was supplied other wise the values are from the current document\n * @param {?Document} [doc]\n * @return {{scrollTop: number, scrollLeft: number}}\n */\nfunction documentScrollPosition(doc) {\n  const documentElem = doc != null ? doc : document;\n  const elem = documentElem.body\n    ? documentElem.body\n    : documentElem.documentElement;\n  return {\n    scrollTop: elem.scrollTop,\n    scrollLeft: elem.scrollLeft,\n  };\n}\n\n/**\n * Helper function to get the actual x and y position,\n * page x and y position (scroll) and the height, width of a given element\n * @param {Element} element\n * @param {Document} [doc]\n * @return {?{y: number, pageY: number, x: number, pageX: number, w: number, h: number}}\n */\nfunction getElementPositionWidthHeight(element, doc) {\n  if (element == null) return null;\n  const rect = element.getBoundingClientRect();\n  const scrollPos = documentScrollPosition(doc);\n  return {\n    y: rect.top,\n    x: rect.left,\n    pageY: rect.top + scrollPos.scrollTop,\n    pageX: rect.left + scrollPos.scrollLeft,\n    w: rect.width,\n    h: rect.height,\n  };\n}\n\n/**\n * Get the position of the passed DOM element\n * @param {SomeElement} element\n * @param {Object} options\n * @return {{clientY: number, clientX: number, pageY: number, pageX: number}}\n */\nfunction getElementClientPagePosition(element, options) {\n  const opts = Object.assign({ x: 1, y: 1, floor: false }, options);\n  const cords = getElementPositionWidthHeight(element, opts.doc);\n  const clientX = cords.x + (cords.w / 100) * opts.x;\n  const clientY = cords.y + (cords.h / 100) * opts.y;\n  const pageX = cords.pageX + (cords.w / 100) * opts.x;\n  const pageY = cords.pageY + (cords.h / 100) * opts.y;\n  return {\n    clientX: opts.floor ? Math.floor(clientX) : clientX,\n    clientY: opts.floor ? Math.floor(clientY) : clientY,\n    pageX: opts.floor ? Math.floor(pageX) : pageX,\n    pageY: opts.floor ? Math.floor(pageY) : pageY,\n  };\n}\n\n/**\n * Get the center of the passed DOM element\n * @param {SomeElement} element\n * @param {{floor: boolean}} [options]\n * @return {?{clientY: number, clientX: number, pageY: number, pageX: number}}\n */\nfunction getElementClientPageCenter(element, options) {\n  if (element == null) return null;\n  const opts = Object.assign({ floor: false }, options);\n  const cords = getElementPositionWidthHeight(element, opts.doc);\n  const clientX = cords.x + cords.w / 2;\n  const clientY = cords.y + cords.h / 2;\n  const pageX = cords.pageX + cords.w / 2;\n  const pageY = cords.pageY + cords.h / 2;\n  return {\n    clientX: opts.floor ? Math.floor(clientX) : clientX,\n    clientY: opts.floor ? Math.floor(clientY) : clientY,\n    pageX: opts.floor ? Math.floor(pageX) : pageX,\n    pageY: opts.floor ? Math.floor(pageY) : pageY,\n  };\n}\n\n/**\n * Determines if the element the supplied selector selects exists.\n *\n * If one of the supplied selectors matches an existing element the idx property\n * of the returned object is set to the index of the selector in the array and\n * the success property is set to true.\n *\n * Otherwise idx = -1 and success = false.\n * @param {Array<string>} selectors - The query selectors to use for testing if\n * the elements it selects exist\n * @param {SomeElement} [cntx] - Optional element to use rather\n * than the current JS context's document object\n * @return {{idx: number, success: boolean}} - The results of the selectors\n * existence check\n */\nfunction anySelectorExists(selectors, cntx) {\n  const numSelectors = selectors.length;\n  for (var i = 0; i < numSelectors; ++i) {\n    if (selectorExists(selectors[i], cntx)) {\n      return { idx: i, success: true };\n    }\n  }\n  return { idx: -1, success: false };\n}\n\n/**\n * Returns T/F indicating if the elements name (localName) is equal to the supplied name\n * @param {SomeElement} elem - The element to check if its name equals the supplied name\n * @param {string} name - The name of the desired element\n * @return {boolean}\n */\nfunction elementsNameEquals(elem, name) {\n  if (!elem) return false;\n  return elem.localName === name;\n}\n\n/**\n * Returns T/F indicating if the nodes name (nodeName) is equal to the supplied name\n * @param {SomeElement} node - The node to check if its name equals the supplied name\n * @param {string} name - The name of the desired Node\n * @return {boolean}\n */\nfunction nodesNameEquals(node, name) {\n  if (!node) return false;\n  return node.nodeName === name;\n}\n\n/**\n * @typedef {Object} XPathOnOfOpts\n * @property {Array<string>} queries\n * @property {*} xpg\n * @property {*} [context]\n */\n\n/**\n * Returns the results of evaluating one of the supplied xpath queries if\n * one of the queries yields results otherwise null/undefined\n * @param {XPathOnOfOpts} options\n * @return {?Array<SomeElement>}\n */\nfunction xpathOneOf({ queries, xpg, context }) {\n  let results = null;\n  for (var i = 0; i < queries.length; i++) {\n    results = xpg(queries[i], context);\n    if (results.length || results.snapshotLength) return results;\n  }\n  return results;\n}\n\n/**\n * Returns the results of querySelector using one of the supplied selectors\n * one if one of the selectors yields results otherwise null/undefined\n * @param {{selectors: Array<string>, context: *}} options\n * @return {?SomeElement}\n */\nfunction qsOneOf({ selectors, context }) {\n  if (selectors == null) return null;\n  let results = null;\n  for (var i = 0; i < selectors.length; i++) {\n    results = qs(selectors[i], context);\n    if (results) return results;\n  }\n  return results;\n}\n\n/**\n * Returns the results of querySelectorAll using one of the supplied\n * selectors if one of the selectors yields results otherwise null/undefined\n * @param {{selectors: Array<string>, context: *}} options - Optional document object to use rather than\n * defaulting to the current execution contexts document object\n * @return {?NodeList<SomeElement>}\n */\nfunction qsaOneOf({ selectors, context }) {\n  if (selectors == null) return null;\n  let results = null;\n  for (var i = 0; i < selectors.length; i++) {\n    results = qsa(selectors[i], context);\n    if (results.length) return results;\n  }\n  return results;\n}\n\n/**\n * Returns the next element sibling of the supplied selector IFF\n * the selector returns a results and the next element sibling exists\n * otherwise null/undefined\n * @param {string} selector - The selector for the element who's next sibling is\n * to be returned\n * @param {SomeElement|Document} [context] - element to use rather than document for the querySelector call\n * @return {?SomeElement}\n */\nfunction selectedNextElementSibling(selector, context) {\n  const maybeSelected = qs(selector, context);\n  if (maybeSelected && maybeSelected.nextElementSibling) {\n    return maybeSelected.nextElementSibling;\n  }\n  return null;\n}\n\n/**\n * Returns the previous element sibling of the supplied selector IFF\n * the selector returns a results and the next element sibling exists\n * otherwise null/undefined\n * @param {string} selector - The selector for the element who's previous sibling is\n * to be returned\n * @param {SomeElement|Document} [context] - element to use rather than document for the querySelector call\n * @return {?SomeElement}\n */\nfunction selectedPreviousElementSibling(selector, context) {\n  const maybeSelected = qs(selector, context);\n  if (maybeSelected && maybeSelected.previousElementSibling) {\n    return maybeSelected.previousElementSibling;\n  }\n  return null;\n}\n\n/**\n * Returns T/F indicating if the documents baseURI ends with the supplied string\n * @param {string} shouldEndWith - What the documents base URI should end with\n * @param {Document} [cntxDoc] - Optional document object to use rather than\n * defaulting to the current execution contexts document object\n * @return {boolean}\n */\nfunction docBaseURIEndsWith(shouldEndWith, cntxDoc) {\n  if (!shouldEndWith) return false;\n  return (cntxDoc || document).baseURI.endsWith(shouldEndWith);\n}\n\n/**\n * Returns T/F indicating if the documents baseURI equals the supplied string\n * @param {string} shouldEqual - What the documents base URI should be equal to\n * @param {Document} [cntxDoc] - Optional document object to use rather than\n * defaulting to the current execution contexts document object\n * @return {boolean}\n */\nfunction docBaseURIEquals(shouldEqual, cntxDoc) {\n  if (!shouldEqual) return false;\n  return (cntxDoc || document).baseURI === shouldEqual;\n}\n\n/**\n * Repeatably performs the supplied xpath query yielding the results\n * of the each query. If an empty result set is encountered and generateMoreElements\n * function was supplied it is called and the query repeated. If the second query\n * try yields another empty set the iterator ends\n * @param {string} query - The xpath query to be repeated until it returns no more elements\n * @param [cntx] - Optional element to execute the xpath query from (defaults to document)\n * @param {function(): void} [generateMoreElements] - Optional function used to generate more elements that may match the supplied xpath\n * @return {IterableIterator<SomeElement>}\n */\nfunction* repeatedXpathQueryIterator(query, cntx, generateMoreElements) {\n  let snapShot = xpathSnapShot(query, cntx);\n  const haveGenMore = typeof generateMoreElements === 'function';\n  while (snapShot.snapshotLength > 0) {\n    for (let i = 0; i < snapShot.snapshotLength; i++) {\n      yield snapShot.snapshotItem(i);\n    }\n    snapShot = xpathSnapShot(query, cntx);\n    if (snapShot.snapshotLength === 0) {\n      if (haveGenMore) generateMoreElements();\n      snapShot = xpathSnapShot(query, cntx);\n    }\n  }\n}\n\n/**\n * Repeatably performs the supplied xpath query yielding the results\n * of the each query. If an empty result set is encountered and generateMoreElements\n * function was supplied it is called and the query repeated. If the second query\n * try yields another empty set the iterator ends\n * @param {string} query - The xpath query to be repeated until it returns no more elements\n * @param [cntx] - Optional element to execute the xpath query from (defaults to document)\n * @param {function(): *} [generateMoreElements] - Optional function used to generate more elements that may match the supplied xpath\n * @return {AsyncIterableIterator<SomeElement>}\n */\nasync function* repeatedXpathQueryIteratorAsync(\n  query,\n  cntx,\n  generateMoreElements\n) {\n  let snapShot = xpathSnapShot(query, cntx);\n  let i;\n  const haveGenMore = typeof generateMoreElements === 'function';\n  while (snapShot.snapshotLength > 0) {\n    for (i = 0; i < snapShot.snapshotLength; i++) {\n      yield snapShot.snapshotItem(i);\n    }\n    snapShot = xpathSnapShot(query, cntx);\n    if (snapShot.snapshotLength === 0) {\n      if (haveGenMore) {\n        const result = generateMoreElements();\n        if (Object(_general__WEBPACK_IMPORTED_MODULE_1__[\"isPromise\"])(result)) await result;\n      }\n      snapShot = xpathSnapShot(query, cntx);\n    }\n  }\n}\n\n/**\n * Returns the value of the elements data attribute if it exists\n * If the element is null/undefined or does not have data attributes null is returned.\n * @param {HTMLElement|SVGElement} elem - The element that should have the data attribute\n * @param {string} dataKey - The name of the data value to be retrieved\n * @return {?string}\n */\nfunction elemDataValue(elem, dataKey) {\n  if (!elem) return null;\n  if (!elem.dataset) return null;\n  return elem.dataset[dataKey];\n}\n\n/**\n * Returns T/F indicating if the element has data attribute equal to the supplied value\n * @param {HTMLElement|SVGElement} elem - The element that should have the data attribute\n * @param dataKey - The name of the data value to be retrieved\n * @param dataValue - The expected value of the elements data attribute\n * @return {boolean}\n */\nfunction elemDataValueEqs(elem, dataKey, dataValue) {\n  if (!elem || !elem.dataset) return false;\n  return elem.dataset[dataKey] === dataValue;\n}\n\n/**\n * Returns T/F indicating if the supplied element has children.\n * If the supplied element is null/undefined false is returned.\n * @param {Element?} elem - The element to be checked for children\n * @return {boolean}\n */\nfunction elemHasChildren(elem) {\n  if (elem == null) return false;\n  if (typeof elem.hasChildNodes === 'function') {\n    return elem.hasChildNodes();\n  }\n  return elem.children.length > 0;\n}\n\n/**\n * Returns an iterator over the supplied parent element's child elements that\n * ends once the current child has no next element sibling.\n *\n * If the parent element is null/undefined then the returned iterator yields nothing.\n * @param {SomeElement?} parentElement - The parent element who's child elements will be iterated over\n * @return {IterableIterator<SomeElement?>}\n */\nfunction* childElementIterator(parentElement) {\n  if (parentElement == null) return;\n  let child = parentElement.firstElementChild;\n  while (child != null) {\n    yield child;\n    child = child.nextElementSibling;\n  }\n}\n\n/**\n * Returns an iterator over the child elements of the selected element\n * @param {string} selector - The selector to be used to select the parent element\n * @param {SomeElement} [cntx] - Optional element to use rather than document for the querySelector call\n * @return {IterableIterator<SomeElement>}\n */\nfunction childElementIteratorOfSelected(selector, cntx) {\n  const parent = qs(selector, cntx);\n  return childElementIterator(parent);\n}\n\n/**\n * Returns an iterator over the supplied parent element's child nodes that\n * ends once the current child has no next sibling.\n *\n * If the parent element is null/undefined then the returned iterator yields nothing.\n * @param {SomeElement?} parentElement - The parent element who's child nodes will be iterated over\n * @return {IterableIterator<Node?>}\n */\nfunction* childNodeIterator(parentElement) {\n  if (parentElement == null) return;\n  let child = parentElement.firstChild;\n  while (child != null) {\n    yield child;\n    child = child.nextSibling;\n  }\n}\n\n/**\n * Applies the supplied `predicate` to every child element of the supplied\n * parent element and returns the element that the `predicate` returns true for\n * otherwise returns null.\n *\n * If the parent element is null then null is returned.\n * @param {SomeElement} parentElement - The parent element who's child element will be searched\n * @param {function(elem: SomeElement): boolean} predicate - The predicate function used to select a child\n * @return {?SomeElement}\n */\nfunction findDirectChildElement(parentElement, predicate) {\n  if (parentElement == null || !Object(_general__WEBPACK_IMPORTED_MODULE_1__[\"isFunction\"])(predicate)) return null;\n  for (let i = 0; i < parentElement.children.length; i++) {\n    if (predicate(parentElement.children[i])) return parentElement.children[i];\n  }\n  return null;\n}\n\n/**\n * Returns a promise that resolves to true once the supplied event is fired from the supplied\n * event target. If `max` is supplied and the event is not fired by `max` the promise resolves to false.\n * @param {EventTarget} eventTarget - The event target that should fire `event`\n * @param {string} event - The event that should be fired from `eventTarget`\n * @param {number} [max] - Optional amount of time in milliseconds that defines\n * a maximum time to be waited for `event`\n * @return {Promise<boolean>}\n */\nfunction waitForEventTargetToFireEvent(eventTarget, event, max) {\n  const promResolveReject = Object(_general__WEBPACK_IMPORTED_MODULE_1__[\"promiseResolveReject\"])();\n  const listener = fromSafety => {\n    eventTarget.removeEventListener(event, listener);\n    promResolveReject.resolve(!fromSafety);\n  };\n  eventTarget.addEventListener(event, listener);\n  if (max) {\n    setTimeout(listener, max, true);\n  }\n  return promResolveReject.promise;\n}\n\n/**\n * Loads the supplied URL in an invisible iframe, waits for the iframes load\n * event, and then removes the iframe\n * @param {string} pageURL\n * @return {Promise<void>}\n */\nasync function loadPageViaIframe(pageURL) {\n  const iframe = document.createElement('iframe');\n  iframe.src = pageURL;\n  iframe.setAttribute(\n    'style',\n    `width: 100vw; \nheight: 100vh;\nopacity: 0;\nvisibility: hidden;\n`\n  );\n  const loadProm = waitForEventTargetToFireEvent(iframe, 'load');\n  document.body.appendChild(iframe);\n  await loadProm;\n  iframe.remove();\n}\n\n/**\n * @typedef {HTMLAnchorElement|HTMLElement|HTMLAppletElement|HTMLAreaElement|HTMLAudioElement|HTMLBaseElement|HTMLBaseFontElement|HTMLQuoteElement|HTMLBodyElement|HTMLBRElement|HTMLButtonElement|HTMLCanvasElement|HTMLTableCaptionElement|HTMLTableColElement|HTMLDataElement|HTMLDataListElement|HTMLModElement|HTMLDetailsElement|HTMLDialogElement|HTMLDirectoryElement|HTMLDivElement|HTMLDListElement|HTMLEmbedElement|HTMLFieldSetElement|HTMLFontElement|HTMLFormElement|HTMLFrameElement|HTMLFrameSetElement|HTMLHeadingElement|HTMLHeadElement|HTMLHRElement|HTMLHtmlElement|HTMLIFrameElement|HTMLImageElement|HTMLInputElement|HTMLLabelElement|HTMLLegendElement|HTMLLIElement|HTMLLinkElement|HTMLMapElement|HTMLMarqueeElement|HTMLMenuElement|HTMLMetaElement|HTMLMeterElement|HTMLObjectElement|HTMLOListElement|HTMLOptGroupElement|HTMLOptionElement|HTMLOutputElement|HTMLParagraphElement|HTMLParamElement|HTMLPictureElement|HTMLPreElement|HTMLProgressElement|HTMLScriptElement|HTMLSelectElement|HTMLSlotElement|HTMLSourceElement|HTMLSpanElement|HTMLStyleElement|HTMLTableElement|HTMLTableSectionElement|HTMLTableDataCellElement|HTMLTemplateElement|HTMLTextAreaElement|HTMLTableHeaderCellElement|HTMLTimeElement|HTMLTitleElement|HTMLTableRowElement|HTMLTrackElement|HTMLUListElement|HTMLVideoElement|Element|Node|SVGAElement|SVGCircleElement|SVGClipPathElement|SVGDefsElement|SVGDescElement|SVGEllipseElement|SVGFEBlendElement|SVGFEColorMatrixElement|SVGFEComponentTransferElement|SVGFECompositeElement|SVGFEConvolveMatrixElement|SVGFEDiffuseLightingElement|SVGFEDisplacementMapElement|SVGFEDistantLightElement|SVGFEFloodElement|SVGFEFuncAElement|SVGFEFuncBElement|SVGFEFuncGElement|SVGFEFuncRElement|SVGFEGaussianBlurElement|SVGFEImageElement|SVGFEMergeElement|SVGFEMergeNodeElement|SVGFEMorphologyElement|SVGFEOffsetElement|SVGFEPointLightElement|SVGFESpecularLightingElement|SVGFESpotLightElement|SVGFETileElement|SVGFETurbulenceElement|SVGFilterElement|SVGForeignObjectElement|SVGGElement|SVGImageElement|SVGLineElement|SVGLinearGradientElement|SVGMarkerElement|SVGMaskElement|SVGMetadataElement|SVGPathElement|SVGPatternElement|SVGPolygonElement|SVGPolylineElement|SVGRadialGradientElement|SVGRectElement|SVGScriptElement|SVGStopElement|SVGStyleElement|SVGSVGElement|SVGSwitchElement|SVGSymbolElement|SVGTextElement|SVGTextPathElement|SVGTitleElement|SVGTSpanElement|SVGUseElement|SVGViewElement} SomeElement\n */\n\n\n//# sourceURL=webpack://lib/./lib/dom.js?");

/***/ }),

/***/ "./lib/events.js":
/*!***********************!*\
  !*** ./lib/events.js ***!
  \***********************/
/*! exports provided: createMouseEvent, fireMouseEventsOnElement, fireEventOn */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"createMouseEvent\", function() { return createMouseEvent; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"fireMouseEventsOnElement\", function() { return fireMouseEventsOnElement; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"fireEventOn\", function() { return fireEventOn; });\n/* harmony import */ var _dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./dom */ \"./lib/dom.js\");\n/* harmony import */ var _general__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./general */ \"./lib/general.js\");\n\n\n\n/**\n * @param {string} type\n * @param {number} clickCount\n * @return {number}\n * @private\n */\nfunction __generateDetailPropForMouseEvent(type, clickCount) {\n  switch (type) {\n    case 'click':\n    case 'dblclick':\n      return clickCount;\n    case 'mousedown':\n    case 'mouseup':\n      return clickCount + 1;\n    default:\n      return 0;\n  }\n}\n\n/**\n * @param {string} type\n * @param {boolean} [buttons]\n * @return {number}\n * @private\n */\nfunction __getWhichButtonForEvent(type, buttons) {\n  switch (type) {\n    case 'click':\n    case 'dblclick':\n    case 'mousedown':\n    case 'mouseup':\n      return buttons ? 1 : 0;\n    default:\n      return 0;\n  }\n}\n\n/**\n * @typedef {Object} CreateMouseEventOptions\n * @see https://developer.mozilla.org/en-US/docs/Web/API/MouseEvent/MouseEvent\n * @property {string} type - The type of the mouse event to be created\n * @property {Element} [elem] - The element the mouse event will be fired on used to calculate mouse position\n * @property {Object} [position] - An object specifying the events position\n * @property {Object} [eventOpts] - Additional options to be used\n * @property {Window} [view] - The view (window)\n * @property {number} [clickCount] - How many clicks have been done before this event\n */\n\n/**\n * Creates and returns a new mouse event configured using the supplied options\n * @param {CreateMouseEventOptions} config\n * @return {MouseEvent}\n */\nfunction createMouseEvent(config) {\n  let eventPosition;\n  if (config.elem && config.position == null) {\n    eventPosition = Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"getElementClientPageCenter\"])(config.elem);\n    // my internet skills did not turn up anything on how to fake\n    // event cords in screen space.... so we just put something here\n    eventPosition.screenX = eventPosition.clientX + eventPosition.pageX;\n    eventPosition.screenY = eventPosition.clientY + eventPosition.pageY;\n  } else {\n    eventPosition = config.position;\n  }\n  const defaultOpts = {\n    button: __getWhichButtonForEvent(config.type),\n    buttons: __getWhichButtonForEvent(config.type, true),\n    bubbles: true,\n    cancelable: true,\n    composed: true,\n    ctrlKey: false,\n    shiftKey: false,\n    altKey: false,\n    metaKey: false,\n    view: config.view || window,\n    detail: __generateDetailPropForMouseEvent(\n      config.type,\n      config.clickCount || 0\n    ),\n  };\n  const eventOpts = Object.assign(defaultOpts, eventPosition, config.eventOpts);\n  return config.view != null\n    ? new config.view.MouseEvent(config.type, eventOpts)\n    : new MouseEvent(config.type, eventOpts);\n}\n\n/**\n * @typedef {Object} FireMouseEventsOnOptions\n * @property {Element} elem - The element the mouse event will be fired on used to calculate mouse position\n * @property {Array<string>} eventNames - The event types to be fired\n * @property {Window} [view] - The view (window)\n * @property {Object} [position] - An object specifying the events position\n * @property {Object} [eventOpts] - Additional options to be used\n * @property {number} [clickCount] - How many clicks have been done before this event\n */\n\n/**\n * Creates mouse events and fires them in order configured using the supplied options\n * @param {FireMouseEventsOnOptions} config\n */\nfunction fireMouseEventsOnElement(config) {\n  const { elem, eventNames, view, position, eventOpts, clickCount } = config;\n  if (elem != null) {\n    const numEvents = eventNames.length;\n    for (var eventIdx = 0; eventIdx < numEvents; ++eventIdx) {\n      elem.dispatchEvent(\n        createMouseEvent({\n          type: eventNames[eventIdx],\n          elem,\n          view,\n          position,\n          eventOpts,\n          clickCount,\n        })\n      );\n    }\n  }\n}\n\n/**\n * Fires the supplied event on the supplied element\n * @param {Element} elem - The element to have an event fired on\n * @param {Event} event - The event to be fired\n */\nfunction fireEventOn(elem, event) {\n  if (elem != null) {\n    elem.dispatchEvent(event);\n  }\n}\n\n\n//# sourceURL=webpack://lib/./lib/events.js?");

/***/ }),

/***/ "./lib/general.js":
/*!************************!*\
  !*** ./lib/general.js ***!
  \************************/
/*! exports provided: getViaPath, autoFetchFromDoc, sendAutoFetchWorkerURLs, noop, autobind, objectHasProps, globalWithPropsExist, extractProps, compose, composeAsync, composeIterators, composeAsyncIterators, promiseResolveReject, objectInstanceOf, partial, partialRight, uuidv4, isGenerator, isPromise, isFunction, mapAsyncIterator, noExceptGeneratorWrap */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getViaPath\", function() { return getViaPath; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"autoFetchFromDoc\", function() { return autoFetchFromDoc; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"sendAutoFetchWorkerURLs\", function() { return sendAutoFetchWorkerURLs; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"noop\", function() { return noop; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"autobind\", function() { return autobind; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"objectHasProps\", function() { return objectHasProps; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"globalWithPropsExist\", function() { return globalWithPropsExist; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"extractProps\", function() { return extractProps; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"compose\", function() { return compose; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"composeAsync\", function() { return composeAsync; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"composeIterators\", function() { return composeIterators; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"composeAsyncIterators\", function() { return composeAsyncIterators; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"promiseResolveReject\", function() { return promiseResolveReject; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"objectInstanceOf\", function() { return objectInstanceOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"partial\", function() { return partial; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"partialRight\", function() { return partialRight; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"uuidv4\", function() { return uuidv4; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isGenerator\", function() { return isGenerator; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isPromise\", function() { return isPromise; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isFunction\", function() { return isFunction; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"mapAsyncIterator\", function() { return mapAsyncIterator; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"noExceptGeneratorWrap\", function() { return noExceptGeneratorWrap; });\n/**\n * Retrieves the property of an object, or item in array at index, based\n * on the supplied path.\n * @example\n *   const obj = { a: { b: { c: [1, 2, 3] } } }\n *   const two = getViaPath(obj, 'a', 'b', 'c', 1); // two == 2\n * @param {*} obj - An object\n * @param {*} pathItems\n * @return {*}\n */\nfunction getViaPath(obj, ...pathItems) {\n  if (obj == null || pathItems.length === 0) return null;\n  let cur = obj[pathItems[0]];\n  for (let i = 1; i < pathItems.length; i++) {\n    cur = cur[pathItems[i]];\n    if (cur == null) return null;\n  }\n  return cur;\n}\n\n/**\n * Initiates an pywb/webrecorder auto-fetch of content\n */\nfunction autoFetchFromDoc() {\n  if (window.$WBAutoFetchWorker$) {\n    window.$WBAutoFetchWorker$.extractFromLocalDoc();\n  }\n}\n\n/**\n * Sends the supplied array of URLs to the backing pywb/webrecorder auto-fetch worker if it exists\n * @param {Array<string>} urls\n */\nfunction sendAutoFetchWorkerURLs(urls) {\n  if (window.$WBAutoFetchWorker$) {\n    window.$WBAutoFetchWorker$.justFetch(urls);\n  }\n}\n\n/**\n * This function is a no op\n */\nfunction noop() {}\n\n/**\n * Automatically binds the non-inherited functions of the supplied\n * class to itself.\n * @param clazz\n */\nfunction autobind(clazz) {\n  const clazzProps = Object.getOwnPropertyNames(clazz.constructor.prototype);\n  let prop;\n  let propValue;\n  for (var i = 0; i < clazzProps.length; ++i) {\n    prop = clazzProps[i];\n    propValue = clazz[prop];\n    if (prop !== 'constructor' && typeof propValue === 'function') {\n      clazz[prop] = propValue.bind(clazz);\n    }\n  }\n}\n\n/**\n * Returns T/F if the supplied object has all of the supplied properties.\n * The existence check is `obj[prop] != null`\n * @param {Object} obj - The object to be tested\n * @param {...string} props - The property names\n * @return {boolean}\n */\nfunction objectHasProps(obj, ...props) {\n  if (obj == null) return false;\n  for (var i = 0; i < props.length; ++i) {\n    if (obj[props[i]] == null) return false;\n  }\n  return true;\n}\n\n/**\n * Returns T/F if an global property (on window) exists and has\n * all properties. The existence check is `obj[prop] != null`\n * @param {string} global - The name of the global\n * @param {...string} props - The property names\n * @return {boolean}\n */\nfunction globalWithPropsExist(global, ...props) {\n  const obj = window[global];\n  if (obj == null) return false;\n  for (var i = 0; i < props.length; ++i) {\n    if (obj[props[i]] == null) return false;\n  }\n  return true;\n}\n\n/**\n * Returns a new object with the properties of the object\n * on the new object\n * @param {?Object} object - The object to extract properties from\n * @param {...string} props - The property names to be extracted\n * @return {?Object} - The new object if the original object was not null\n */\nfunction extractProps(object, ...props) {\n  if (object == null) return null;\n  const extracted = {};\n  for (var i = 0; i < props.length; ++i) {\n    extracted[props[i]] = object[props[i]];\n  }\n  return extracted;\n}\n\n/**\n * Composes multiple functions from right to left into a single function.\n * The rightmost function can take multiple arguments with the remaining\n * functions taking only a single argument, the return value of the previous\n * invocation.\n * @param {...function(...args: *): *} funcs - The functions to compose.\n * @return {function(...args: *): *} - A function obtained by composing the functions\n * from right to left. For example, compose(f, g, h) is identical to doing\n * (...args) => f(g(h(...args))).\n */\nfunction compose(...funcs) {\n  if (funcs.length === 0) {\n    return arg => arg;\n  }\n\n  if (funcs.length === 1) {\n    return funcs[0];\n  }\n\n  return funcs.reduce((a, b) => (...args) => a(b(...args)));\n}\n\n/**\n * Composes multiple async or Promise returning functions from right to\n * left into a single function. The rightmost function can take multiple arguments\n * with the remaining functions taking only a single argument, the return value\n * of the previous invocation.\n * @param {...function(...args: *): Promise<*>} funcs - The functions to compose.\n * @return {function(...args: *): Promise<*>} - A function obtained by composing the functions\n * from right to left. For example, composeAsync(f, g, h) is identical to doing\n * (...args) => h(...args).then(result => g(result).then(result => f(result)).\n */\nfunction composeAsync(...funcs) {\n  if (funcs.length === 0) {\n    return async function() {\n      return arguments[0];\n    };\n  }\n\n  if (funcs.length === 1) {\n    return funcs[0];\n  }\n\n  return funcs.reduce((a, b) => (...args) => b(...args).then(a));\n}\n\n/**\n * Composes multiple Iterator returning functions from right to\n * left into a single function. The rightmost function can take multiple arguments\n * with the remaining functions taking only a single argument, the iterator returned\n * by the previous invocation. The composition of iterators can thought of\n * as chaining multiple iterators together, that is to say feeding the yielded results\n * of each iterator from right to left. This can be useful for chaining cascading\n * ordered actions.\n * @example\n *   function* iter1(iter) {\n *     console.log('start iter1');\n *     for (const value of iter) {\n *       yield `iter1 got \"${value}\" from the previous iter`;\n *     }\n *     yield 'end iter1';\n *   }\n *\n *   function* iter2(iter) {\n *     console.log('start iter2');\n *     for (const value of iter) {\n *       yield `iter2 got \"${value}\" from the previous iter`;\n *     }\n *     yield 'end iter2';\n *   }\n *\n *   function* iter3(...args) {\n *     console.log('start iter3');\n *     let i = args.length;\n *     while (i--) {\n *       yield args[i];\n *     }\n *    yield 'end iter3';\n *  }\n *\n *  const finalIter = composeIterators(iter1, iter2, iter3)(1, 2, 3, 4, 5);\n *\n *  for (const it of finalIter) {\n *    console.log(it);\n * }\n *\n *  // The output is as follows\n *  // logs \"start iter\" from iter1\n *  // logs \"start iter2\" from iter2\n *  // logs \"start iter3\" from iter3\n *  // logs 'iter1 got \"iter2 got \"5\" from the previous iter\" from the previous iter' from the for of finalIter\n *  // logs 'iter1 got \"iter2 got \"4\" from the previous iter\" from the previous iter' from the for of finalIter\n *  // logs 'iter1 got \"iter2 got \"3\" from the previous iter\" from the previous iter' from the for of finalIter\n *  // logs 'iter1 got \"iter2 got \"2\" from the previous iter\" from the previous iter' from the for of finalIter\n *  // logs 'iter1 got \"iter2 got \"1\" from the previous iter\" from the previous iter' from the for of finalIter\n *  // logs 'iter1 got \"iter2 got \"end iter3\" from the previous iter\" from the previous iter' from the for of finalIter\n *  // logs 'iter1 got \"end iter2\" from the previous iter' from the for of finalIter\n *  // logs 'end iter1' from the for of finalIter\n *\n * @param {...function(...args:*): Iterator<*>} funcs - The Iterator returning functions to be composed (chained)\n * @return {function(...args:*): Iterator<*>} - A function obtained by composing the functions\n * from right to left. See the documentations example for details\n */\nfunction composeIterators(...funcs) {\n  if (funcs.length === 0) {\n    return function*() {\n      yield arguments[0];\n    };\n  }\n\n  if (funcs.length === 1) {\n    return funcs[0];\n  }\n\n  return funcs.reduce(\n    (a, b) =>\n      function*(...args) {\n        yield* a(b(...args));\n      }\n  );\n}\n\n/**\n * Composes multiple AsyncIterator returning functions from right to\n * left into a single function. The rightmost function can take multiple arguments\n * with the remaining functions taking only a single argument, the iterator returned\n * by the previous invocation. The composition of AsyncIterator can thought of\n * as chaining multiple AsyncIterator together, that is to say feeding the yielded results\n * of each iterator from right to left. This can be useful for chaining cascading asynchronous\n * ordered actions.\n * @example\n *   async function* iter1(iter) {\n *     console.log('start iter1');\n *     for await (const value of iter) {\n *       yield `iter1 got \"${value}\" from the previous iter`;\n *     }\n *     yield 'end iter1';\n *   }\n *\n *   async function* iter2(iter) {\n *     console.log('start iter2');\n *     for await (const value of iter) {\n *       yield `iter2 got \"${value}\" from the previous iter`;\n *     }\n *     yield 'end iter2';\n *   }\n *\n *   async function* iter3(...args) {\n *     console.log('start iter3');\n *     let i = args.length;\n *     while (i--) {\n *       yield args[i];\n *     }\n *    yield 'end iter3';\n *  }\n *\n *  const finalAIter = composeAsyncIterators(iter1, iter2, iter3)(1, 2, 3, 4, 5);\n *\n *  (async () => {\n *    for await (const it of finalAIter) {\n *      console.log(it);\n *    }\n *  })();\n *\n *  // The output is as follows\n *  // logs \"start iter\" from iter1\n *  // logs \"start iter2\" from iter2\n *  // logs \"start iter3\" from iter3\n *  // logs 'iter1 got \"iter2 got \"5\" from the previous iter\" from the previous iter' from the for await of finalAiter\n *  // logs 'iter1 got \"iter2 got \"4\" from the previous iter\" from the previous iter' from the for await of finalAiter\n *  // logs 'iter1 got \"iter2 got \"3\" from the previous iter\" from the previous iter' from the for await of finalAiter\n *  // logs 'iter1 got \"iter2 got \"2\" from the previous iter\" from the previous iter' from the for await of finalAiter\n *  // logs 'iter1 got \"iter2 got \"1\" from the previous iter\" from the previous iter' from the for await of finalAiter\n *  // logs 'iter1 got \"iter2 got \"end iter3\" from the previous iter\" from the previous iter' from the for await of finalAiter\n *  // logs 'iter1 got \"end iter2\" from the previous iter' from the for await of finalAiter\n *  // logs 'end iter1' from the for await of finalAiter\n *\n * @param {...function(...args:*): AsyncIterator<*>} funcs - The AsyncIterator returning functions to be composed (chained)\n * @return {function(...args:*): AsyncIterator<*>} - A function obtained by composing the functions\n * from right to left. See the documentations example for details\n */\nfunction composeAsyncIterators(...funcs) {\n  if (funcs.length === 0) {\n    return async function*() {\n      yield arguments[0];\n    };\n  }\n\n  if (funcs.length === 1) {\n    return funcs[0];\n  }\n\n  return funcs.reduce(\n    (a, b) =>\n      async function*(...args) {\n        yield* a(b(...args));\n      }\n  );\n}\n\n/**\n * Creates a Promise that is resolvable externally returning an object\n * that exposes the promise itself and the resolve and reject functions\n * passed as arguments to the executor function\n * @return {{resolve: function, reject: function, promise: Promise<*>}}\n */\nfunction promiseResolveReject() {\n  const promResolveReject = { promise: null, resolve: null, reject: null };\n  promResolveReject.promise = new Promise((resolve, reject) => {\n    promResolveReject.resolve = resolve;\n    promResolveReject.reject = reject;\n  });\n  return promResolveReject;\n}\n\n/**\n * Returns T/F indicating if the supplied object is an instance of some class.\n * If the object to be tested is falsy false is returned otherwise the results\n * of the instanceof check.\n * @param {Object} obj - The object to be tested\n * @param {Object} shouldBeInstanceOfThis - The class the obj should be an instance of\n * @return {boolean}\n */\nfunction objectInstanceOf(obj, shouldBeInstanceOfThis) {\n  if (!obj) return false;\n  return obj instanceof shouldBeInstanceOfThis;\n}\n\n/**\n * Creates and returns a partially applied function with args being applied left to right\n * @param {function} fn - The function to partially apply arguments to\n * @param {*} [a1] - 1st arg\n * @param {*} [a2] - 2nd arg\n * @param {*} [a3] - 3rd arg\n * @param {*} [a4] - 4th arg\n * @param {*} [a5] - 5th arg\n * @param {...*} [aN] - The remaining arguments to be partially applied\n * @return {function(...args: *): *} - Returns the new partially applied function\n */\nfunction partial(fn, a1, a2, a3, a4, a5, ...aN) {\n  if (a1 == null) return fn;\n  if (aN.length) {\n    return (...args) => fn(a1, a2, a3, a4, a5, ...aN, ...args);\n  }\n  if (a5 != null) return (...args) => fn(a1, a2, a3, a4, a5, ...args);\n  if (a4 != null) return (...args) => fn(a1, a2, a3, a4, ...args);\n  if (a3 != null) return (...args) => fn(a1, a2, a3, ...args);\n  if (a2 != null) return (...args) => fn(a1, a2, ...args);\n  return (...args) => fn(a1, ...args);\n}\n\n/**\n * Creates and returns a partially applied function with args being applied right to left\n * @param {function} fn - The function to partially apply arguments to\n * @param {*} [a1] - 1st nth arg\n * @param {*} [a2] - 2nd nth arg\n * @param {*} [a3] - 3rd nth arg\n * @param {*} [a4] - 4th nth arg\n * @param {*} [a5] - 5th nth arg\n * @param {...*} [aN] - The remaining arguments to be partially applied\n * @return {function(...args: *): *} - Returns the new partially applied function\n */\nfunction partialRight(fn, a1, a2, a3, a4, a5, ...aN) {\n  if (a1 == null) return fn;\n  if (aN.length) {\n    return (...args) => fn(...args, a1, a2, a3, a4, a5, ...aN);\n  }\n  if (a5 != null) return (...args) => fn(...args, a1, a2, a3, a4, a5);\n  if (a4 != null) return (...args) => fn(...args, a1, a2, a3, a4);\n  if (a3 != null) return (...args) => fn(...args, a1, a2, a3);\n  if (a2 != null) return (...args) => fn(...args, a1, a2);\n  return (...args) => fn(...args, a1);\n}\n/** @ignore */\nlet __BytesToHex__;\n\n/**\n * Creates and returns a valid uuid v4\n * @return {string}\n */\nfunction uuidv4() {\n  if (__BytesToHex__ == null) {\n    __BytesToHex__ = new Array(256);\n    for (let i = 0; i < 256; ++i) {\n      __BytesToHex__[i] = (i + 0x100).toString(16).substr(1);\n    }\n  }\n  const randomBytes = crypto.getRandomValues(new Uint8Array(16));\n  randomBytes[6] = (randomBytes[6] & 0x0f) | 0x40;\n  randomBytes[8] = (randomBytes[8] & 0x3f) | 0x80;\n  return [\n    __BytesToHex__[randomBytes[0]],\n    __BytesToHex__[randomBytes[1]],\n    __BytesToHex__[randomBytes[2]],\n    __BytesToHex__[randomBytes[3]],\n    '-',\n    __BytesToHex__[randomBytes[4]],\n    __BytesToHex__[randomBytes[5]],\n    '-',\n    __BytesToHex__[randomBytes[6]],\n    __BytesToHex__[randomBytes[7]],\n    '-',\n    __BytesToHex__[randomBytes[8]],\n    __BytesToHex__[randomBytes[9]],\n    '-',\n    __BytesToHex__[randomBytes[10]],\n    __BytesToHex__[randomBytes[11]],\n    __BytesToHex__[randomBytes[12]],\n    __BytesToHex__[randomBytes[13]],\n    __BytesToHex__[randomBytes[14]],\n    __BytesToHex__[randomBytes[15]],\n  ].join('');\n}\n\n/**\n * Returns T/F indicating if the supplied object is a generator or async generator\n * @param {*} obj\n * @return {boolean}\n */\nfunction isGenerator(obj) {\n  if (!obj) return false;\n  const tag = obj[Symbol.toStringTag];\n  if (tag === 'AsyncGenerator' || tag === 'Generator') return true;\n  if (isFunction(obj.next) && isFunction(obj.throw) && isFunction(obj.return)) {\n    return true;\n  }\n  if (!obj.constructor) return false;\n  const ctag = obj.constructor[Symbol.toStringTag];\n  return ctag === 'GeneratorFunction' || ctag === 'AsyncGeneratorFunction';\n}\n\n/**\n * Returns T/F indicating if the supplied object is a Promise or Promise like\n * @param {*} obj\n * @return {boolean}\n */\nfunction isPromise(obj) {\n  if (!obj) return false;\n  return (\n    obj instanceof Promise ||\n    (typeof obj === 'object' &&\n      isFunction(obj.then) &&\n      isFunction(obj.catch)) ||\n    obj[Symbol.toStringTag] === 'Promise'\n  );\n}\n\n/**\n * Returns T/F indicating if the supplied arument is a function\n * @param {*} obj\n * @return {boolean}\n */\nfunction isFunction(obj) {\n  return typeof obj === 'function';\n}\n\n/**\n * Like map for arrays but over async-iterators\n * @param {AsyncIterableIterator<*>} iterator - The iterator\n * to have a mapping function applied over\n * @param {function(arg: *): *} mapper - The mapping function\n * @return {AsyncIterableIterator<*>}\n */\nasync function* mapAsyncIterator(iterator, mapper) {\n  for await (const item of iterator) {\n    const nextValue = mapper(item);\n    if (isGenerator(nextValue)) {\n      for await (const next of noExceptGeneratorWrap(nextValue)) {\n        yield next;\n      }\n    } else yield nextValue;\n  }\n}\n\n/**\n * Wraps the supplied generator in a try catch and re-yields its values generator.\n * If the wrapped generator throws an exception the wrapping generator ends.\n * @param {AsyncIterableIterator<*>|IterableIterator<*>} generator - The generator to be wrapped\n * @param {boolean} [returnLast] - Should the last value of the supplied generator be returned\n * @return {AsyncIterableIterator<*>}\n */\nasync function* noExceptGeneratorWrap(generator, returnLast) {\n  try {\n    let next;\n    let nv;\n    while (true) {\n      next = generator.next();\n      if (isPromise(next)) nv = await next;\n      else nv = next;\n      if (nv.done) {\n        if (nv.value) {\n          if (returnLast) return nv.value;\n          else yield nv.value;\n        }\n        break;\n      } else {\n        yield nv.value;\n      }\n    }\n  } catch (e) {}\n}\n\n\n//# sourceURL=webpack://lib/./lib/general.js?");

/***/ }),

/***/ "./lib/index.js":
/*!**********************!*\
  !*** ./lib/index.js ***!
  \**********************/
/*! exports provided: BehaviorRunner, initRunnableBehavior, createState, stateWithMsgNoWait, stateWithMsgWait, stateWithMsgWaitFromAwaitable, stateWithMsgWaitFromFn, browserHistoryGoBack, browserHistoryNav, browserLocation, locationContains, locationEquals, waitForHistoryManipToChangeLocation, click, clickAndWaitFor, clickAndWaitForHistoryChange, clickInContext, clickInContextWithDelay, clickSelectedElementsNextElementSibling, clickSelectedElementsPreviousElementSibling, clickWithDelay, scrollAllIntoViewAndClick, scrollAllIntoViewAndClickWithDelay, scrollIntoViewAndClick, scrollIntoViewAndClickWithDelay, selectAllAndClick, selectAllAndClickWithDelay, selectClickAndWaitFor, selectElemAndClick, selectElemAndClickWithDelay, selectElemFromAndClick, selectElemFromAndClickWithDelay, selectFromAndClickNTimes, selectFromAndClickNTimesWithDelay, selectFromAndClickUntilNullWithDelay, selectScrollIntoViewAndClickSelectedWhileConnected, selectScrollIntoViewAndClickWithDelayWhileSelectedConnected, delay, DelayAmount1Second, DelayAmount2Seconds, DelayAmount3Seconds, DelayAmount4Seconds, DelayAmount5Seconds, domCompletePromise, resolveWhenBehaviorUnPaused, secondsToDelayAmount, setIntervalP, setTimeoutP, waitForAdditionalElemChildren, waitForAdditionalElemChildrenMO, waitForAndSelectElement, waitForElementToBecomeInvisible, waitForElementToBecomeVisible, waitForPredicate, waitForSelector, waitUntilElementIsRemovedFromDom, addBehaviorStyle, addClass, anySelectorExists, attr, attrEq, canAccessIf, chainFistChildElemOf, chainLastChildElemOf, chainNthChildElementOf, chainNthChildNodeOf, chainQs, childElementIterator, childElementIteratorOfSelected, childNodeIterator, docBaseURIEndsWith, docBaseURIEquals, documentScrollPosition, elemDataValue, elemDataValueEqs, elementsNameEquals, elementTextContains, elementTextEndsWith, elementTextEqs, elementTextStartsWith, elementTextStartsWithAndEndsWith, elemHasChildren, elemHasSibling, elemHasZeroBoundingRect, elemInnerText, elemInnerTextEqs, elemInnerTextEqsInsensitive, elemInnerTextEqsOneOf, elemInnerTextMatchesRegex, elemMatchesSelector, elemOffsetTopZero, elemTextContent, filteredQs, findDirectChildElement, findTag, firstChildElementOf, firstChildElementOfSelector, firstChildElemOfParent, getElementClientPageCenter, getElementClientPagePosition, getElementPositionWidthHeight, getElemSibling, getElemSiblingAndRemoveElem, getElemsParentsSibling, getNthParentElement, hasAnyClass, hasClass, hasClasses, id, idExists, innerTextOfSelected, isClasslessElem, isElemVisible, lastChildElementOf, lastChildElementOfSelector, loadPageViaIframe, markElemAsVisited, maybePolyfillXPG, maybeRemoveElem, maybeRemoveElemById, nodesNameEquals, nthChildElementOf, nthChildNodeOf, nthPreviousSibling, numElemChildren, qs, qsa, qsaOneOf, qsOneOf, removeClass, repeatedQSAIterator, repeatedXpathQueryIterator, repeatedXpathQueryIteratorAsync, selectedNextElementSibling, selectedPreviousElementSibling, selectorExists, splitElemInnerText, splitElemTextContents, waitForEventTargetToFireEvent, xpathNumberQuery, xpathOneOf, xpathSnapShot, xpathSnapShotArray, createMouseEvent, fireEventOn, fireMouseEventsOnElement, autobind, autoFetchFromDoc, compose, composeAsync, composeAsyncIterators, composeIterators, extractProps, getViaPath, globalWithPropsExist, isFunction, isGenerator, isPromise, mapAsyncIterator, noExceptGeneratorWrap, noop, objectHasProps, objectInstanceOf, partial, partialRight, promiseResolveReject, sendAutoFetchWorkerURLs, uuidv4, findAllMediaElementsAndPlay, noExceptPlayMediaElement, selectAndPlay, selectIdAndPlay, setMediaElemPlaybackRate, uaThinksMediaElementCanPlayAllTheWay, MutationStream, addOutlink, addOutLinks, collectOutlinksFrom, collectOutlinksFromDoc, buildCustomPostStepFn, doneOrWait, findChildWithKey, findReduxStore, getInternalRootOnElem, getReactRootContainer, getReactRootHostElem, makeReactInstanceFromDOMElemFun, reactInstanceFromDOMElem, reactInstancesFromElements, canScrollDownMore, canScrollUpMore, createScrollAmount, createScroller, scrollDownByElemHeight, scrollDownByElemHeightWithDelay, scrollIntoView, scrollIntoViewAndWaitFor, scrollIntoViewWithDelay, scrollToElemOffset, scrollToElemOffsetWithDelay, scrollWindowBy, scrollWindowByWithDelay, scrollWindowDownBy, scrollWindowDownByWithDelay, smoothScrollWindowBy, smoothScrollWindowDownBy, camelCase, camelCaseToDashDelim, camelCaseToUnderscoreDelim, capitalize, collapseWhitespace, isAlpha, isAlphaNumeric, isEmptyString, isLower, isNumeric, isUpper, stringBetween, substringFromIndexOf, substringFromLastIndexOf, titleCase, toBoolean, toFloat, toInt, DisconnectingWalk, traverseChildrenOf, traverseChildrenOfCustom, traverseChildrenOfLoaderParent, traverseChildrenOfLoaderParentGenFn, traverseChildrenOfLoaderParentRemovingPrevious, traverseChildrenOfRemovingPrevious, walkChildrenOfCustom, WalkEndedReasons */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _behaviorRunner__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./behaviorRunner */ \"./lib/behaviorRunner.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"BehaviorRunner\", function() { return _behaviorRunner__WEBPACK_IMPORTED_MODULE_0__[\"BehaviorRunner\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"initRunnableBehavior\", function() { return _behaviorRunner__WEBPACK_IMPORTED_MODULE_0__[\"initRunnableBehavior\"]; });\n\n/* harmony import */ var _behaviorState__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./behaviorState */ \"./lib/behaviorState.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"createState\", function() { return _behaviorState__WEBPACK_IMPORTED_MODULE_1__[\"createState\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"stateWithMsgNoWait\", function() { return _behaviorState__WEBPACK_IMPORTED_MODULE_1__[\"stateWithMsgNoWait\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"stateWithMsgWait\", function() { return _behaviorState__WEBPACK_IMPORTED_MODULE_1__[\"stateWithMsgWait\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"stateWithMsgWaitFromAwaitable\", function() { return _behaviorState__WEBPACK_IMPORTED_MODULE_1__[\"stateWithMsgWaitFromAwaitable\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"stateWithMsgWaitFromFn\", function() { return _behaviorState__WEBPACK_IMPORTED_MODULE_1__[\"stateWithMsgWaitFromFn\"]; });\n\n/* harmony import */ var _browser__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./browser */ \"./lib/browser.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"browserHistoryGoBack\", function() { return _browser__WEBPACK_IMPORTED_MODULE_2__[\"browserHistoryGoBack\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"browserHistoryNav\", function() { return _browser__WEBPACK_IMPORTED_MODULE_2__[\"browserHistoryNav\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"browserLocation\", function() { return _browser__WEBPACK_IMPORTED_MODULE_2__[\"browserLocation\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"locationContains\", function() { return _browser__WEBPACK_IMPORTED_MODULE_2__[\"locationContains\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"locationEquals\", function() { return _browser__WEBPACK_IMPORTED_MODULE_2__[\"locationEquals\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForHistoryManipToChangeLocation\", function() { return _browser__WEBPACK_IMPORTED_MODULE_2__[\"waitForHistoryManipToChangeLocation\"]; });\n\n/* harmony import */ var _clicks__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./clicks */ \"./lib/clicks.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"click\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"click\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"clickAndWaitFor\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"clickAndWaitFor\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"clickAndWaitForHistoryChange\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"clickAndWaitForHistoryChange\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"clickInContext\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"clickInContext\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"clickInContextWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"clickInContextWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"clickSelectedElementsNextElementSibling\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"clickSelectedElementsNextElementSibling\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"clickSelectedElementsPreviousElementSibling\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"clickSelectedElementsPreviousElementSibling\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"clickWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"clickWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollAllIntoViewAndClick\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"scrollAllIntoViewAndClick\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollAllIntoViewAndClickWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"scrollAllIntoViewAndClickWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoViewAndClick\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"scrollIntoViewAndClick\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoViewAndClickWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"scrollIntoViewAndClickWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectAllAndClick\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectAllAndClick\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectAllAndClickWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectAllAndClickWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectClickAndWaitFor\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectClickAndWaitFor\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectElemAndClick\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectElemAndClick\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectElemAndClickWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectElemAndClickWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectElemFromAndClick\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectElemFromAndClick\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectElemFromAndClickWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectElemFromAndClickWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectFromAndClickNTimes\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectFromAndClickNTimes\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectFromAndClickNTimesWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectFromAndClickNTimesWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectFromAndClickUntilNullWithDelay\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectFromAndClickUntilNullWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectScrollIntoViewAndClickSelectedWhileConnected\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectScrollIntoViewAndClickSelectedWhileConnected\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectScrollIntoViewAndClickWithDelayWhileSelectedConnected\", function() { return _clicks__WEBPACK_IMPORTED_MODULE_3__[\"selectScrollIntoViewAndClickWithDelayWhileSelectedConnected\"]; });\n\n/* harmony import */ var _delays__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./delays */ \"./lib/delays.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"delay\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"delay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount1Second\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"DelayAmount1Second\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount2Seconds\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"DelayAmount2Seconds\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount3Seconds\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"DelayAmount3Seconds\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount4Seconds\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"DelayAmount4Seconds\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"DelayAmount5Seconds\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"DelayAmount5Seconds\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"domCompletePromise\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"domCompletePromise\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"resolveWhenBehaviorUnPaused\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"resolveWhenBehaviorUnPaused\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"secondsToDelayAmount\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"secondsToDelayAmount\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"setIntervalP\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"setIntervalP\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"setTimeoutP\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"setTimeoutP\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForAdditionalElemChildren\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"waitForAdditionalElemChildren\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForAdditionalElemChildrenMO\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"waitForAdditionalElemChildrenMO\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForAndSelectElement\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"waitForAndSelectElement\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForElementToBecomeInvisible\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"waitForElementToBecomeInvisible\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForElementToBecomeVisible\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"waitForElementToBecomeVisible\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForPredicate\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"waitForPredicate\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForSelector\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"waitForSelector\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitUntilElementIsRemovedFromDom\", function() { return _delays__WEBPACK_IMPORTED_MODULE_4__[\"waitUntilElementIsRemovedFromDom\"]; });\n\n/* harmony import */ var _dom__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./dom */ \"./lib/dom.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"addBehaviorStyle\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"addBehaviorStyle\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"addClass\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"addClass\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"anySelectorExists\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"anySelectorExists\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"attr\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"attr\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"attrEq\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"attrEq\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"canAccessIf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"canAccessIf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"chainFistChildElemOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"chainFistChildElemOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"chainLastChildElemOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"chainLastChildElemOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"chainNthChildElementOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"chainNthChildElementOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"chainNthChildNodeOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"chainNthChildNodeOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"chainQs\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"chainQs\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"childElementIterator\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"childElementIterator\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"childElementIteratorOfSelected\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"childElementIteratorOfSelected\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"childNodeIterator\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"childNodeIterator\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"docBaseURIEndsWith\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"docBaseURIEndsWith\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"docBaseURIEquals\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"docBaseURIEquals\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"documentScrollPosition\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"documentScrollPosition\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemDataValue\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemDataValue\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemDataValueEqs\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemDataValueEqs\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elementsNameEquals\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elementsNameEquals\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elementTextContains\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elementTextContains\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elementTextEndsWith\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elementTextEndsWith\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elementTextEqs\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elementTextEqs\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elementTextStartsWith\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elementTextStartsWith\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elementTextStartsWithAndEndsWith\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elementTextStartsWithAndEndsWith\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemHasChildren\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemHasChildren\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemHasSibling\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemHasSibling\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemHasZeroBoundingRect\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemHasZeroBoundingRect\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemInnerText\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemInnerText\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemInnerTextEqs\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemInnerTextEqs\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemInnerTextEqsInsensitive\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemInnerTextEqsInsensitive\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemInnerTextEqsOneOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemInnerTextEqsOneOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemInnerTextMatchesRegex\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemInnerTextMatchesRegex\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemMatchesSelector\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemMatchesSelector\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemOffsetTopZero\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemOffsetTopZero\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"elemTextContent\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"elemTextContent\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"filteredQs\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"filteredQs\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"findDirectChildElement\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"findDirectChildElement\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"findTag\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"findTag\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"firstChildElementOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"firstChildElementOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"firstChildElementOfSelector\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"firstChildElementOfSelector\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"firstChildElemOfParent\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"firstChildElemOfParent\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getElementClientPageCenter\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"getElementClientPageCenter\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getElementClientPagePosition\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"getElementClientPagePosition\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getElementPositionWidthHeight\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"getElementPositionWidthHeight\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getElemSibling\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"getElemSibling\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getElemSiblingAndRemoveElem\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"getElemSiblingAndRemoveElem\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getElemsParentsSibling\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"getElemsParentsSibling\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getNthParentElement\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"getNthParentElement\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"hasAnyClass\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"hasAnyClass\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"hasClass\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"hasClass\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"hasClasses\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"hasClasses\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"id\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"id\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"idExists\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"idExists\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"innerTextOfSelected\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"innerTextOfSelected\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isClasslessElem\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"isClasslessElem\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isElemVisible\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"isElemVisible\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"lastChildElementOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"lastChildElementOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"lastChildElementOfSelector\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"lastChildElementOfSelector\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"loadPageViaIframe\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"loadPageViaIframe\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"markElemAsVisited\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"markElemAsVisited\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"maybePolyfillXPG\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"maybePolyfillXPG\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"maybeRemoveElem\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"maybeRemoveElem\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"maybeRemoveElemById\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"maybeRemoveElemById\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"nodesNameEquals\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"nodesNameEquals\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"nthChildElementOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"nthChildElementOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"nthChildNodeOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"nthChildNodeOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"nthPreviousSibling\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"nthPreviousSibling\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"numElemChildren\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"numElemChildren\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"qs\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"qs\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"qsa\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"qsa\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"qsaOneOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"qsaOneOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"qsOneOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"qsOneOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"removeClass\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"removeClass\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"repeatedQSAIterator\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"repeatedQSAIterator\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"repeatedXpathQueryIterator\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"repeatedXpathQueryIterator\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"repeatedXpathQueryIteratorAsync\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"repeatedXpathQueryIteratorAsync\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectedNextElementSibling\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"selectedNextElementSibling\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectedPreviousElementSibling\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"selectedPreviousElementSibling\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectorExists\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"selectorExists\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"splitElemInnerText\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"splitElemInnerText\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"splitElemTextContents\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"splitElemTextContents\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"waitForEventTargetToFireEvent\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"waitForEventTargetToFireEvent\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"xpathNumberQuery\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"xpathNumberQuery\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"xpathOneOf\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"xpathOneOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"xpathSnapShot\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"xpathSnapShot\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"xpathSnapShotArray\", function() { return _dom__WEBPACK_IMPORTED_MODULE_5__[\"xpathSnapShotArray\"]; });\n\n/* harmony import */ var _events__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./events */ \"./lib/events.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"createMouseEvent\", function() { return _events__WEBPACK_IMPORTED_MODULE_6__[\"createMouseEvent\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"fireEventOn\", function() { return _events__WEBPACK_IMPORTED_MODULE_6__[\"fireEventOn\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"fireMouseEventsOnElement\", function() { return _events__WEBPACK_IMPORTED_MODULE_6__[\"fireMouseEventsOnElement\"]; });\n\n/* harmony import */ var _general__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./general */ \"./lib/general.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"autobind\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"autobind\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"autoFetchFromDoc\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"autoFetchFromDoc\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"compose\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"compose\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"composeAsync\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"composeAsync\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"composeAsyncIterators\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"composeAsyncIterators\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"composeIterators\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"composeIterators\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"extractProps\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"extractProps\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getViaPath\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"getViaPath\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"globalWithPropsExist\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"globalWithPropsExist\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isFunction\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"isFunction\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isGenerator\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"isGenerator\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isPromise\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"isPromise\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"mapAsyncIterator\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"mapAsyncIterator\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"noExceptGeneratorWrap\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"noExceptGeneratorWrap\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"noop\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"noop\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"objectHasProps\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"objectHasProps\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"objectInstanceOf\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"objectInstanceOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"partial\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"partial\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"partialRight\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"partialRight\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"promiseResolveReject\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"promiseResolveReject\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"sendAutoFetchWorkerURLs\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"sendAutoFetchWorkerURLs\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"uuidv4\", function() { return _general__WEBPACK_IMPORTED_MODULE_7__[\"uuidv4\"]; });\n\n/* harmony import */ var _media__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./media */ \"./lib/media.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"findAllMediaElementsAndPlay\", function() { return _media__WEBPACK_IMPORTED_MODULE_8__[\"findAllMediaElementsAndPlay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"noExceptPlayMediaElement\", function() { return _media__WEBPACK_IMPORTED_MODULE_8__[\"noExceptPlayMediaElement\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectAndPlay\", function() { return _media__WEBPACK_IMPORTED_MODULE_8__[\"selectAndPlay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"selectIdAndPlay\", function() { return _media__WEBPACK_IMPORTED_MODULE_8__[\"selectIdAndPlay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"setMediaElemPlaybackRate\", function() { return _media__WEBPACK_IMPORTED_MODULE_8__[\"setMediaElemPlaybackRate\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"uaThinksMediaElementCanPlayAllTheWay\", function() { return _media__WEBPACK_IMPORTED_MODULE_8__[\"uaThinksMediaElementCanPlayAllTheWay\"]; });\n\n/* harmony import */ var _mutations__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./mutations */ \"./lib/mutations.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"MutationStream\", function() { return _mutations__WEBPACK_IMPORTED_MODULE_9__[\"MutationStream\"]; });\n\n/* harmony import */ var _outlinkCollector__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./outlinkCollector */ \"./lib/outlinkCollector.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"addOutlink\", function() { return _outlinkCollector__WEBPACK_IMPORTED_MODULE_10__[\"addOutlink\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"addOutLinks\", function() { return _outlinkCollector__WEBPACK_IMPORTED_MODULE_10__[\"addOutLinks\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"collectOutlinksFrom\", function() { return _outlinkCollector__WEBPACK_IMPORTED_MODULE_10__[\"collectOutlinksFrom\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"collectOutlinksFromDoc\", function() { return _outlinkCollector__WEBPACK_IMPORTED_MODULE_10__[\"collectOutlinksFromDoc\"]; });\n\n/* harmony import */ var _postStepFNs__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./postStepFNs */ \"./lib/postStepFNs.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"buildCustomPostStepFn\", function() { return _postStepFNs__WEBPACK_IMPORTED_MODULE_11__[\"buildCustomPostStepFn\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"doneOrWait\", function() { return _postStepFNs__WEBPACK_IMPORTED_MODULE_11__[\"doneOrWait\"]; });\n\n/* harmony import */ var _reactUtils__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./reactUtils */ \"./lib/reactUtils.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"findChildWithKey\", function() { return _reactUtils__WEBPACK_IMPORTED_MODULE_12__[\"findChildWithKey\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"findReduxStore\", function() { return _reactUtils__WEBPACK_IMPORTED_MODULE_12__[\"findReduxStore\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getInternalRootOnElem\", function() { return _reactUtils__WEBPACK_IMPORTED_MODULE_12__[\"getInternalRootOnElem\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getReactRootContainer\", function() { return _reactUtils__WEBPACK_IMPORTED_MODULE_12__[\"getReactRootContainer\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"getReactRootHostElem\", function() { return _reactUtils__WEBPACK_IMPORTED_MODULE_12__[\"getReactRootHostElem\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"makeReactInstanceFromDOMElemFun\", function() { return _reactUtils__WEBPACK_IMPORTED_MODULE_12__[\"makeReactInstanceFromDOMElemFun\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"reactInstanceFromDOMElem\", function() { return _reactUtils__WEBPACK_IMPORTED_MODULE_12__[\"reactInstanceFromDOMElem\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"reactInstancesFromElements\", function() { return _reactUtils__WEBPACK_IMPORTED_MODULE_12__[\"reactInstancesFromElements\"]; });\n\n/* harmony import */ var _scrolls__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./scrolls */ \"./lib/scrolls.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"canScrollDownMore\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"canScrollDownMore\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"canScrollUpMore\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"canScrollUpMore\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"createScrollAmount\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"createScrollAmount\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"createScroller\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"createScroller\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollDownByElemHeight\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollDownByElemHeight\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollDownByElemHeightWithDelay\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollDownByElemHeightWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoView\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollIntoView\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoViewAndWaitFor\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollIntoViewAndWaitFor\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoViewWithDelay\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollIntoViewWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollToElemOffset\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollToElemOffset\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollToElemOffsetWithDelay\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollToElemOffsetWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollWindowBy\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollWindowBy\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollWindowByWithDelay\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollWindowByWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollWindowDownBy\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollWindowDownBy\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"scrollWindowDownByWithDelay\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"scrollWindowDownByWithDelay\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"smoothScrollWindowBy\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"smoothScrollWindowBy\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"smoothScrollWindowDownBy\", function() { return _scrolls__WEBPACK_IMPORTED_MODULE_13__[\"smoothScrollWindowDownBy\"]; });\n\n/* harmony import */ var _strings__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./strings */ \"./lib/strings.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"camelCase\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"camelCase\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"camelCaseToDashDelim\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"camelCaseToDashDelim\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"camelCaseToUnderscoreDelim\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"camelCaseToUnderscoreDelim\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"capitalize\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"capitalize\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"collapseWhitespace\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"collapseWhitespace\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isAlpha\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"isAlpha\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isAlphaNumeric\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"isAlphaNumeric\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isEmptyString\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"isEmptyString\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isLower\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"isLower\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isNumeric\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"isNumeric\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"isUpper\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"isUpper\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"stringBetween\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"stringBetween\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"substringFromIndexOf\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"substringFromIndexOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"substringFromLastIndexOf\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"substringFromLastIndexOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"titleCase\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"titleCase\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"toBoolean\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"toBoolean\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"toFloat\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"toFloat\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"toInt\", function() { return _strings__WEBPACK_IMPORTED_MODULE_14__[\"toInt\"]; });\n\n/* harmony import */ var _traversals__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./traversals */ \"./lib/traversals.js\");\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"DisconnectingWalk\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"DisconnectingWalk\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOf\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"traverseChildrenOf\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfCustom\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"traverseChildrenOfCustom\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfLoaderParent\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"traverseChildrenOfLoaderParent\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfLoaderParentGenFn\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"traverseChildrenOfLoaderParentGenFn\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfLoaderParentRemovingPrevious\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"traverseChildrenOfLoaderParentRemovingPrevious\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfRemovingPrevious\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"traverseChildrenOfRemovingPrevious\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"walkChildrenOfCustom\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"walkChildrenOfCustom\"]; });\n\n/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, \"WalkEndedReasons\", function() { return _traversals__WEBPACK_IMPORTED_MODULE_15__[\"WalkEndedReasons\"]; });\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n//# sourceURL=webpack://lib/./lib/index.js?");

/***/ }),

/***/ "./lib/media.js":
/*!**********************!*\
  !*** ./lib/media.js ***!
  \**********************/
/*! exports provided: selectAndPlay, selectIdAndPlay, setMediaElemPlaybackRate, noExceptPlayMediaElement, uaThinksMediaElementCanPlayAllTheWay, findAllMediaElementsAndPlay */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectAndPlay\", function() { return selectAndPlay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"selectIdAndPlay\", function() { return selectIdAndPlay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"setMediaElemPlaybackRate\", function() { return setMediaElemPlaybackRate; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"noExceptPlayMediaElement\", function() { return noExceptPlayMediaElement; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"uaThinksMediaElementCanPlayAllTheWay\", function() { return uaThinksMediaElementCanPlayAllTheWay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"findAllMediaElementsAndPlay\", function() { return findAllMediaElementsAndPlay; });\n/* harmony import */ var _dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./dom */ \"./lib/dom.js\");\n/* harmony import */ var _general__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./general */ \"./lib/general.js\");\n\n\n\n/**\n * Calls the play method of the element returned by the querySelector\n * @param {string} selector - The query selector to select a media element\n * @param {Document|Element} [cntx] - Optional element/Document to use\n * to perform the querySelector call from\n * @return {Promise<boolean>} - Returns false if an exception occurred or true if playing was successful\n */\nfunction selectAndPlay(selector, cntx) {\n  const elem = Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"qs\"])(selector, cntx);\n  return noExceptPlayMediaElement(elem);\n}\n\n/**\n * Calls the play method of the element returned by calling document.getElementById\n * @param {string} eid - The id of the media element\n * @param {Document} [cntx] - Optional document object to use\n * @return {Promise<boolean>} - Returns false if an exception occurred or true if playing was successful\n */\nfunction selectIdAndPlay(eid, cntx) {\n  const elem = Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"id\"])(eid, cntx);\n  return noExceptPlayMediaElement(elem);\n}\n\n/**\n * Attempts to set the playback rate of the supplied media element\n * to the supplied rate if it was supplied otherwise defaults to 16x.\n *\n * If the setting the playback rate fails the rate is subtracted by 0.25\n * until a valid rate is found or the rate returns to 1.\n * In the case when a non-suitable playback rate is found the original\n * playback rate is re-set.\n * @param {HTMLMediaElement} mediaElem - The media element who's playback rate\n * is to be changed\n * @param {number} [rate] - Optional playback rate, defaults to 16\n */\nfunction setMediaElemPlaybackRate(mediaElem, rate) {\n  if (!mediaElem) return;\n  const originalPlaybackRate = mediaElem.playbackRate;\n  let good = false;\n  let mediaRate = 16.0;\n  if (rate && typeof rate === 'number') {\n    // if rate is not greater than 1 just use default rate\n    mediaRate = rate > 1 ? rate : mediaRate;\n  }\n  for (; mediaRate > 1.0; mediaRate -= 0.25) {\n    // a exception is thrown if the media element does not support\n    // the new playback rate\n    try {\n      mediaElem.playbackRate = mediaRate;\n      good = true;\n      break;\n    } catch (e) {}\n  }\n  if (!good) {\n    mediaElem.playbackRate =\n      originalPlaybackRate > 0 ? originalPlaybackRate : 1;\n  }\n}\n\n/**\n * Calls the play function of the supplied element catching the exception thrown\n * if any was thrown. Returns true indicating call to play was successful otherwise false\n * @param {HTMLMediaElement} mediaElement - The media element to be played\n * @param {boolean} [playThrough] - Should the can play through event be waited for\n * @return {Promise<boolean>} - Returns false if an exception occurred or true if playing was successful\n */\nasync function noExceptPlayMediaElement(mediaElement, playThrough) {\n  if (mediaElement == null || typeof mediaElement.play !== 'function') {\n    return false;\n  }\n  try {\n    let playProm;\n    if (playThrough) {\n      const plp = uaThinksMediaElementCanPlayAllTheWay(mediaElement);\n      playProm = Promise.all([mediaElement.play(), plp]);\n    } else {\n      playProm = mediaElement.play();\n    }\n    await playProm;\n  } catch (e) {\n    return false;\n  }\n  return true;\n}\n\n/**\n * Returns a promise that resolves once the canplaythrough or error event\n * is fired from the supplied media element\n * @param {HTMLMediaElement} mediaElement\n * @return {Promise<void>}\n */\nfunction uaThinksMediaElementCanPlayAllTheWay(mediaElement) {\n  return new Promise(resolve => {\n    let to;\n    const listener = () => {\n      if (to != null) {\n        clearTimeout(to);\n        to = null;\n      }\n      mediaElement.removeEventListener('canplaythrough', listener);\n      mediaElement.removeEventListener('error', listener);\n      resolve();\n    };\n    mediaElement.addEventListener('canplaythrough', listener);\n    mediaElement.addEventListener('error', listener);\n    to = setTimeout(listener, 60000);\n  });\n}\n\n/**\n * @type {string}\n * @private\n */\nconst __MediaElementSelector__ = 'audio, video';\n/** @ignore  */\nlet __Played__;\n\n/**\n * Calls the play function on all audio and video elements found in the document.\n * If context is supplied, the selection of audio and video elements is done from\n * the supplied context.\n * @param {Document|Element} [cntx] - Optional element to use rather\n * than the current JS context's document object\n * @return {Promise<boolean>}\n */\nasync function findAllMediaElementsAndPlay(cntx) {\n  if (__Played__ == null) __Played__ = Symbol(Object(_general__WEBPACK_IMPORTED_MODULE_1__[\"uuidv4\"])());\n  const mediaElems = Object(_dom__WEBPACK_IMPORTED_MODULE_0__[\"qsa\"])(__MediaElementSelector__, cntx);\n  if (mediaElems.length === 0) return false;\n  const proms = [];\n  let shouldWait = false;\n  for (var i = 0; i < mediaElems.length; i++) {\n    const mediaElem = mediaElems[i];\n    if (!mediaElem[__Played__]) {\n      setMediaElemPlaybackRate(mediaElem, 10.0);\n      proms.push(noExceptPlayMediaElement(mediaElem));\n      Object.defineProperty(mediaElem, __Played__, {\n        value: true,\n        enumerable: false,\n      });\n      shouldWait = true;\n    }\n  }\n  if (proms.length) {\n    await Promise.all(proms);\n  }\n  return shouldWait;\n}\n\n\n//# sourceURL=webpack://lib/./lib/media.js?");

/***/ }),

/***/ "./lib/mutations.js":
/*!**************************!*\
  !*** ./lib/mutations.js ***!
  \**************************/
/*! exports provided: MutationStream */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"MutationStream\", function() { return MutationStream; });\n/**\n * Observe dom mutation using a MutationObserver as a stream (AsyncIterator)\n */\nclass MutationStream {\n  /**\n   * Construct a new MutationStream\n   */\n  constructor() {\n    /**\n     * @type {MutationObserver}\n     */\n    this.mo = new MutationObserver((ml, ob) => {\n      if (this._resolve) {\n        this._resolve(ml);\n      }\n    });\n\n    /**\n     * @type {?function(arg: *): *}\n     * @private\n     */\n    this._resolve = null;\n\n    /**\n     * @type {boolean}\n     * @private\n     */\n    this._loopStream = false;\n  }\n\n  /**\n   * Start observing an element for mutations\n   * @param {Node} elem - The element to be observed for mutations\n   * @param {Object} config - Configuration object accepted by mutation observers\n   */\n  observe(elem, config) {\n    this.mo.observe(elem, config);\n    this._loopStream = true;\n  }\n\n  /**\n   * Start observing an element for mutations and receive an async iterator\n   * yielding the observed mutations\n   * @param {Node} elem - The element to be observed for mutations\n   * @param {Object} config - Configuration object accepted by mutation observers\n   * @return {AsyncIterableIterator<MutationRecord[]>}\n   */\n  observeStream(elem, config) {\n    this.observe(elem, config);\n    return this.streamItr();\n  }\n\n  /**\n   * Creates a conditional mutation stream. If the startPredicate\n   * does not return true then the the observer discontents ending the stream.\n   * Otherwise the stream continues to emit mutations until the observer is\n   * disconnected or the stopPredicate returns true. The stopPredicate is polled\n   * at 1.5 second intervals when the observer is waiting for the next mutation.\n   * @param {Node} elem - The element to be observed for mutations\n   * @param {Object} config - Configuration object accepted by mutation observers\n   * @param {function(): boolean} startPredicate - Predicate function right before\n   * mutations are yielded to determine if the stream should end immediately or not\n   * @param {function(): boolean} stopPredicate - Predicate function polled\n   * while waiting for mutations to occur that returns false to indicate\n   * the stream should end.\n   * @return {AsyncIterator<MutationRecord[]>}\n   */\n  predicatedStream(elem, config, startPredicate, stopPredicate) {\n    this.observe(elem, config);\n    return this.predicateStreamItr(startPredicate, stopPredicate);\n  }\n\n  /**\n   * Disconnects the mutation observer and ends the stream\n   */\n  disconnect() {\n    this.mo.disconnect();\n    this._loopStream = false;\n    if (this._resolve) {\n      this._resolve(null);\n    }\n    this._resolve = null;\n  }\n\n  /**\n   * Returns a promise that resolves with the next observed mutation\n   * @return {Promise<?MutationRecord[]>}\n   * @private\n   */\n  _getNext() {\n    return new Promise(resolve => {\n      this._resolve = resolve;\n    });\n  }\n\n  /**\n   * Returns an stream (async iterator) that yields\n   * the observed mutations. Must have called {@link observe} before\n   * calling this method, otherwise no mutations will be yielded\n   * @return {AsyncIterator<MutationRecord[]>}\n   */\n  async *streamItr() {\n    let next;\n    while (this._loopStream) {\n      next = await this._getNext();\n      if (next == null) {\n        break;\n      }\n      yield next;\n    }\n    this.disconnect();\n  }\n\n  /**\n   * Returns an mutation stream that ends if the startPredicate returns false\n   * otherwise keeps the stream alive until disconnect or the stopPredicate, polled\n   * at 1.5 second intervals when waiting for next mutation, returns false.\n   * Automatically disconnects at the end.\n   * @param {function(): boolean} startPredicate - Predicate function right before\n   * mutations are yielded to determine if the stream should end immediately or not\n   * @param {function(): boolean} stopPredicate - Predicate function polled\n   * while waiting for mutations to occur that returns false to indicate\n   * the stream should end.\n   * @return {AsyncIterator<?MutationRecord[]>}\n   */\n  async *predicateStreamItr(startPredicate, stopPredicate) {\n    if (!startPredicate()) {\n      return this.disconnect();\n    }\n    let checkTo;\n    let next;\n    while (this._loopStream) {\n      next = await Promise.race([\n        this._getNext(),\n        new Promise(resolve => {\n          checkTo = setInterval(() => {\n            if (stopPredicate()) {\n              checkTo = null;\n              clearInterval(checkTo);\n              return resolve(null);\n            }\n          }, 1500);\n        }),\n      ]);\n      if (checkTo) {\n        clearInterval(checkTo);\n        checkTo = null;\n      }\n      if (next == null) break;\n      yield next;\n    }\n    this.disconnect();\n  }\n\n  /**\n   * @return {AsyncIterator<?MutationRecord[]>}\n   */\n  [Symbol.asyncIterator]() {\n    return this.streamItr();\n  }\n}\n\n\n//# sourceURL=webpack://lib/./lib/mutations.js?");

/***/ }),

/***/ "./lib/outlinkCollector.js":
/*!*********************************!*\
  !*** ./lib/outlinkCollector.js ***!
  \*********************************/
/*! exports provided: addOutLinks, collectOutlinksFromDoc, collectOutlinksFrom, addOutlink */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"addOutLinks\", function() { return addOutLinks; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"collectOutlinksFromDoc\", function() { return collectOutlinksFromDoc; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"collectOutlinksFrom\", function() { return collectOutlinksFrom; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"addOutlink\", function() { return addOutlink; });\n/** @ignore */\nlet __outlinksSet__;\n/** @ignore */\nconst ignoredSchemes = [\n  'about:',\n  'data:',\n  'mailto:',\n  'javascript:',\n  'js:',\n  '{',\n  '*',\n  'ftp:',\n  'tel:',\n];\n/** @ignore */\nconst goodSchemes = { 'http:': true, 'https:': true };\n/** @ignore */\nconst outlinkSelector = 'a[href], area[href]';\n/** @ignore */\nlet outLinkURLParser;\n/** @ignore */\nlet didInit = false;\n\n/**\n * @ignore\n */\nfunction initOutlinkCollection() {\n  didInit = true;\n  outLinkURLParser = new URL('about:blank');\n  if (window.$wbOutlinkSet$ == null) {\n    __outlinksSet__ = new Set();\n    Object.defineProperty(window, '$wbOutlinkSet$', {\n      value: __outlinksSet__,\n      enumerable: false,\n    });\n  } else {\n    window.$wbOutlinkSet$.clear();\n    __outlinksSet__ = window.$wbOutlinkSet$;\n  }\n\n  if (typeof window.$wbOutlinks$ === 'undefined') {\n    Object.defineProperty(window, '$wbOutlinks$', {\n      get() {\n        const outlinks = Array.from(__outlinksSet__);\n        __outlinksSet__.clear();\n        return outlinks;\n      },\n      set() {},\n      enumerable: false,\n    });\n  }\n}\n\n/**\n * @ignore\n */\nfunction shouldIgnoreLink(test) {\n  for (let i = 0; i < ignoredSchemes.length; ++i) {\n    if (test.startsWith(ignoredSchemes[i])) {\n      return true;\n    }\n  }\n  let parsed = true;\n  try {\n    outLinkURLParser.href = test;\n  } catch (error) {\n    parsed = false;\n  }\n  return !(parsed && goodSchemes[outLinkURLParser.protocol]);\n}\n\n/**\n * Add the array/nodelist of A or Anchor tags href properties the collected outlinks\n * @param {Array<HTMLAnchorElement|HTMLAreaElement>|NodeList<HTMLAnchorElement|HTMLAreaElement>} toAdd - The elements with\n * href properties that are to be added to the collected outlinks\n */\nfunction addOutLinks(toAdd) {\n  if (window.$WBNOOUTLINKS) {\n    return;\n  }\n  if (!didInit) initOutlinkCollection();\n  let href;\n  for (var i = 0; i < toAdd.length; i++) {\n    href = toAdd[i].href.trim();\n    if (href && !__outlinksSet__.has(href) && !shouldIgnoreLink(href)) {\n      __outlinksSet__.add(href);\n    }\n  }\n}\n\n/**\n * Collects the outlinks from the document\n */\nfunction collectOutlinksFromDoc() {\n  if (window.$WBNOOUTLINKS) {\n    return;\n  }\n  if (!didInit) initOutlinkCollection();\n  addOutLinks(document.querySelectorAll(outlinkSelector));\n}\n\n/**\n * Collects outlinks form the supplied element\n * @param {SomeElement|Document} queryFrom - The element to collect outlinks\n * from\n */\nfunction collectOutlinksFrom(queryFrom) {\n  if (window.$WBNOOUTLINKS) {\n    return;\n  }\n  if (!didInit) initOutlinkCollection();\n  addOutLinks(queryFrom.querySelectorAll(outlinkSelector));\n}\n\n/**\n * Add the URL or href value of the supplied argument\n * @param {HTMLAnchorElement|HTMLAreaElement|string} elemOrString - The\n * element with href property or string to add to the collected outlinks\n */\nfunction addOutlink(elemOrString) {\n  if (window.$WBNOOUTLINKS) {\n    return;\n  }\n  if (!didInit) initOutlinkCollection();\n  const href = (elemOrString.href || elemOrString).trim();\n  if (href && !__outlinksSet__.has(href) && !shouldIgnoreLink(href)) {\n    __outlinksSet__.add(href);\n  }\n}\n\n\n//# sourceURL=webpack://lib/./lib/outlinkCollector.js?");

/***/ }),

/***/ "./lib/postStepFNs.js":
/*!****************************!*\
  !*** ./lib/postStepFNs.js ***!
  \****************************/
/*! exports provided: doneOrWait, buildCustomPostStepFn */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"doneOrWait\", function() { return doneOrWait; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"buildCustomPostStepFn\", function() { return buildCustomPostStepFn; });\n/**\n * The default post step function used for all behaviors that do not provide one.\n * @param {RawBehaviorStepResults} rawResults\n * @return {BehaviorStepResults}\n */\nfunction doneOrWait(rawResults) {\n  const result = {\n    done: rawResults.done,\n    wait: false,\n    msg: 'No message',\n    state: {},\n  };\n  if (typeof rawResults.value === 'object') {\n    result.wait = !!rawResults.value.wait;\n    result.msg = rawResults.value.msg || result.msg;\n    result.state = rawResults.value.state || result.state;\n  } else {\n    result.wait = !!rawResults.value;\n  }\n  return result;\n}\n\n/**\n * Creates and returns a function wrapping the provided post step function\n * will call the supplied function and return the expected output.\n * @param {function(rawResults: RawBehaviorStepResults): void} customFN\n * @return {function(state: RawBehaviorStepResults): BehaviorStepResults}\n */\nfunction buildCustomPostStepFn(customFN) {\n  return function wrappedPostStep(state) {\n    customFN(state);\n    return doneOrWait(state);\n  };\n}\n\n/**\n * @typedef {Object} BehaviorStepResults\n * @property {boolean} done - Has the behavior finished performing its actions\n * @property {boolean} wait - Should a wait for network idle be performed if possible by the runner\n * @property {string} msg - A message from the behavior representing a description of the action just performed\n * @property {*} state - Any additional information about the full state of the behavior\n * @property {Array<string>} [outlinks] - Any outlinks collected during this step, will only be defined if\n * outlinks are to be collected when using {@link BehaviorRunner#autoRunIter}.\n */\n\n/**\n * @typedef {Object} RawBehaviorStepResults\n * @property {boolean} done - Is the generator finished\n * @property {?BehaviorState} value - The value yielded by the behavior\n */\n\n\n//# sourceURL=webpack://lib/./lib/postStepFNs.js?");

/***/ }),

/***/ "./lib/reactUtils.js":
/*!***************************!*\
  !*** ./lib/reactUtils.js ***!
  \***************************/
/*! exports provided: getReactRootHostElem, getInternalRootOnElem, getReactRootContainer, reactInstanceFromDOMElem, makeReactInstanceFromDOMElemFun, reactInstancesFromElements, findChildWithKey, findReduxStore */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getReactRootHostElem\", function() { return getReactRootHostElem; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getInternalRootOnElem\", function() { return getInternalRootOnElem; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"getReactRootContainer\", function() { return getReactRootContainer; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"reactInstanceFromDOMElem\", function() { return reactInstanceFromDOMElem; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"makeReactInstanceFromDOMElemFun\", function() { return makeReactInstanceFromDOMElemFun; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"reactInstancesFromElements\", function() { return reactInstancesFromElements; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"findChildWithKey\", function() { return findChildWithKey; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"findReduxStore\", function() { return findReduxStore; });\n/** @ignore */\nconst __ReactProps = {\n  rootContainer: '_reactRootContainer',\n  internalRoot: '_internalRoot',\n  onDomNode: '__reactInternalInstance',\n  rootHostElemId: 'react-root',\n  mProps: 'memoizedProps',\n};\n\n/**\n * Returns the root host element of an react application (id = react-root)\n * @param {string} [alternativeId] - An alternative id to use rather than the default\n * @return {?HTMLElement}\n */\nfunction getReactRootHostElem(alternativeId) {\n  return document.getElementById(\n    alternativeId != null ? alternativeId : __ReactProps.rootHostElemId\n  );\n}\n\n/**\n * Returns the internal root object added by react on the supplied element.\n * Property `_internalRoot`\n * @param {HTMLElement|Element|Node} elem\n * @return {Object}\n */\nfunction getInternalRootOnElem(elem) {\n  return elem[__ReactProps.internalRoot];\n}\n\n/**\n * Returns the root container object added by react\n * @param {HTMLElement|Element|Node} elem\n * @return {?Object}\n */\nfunction getReactRootContainer(elem) {\n  if (!elem) return null;\n  const hostContainer = elem[__ReactProps.rootContainer];\n  if (hostContainer) {\n    return hostContainer[__ReactProps.internalRoot];\n  }\n  return null;\n}\n\n/**\n * Returns the react instance object that lives on the live element\n * @param {HTMLElement|Element|Node} elem - The element to have its\n * react instance extracted from\n * @return {?Object}\n */\nfunction reactInstanceFromDOMElem(elem) {\n  const keys = Object.keys(elem);\n  let len = keys.length;\n  let internalKey;\n  for (var i = 0; i < len; ++i) {\n    if (keys[i].startsWith(__ReactProps.onDomNode)) {\n      internalKey = keys[i];\n      break;\n    }\n  }\n  if (!internalKey) return null;\n  return elem[internalKey];\n}\n\n/**\n * Returns a function for retrieving the react instance associated with\n * the supplied element. Unlike {@link reactInstanceFromDOMElem} the returned\n * function discovers how react is embedding itself on the element once\n * and uses the discovered information for every other element.\n * @return {function(elem: SomeElement): ?Object}\n */\nfunction makeReactInstanceFromDOMElemFun() {\n  let memoizedInstanceKey;\n  const whichKey = key => key.startsWith('__reactInternalInstance');\n  const findKey = elem => Object.keys(elem).find(whichKey);\n  return function reactInstanceFromDOMElem(elem) {\n    if (!elem) return null;\n    if (memoizedInstanceKey == null) {\n      memoizedInstanceKey = findKey(elem);\n      if (memoizedInstanceKey == null) return null;\n    }\n    if (elem[memoizedInstanceKey]) return elem[memoizedInstanceKey];\n    const maybeNewKey = findKey(elem);\n    if (maybeNewKey && elem[maybeNewKey]) {\n      memoizedInstanceKey = maybeNewKey;\n      return elem[maybeNewKey];\n    }\n    return null;\n  };\n}\n\n/**\n * Converts the supplied array of elements into an array of objects\n * each with property node (the live dom element) and reactInstance (the live\n * react component). An optional selection function can be supplied that\n * receives the rendered components key and returns T/F to indicate if the\n * component and element is to be selected\n * @param {Array<Node|HTMLElement|Element>} elems - The array of elements to\n * get their react instances\n * @param {function(key: string): boolean} [selectingFn] - Optional selection\n * function that takes a components key and returns T/F indicating if the\n * component is selected\n * @return {Array<{node: HTMLElement|Element|Node, reactInstance: Object}>}\n */\nfunction reactInstancesFromElements(elems, selectingFn) {\n  const renderedNodes = [];\n  const length = elems.length;\n  let node;\n  let reactInstance;\n  for (var i = 0; i < length; ++i) {\n    node = elems[i];\n    reactInstance = reactInstanceFromDOMElem(node);\n    if (selectingFn && selectingFn(reactInstance.key)) {\n      renderedNodes.push({ node, reactInstance });\n    } else {\n      renderedNodes.push({ node, reactInstance });\n    }\n  }\n  return renderedNodes;\n}\n\n/**\n * Walks the children of a react component to\n * find the one that has the supplied key\n * @param {Object} reactInstance - The react instance who's children are\n * to be descended looking for the one with key\n * @param {string} key - The key of the child to retrieve\n * @return {?Object} - The found child if found\n */\nfunction findChildWithKey(reactInstance, key) {\n  let child = reactInstance.child;\n  while (child) {\n    if (child.key && child.key === key) {\n      return child;\n    }\n    child = child.child;\n  }\n  return null;\n}\n\n/**\n * Attempts the find the redux store from the supplied component\n * @param {Object} startingComponent\n * @return {?Object}\n */\nfunction findReduxStore(startingComponent) {\n  if (!startingComponent) return null;\n  let component;\n  const q = [startingComponent];\n  while (q.length) {\n    component = q.shift();\n    if (component.memoizedProps && component.memoizedProps.store) {\n      return component.memoizedProps.store;\n    }\n    if (component.child) {\n      q.push(component.child);\n    }\n    if (component.sibling) {\n      q.push(component.sibling);\n    }\n  }\n  return null;\n}\n\n\n//# sourceURL=webpack://lib/./lib/reactUtils.js?");

/***/ }),

/***/ "./lib/scrolls.js":
/*!************************!*\
  !*** ./lib/scrolls.js ***!
  \************************/
/*! exports provided: scrollIntoView, scrollIntoViewWithDelay, scrollIntoViewAndWaitFor, scrollToElemOffset, scrollToElemOffsetWithDelay, scrollDownByElemHeight, scrollDownByElemHeightWithDelay, canScrollDownMore, canScrollUpMore, scrollWindowBy, smoothScrollWindowBy, scrollWindowDownBy, smoothScrollWindowDownBy, scrollWindowByWithDelay, scrollWindowDownByWithDelay, createScrollAmount, createScroller */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoView\", function() { return scrollIntoView; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoViewWithDelay\", function() { return scrollIntoViewWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollIntoViewAndWaitFor\", function() { return scrollIntoViewAndWaitFor; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollToElemOffset\", function() { return scrollToElemOffset; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollToElemOffsetWithDelay\", function() { return scrollToElemOffsetWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollDownByElemHeight\", function() { return scrollDownByElemHeight; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollDownByElemHeightWithDelay\", function() { return scrollDownByElemHeightWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"canScrollDownMore\", function() { return canScrollDownMore; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"canScrollUpMore\", function() { return canScrollUpMore; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollWindowBy\", function() { return scrollWindowBy; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"smoothScrollWindowBy\", function() { return smoothScrollWindowBy; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollWindowDownBy\", function() { return scrollWindowDownBy; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"smoothScrollWindowDownBy\", function() { return smoothScrollWindowDownBy; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollWindowByWithDelay\", function() { return scrollWindowByWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"scrollWindowDownByWithDelay\", function() { return scrollWindowDownByWithDelay; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"createScrollAmount\", function() { return createScrollAmount; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"createScroller\", function() { return createScroller; });\n/* harmony import */ var _delays__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./delays */ \"./lib/delays.js\");\n\n\n/** @ignore */\nlet isBadFF;\n\n/**\n * Scrolls the supplied element into view.\n *\n * Scroll behavior:\n *  - behavior: smooth\n *  - block: center\n *  - inline: center\n * @param {Element} elem - The element to be scrolled into view\n * @param {Object} [opts] - Optional scroll behavior to be\n * used rather than the default\n * @return {void}\n */\nfunction scrollIntoView(elem, opts) {\n  if (elem == null) return;\n  if (isBadFF == null) {\n    isBadFF = /Firefox\\/57(?:\\.[0-9]+)?/i.test(window.navigator.userAgent);\n  }\n  const defaults = isBadFF\n    ? { behavior: 'smooth', inline: 'center' }\n    : {\n        behavior: 'smooth',\n        block: 'center',\n        inline: 'center',\n      };\n  elem.scrollIntoView(\n    opts && typeof opts === 'object' ? Object.assign(defaults, opts) : defaults\n  );\n}\n\n/**\n * Scrolls the supplied element into view, afterwards waits for the specified delay time\n *\n * Scroll behavior:\n *  - behavior: smooth\n *  - block: center\n *  - inline: center\n * @param {Element|HTMLElement|Node} elem - The element to be scrolled into view with delay\n * @param {number} [delayTime = 1000] - How long is the delay\n * @return {Promise<void>}\n */\nfunction scrollIntoViewWithDelay(elem, delayTime) {\n  scrollIntoView(elem);\n  return Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"delay\"])(delayTime || 1000);\n}\n\n/**\n * Scrolls the supplied element into view and then waits for the supplied predicate function to return true\n *\n * Scroll behavior:\n *  - behavior: smooth\n *  - block: center\n *  - inline: center\n * @param {Element|HTMLElement|Node} elem - The element to be scrolled into view with delay\n * @param {function(): boolean} predicate - Function returning T/F to indicate when the\n * condition waited for has been satisfied\n * @param {{wait: ?WaitForOptions, scrollBehavior: ?Object}} [options] - Options controlling\n * the scroll behavior and how the wait will happen\n * @return {Promise<{predicate: boolean, maxExceeded: boolean}>}\n */\nfunction scrollIntoViewAndWaitFor(elem, predicate, options) {\n  scrollIntoView(elem, options && options.scrollBehavior);\n  return Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"waitForPredicate\"])(predicate, options && options.wait);\n}\n\n/**\n * Scrolls the window by the supplied elements offsetTop. If the elements\n * offsetTop is zero then {@link scrollIntoView} is used\n * @param {Element|HTMLElement|Node} elem - The element who's offsetTop will be used to scroll by\n * @param {string} [behavior] - Options controlling the behavior of window.scrollTo\n * defaults to auto\n * @return {void}\n */\nfunction scrollToElemOffset(elem, behavior) {\n  if (elem.offsetTop === 0) {\n    return scrollIntoView(elem);\n  }\n  window.scrollTo({\n    behavior: behavior || 'auto',\n    left: 0,\n    top: elem.offsetTop,\n  });\n}\n\n/**\n * Scrolls the window by the supplied elements offsetTop. If the elements\n * offsetTop is zero then {@link scrollIntoView} is used\n * @param {Element|HTMLElement|Node} elem - The element who's offsetTop will be used to scroll by\n * @param {number} [delayTime = 1000] - How long is the delay\n * @return {Promise<void>}\n */\nfunction scrollToElemOffsetWithDelay(elem, delayTime) {\n  scrollToElemOffset(elem);\n  return Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"delay\"])(delayTime || 1000);\n}\n\n/**\n * Scrolls down by the elements height\n * @param {Element|HTMLElement|Node} elem - The element who's height\n * to scroll down by\n * @return {void}\n */\nfunction scrollDownByElemHeight(elem) {\n  if (!elem) return;\n  const rect = elem.getBoundingClientRect();\n  scrollWindowBy(0, rect.height + elem.offsetHeight);\n}\n\n/**\n * Scrolls down by supplied elements height and then waits for\n * the supplied delay time.\n * @param {Element|HTMLElement|Node} elem - The element to be\n * @param {number} [delayTime = 1000] - How long is the delay\n * @return {Promise<void>}\n */\nfunction scrollDownByElemHeightWithDelay(elem, delayTime) {\n  scrollDownByElemHeight(elem);\n  return Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"delay\"])(delayTime || 1000);\n}\n\n/**\n * Determines if we can scroll down any more\n * @return {boolean} - T/F indicating if we can scroll down some more\n */\nfunction canScrollDownMore() {\n  return (\n    window.scrollY + window.innerHeight <\n    Math.max(\n      document.body.scrollHeight,\n      document.body.offsetHeight,\n      document.body.clientHeight,\n      document.documentElement.scrollHeight,\n      document.documentElement.offsetHeight,\n      document.documentElement.clientHeight\n    )\n  );\n}\n\n/**\n * Determines if we can scroll up any more\n * @return {boolean} - T/F indicating if we can scroll up some more\n */\nfunction canScrollUpMore() {\n  return window.scrollY !== 0;\n}\n\n/**\n * Scrolls the window by the supplied x and y amount\n * @param {number} x - Amount to scroll in the x direction\n * @param {number} y - Amount to scroll in the y direction\n */\nfunction scrollWindowBy(x, y) {\n  window.scrollBy(x, y);\n}\n\n/**\n * Scrolls the window by the supplied x and y amount smoothly\n * @param {number} x - Amount to scroll in the x direction\n * @param {number} y - Amount to scroll in the y direction\n */\nfunction smoothScrollWindowBy(x, y) {\n  window.scrollBy({ left: x, top: y, behavior: 'smooth' });\n}\n\n/**\n * Scrolls the window down by the supplied amount\n * @param {number} amount - Amount to scroll the down by\n */\nfunction scrollWindowDownBy(amount) {\n  scrollWindowBy(0, amount);\n}\n\n/**\n * Scrolls the window down by the supplied amount smoothly\n * @param {number} amount - Amount to scroll the down by\n */\nfunction smoothScrollWindowDownBy(amount) {\n  smoothScrollWindowBy(0, amount);\n}\n\n/**\n * Scrolls the window by the supplied x and y amount\n * @param {number} x - Amount to scroll in the x direction\n * @param {number} y - Amount to scroll in the y direction\n * @param {number} [delayTime = 1500]\n * @return {Promise<void>}\n */\nfunction scrollWindowByWithDelay(x, y, delayTime) {\n  scrollWindowBy(x, y);\n  return Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"delay\"])(delayTime || 1500);\n}\n\n/**\n * Scrolls the window down by the supplied amount\n * @param {number} amount - Amount to scroll the down by\n * @param {number} [delayTime = 1500]\n * @return {Promise<void>}\n */\nfunction scrollWindowDownByWithDelay(amount, delayTime) {\n  scrollWindowBy(0, amount);\n  return Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"delay\"])(delayTime || 1500);\n}\n\n/**\n * Creates and returns an object that calculates the scroll amount, up/down or left/right,\n * based on dividing the scroll width/height by the supplied desired times to completely\n * scroll the page, defaults to 10.\n *\n * The calculation also takes into account the fact that the height or width of\n * the document may change and reacts to those changes such that the returned scroll\n * amount is always proportional to the documents current maximum scroll height or width\n *\n * @param {number} [desiredTimesToScroll = 10] - How many scrolls until the maximum scroll\n * position is reached\n * @return {{timesToScroll: number, scrollUpDownAmount: number, scrollLeftRightAmount: number}}\n */\nfunction createScrollAmount(desiredTimesToScroll) {\n  let docsBoundingCRect = document.documentElement.getBoundingClientRect();\n  const getMaxUpDown = () =>\n    Math.max(\n      document.scrollingElement.scrollHeight,\n      document.documentElement.scrollHeight,\n      docsBoundingCRect.bottom\n    );\n  const getMaxLeftRight = () =>\n    Math.max(\n      document.scrollingElement.scrollWidth,\n      document.documentElement.scrollWidth,\n      docsBoundingCRect.right\n    );\n  let lastUpDownMax = getMaxUpDown();\n  let lastLeftRightMax = getMaxLeftRight();\n  return {\n    timesToScroll: desiredTimesToScroll || 10,\n    get scrollUpDownAmount() {\n      let currentMax = getMaxUpDown();\n      if (currentMax !== lastUpDownMax) {\n        docsBoundingCRect = document.documentElement.getBoundingClientRect();\n        currentMax = getMaxUpDown();\n        lastUpDownMax = currentMax;\n      }\n      return currentMax / this.timesToScroll;\n    },\n    get scrollLeftRightAmount() {\n      let currentMax = getMaxLeftRight();\n      if (currentMax !== lastLeftRightMax) {\n        docsBoundingCRect = document.documentElement.getBoundingClientRect();\n        currentMax = getMaxLeftRight();\n        lastLeftRightMax = currentMax;\n      }\n      return currentMax / this.timesToScroll;\n    },\n  };\n}\n\n/**\n * @typedef {Object} Scroller\n * @property {number} timesToScroll - the desired number of times to be scrolled\n * @property {number} scrollUpDownAmount - The current amount to be scrolled up or down\n * @property {number} scrollLeftRightAmount - The current amount to be scrolled left or right\n * @property {function(): void} scrollRight - Scroll the page right\n * @property {function(): void} scrollLeft - Scroll the page left\n * @property {function(): void} scrollUp - Scroll the page up\n * @property {function(): void} scrollDown - Scroll the page down\n * @property {function(): boolean} canScrollDownMore - Can the page be scrolled some more down\n * @property {function(): boolean} canScrollUpMore - Can the page be scrolled some more up\n */\n\n/**\n * Creates and returns an object for scrolling the page up/down and left/right.\n * The number of times a respective scroll has occurred is also tracked.\n *\n * For additional details see the documentation of {@link createScrollAmount}\n * as the amount of scroll applied is determined the object it returns.\n *\n * @param {number} [timesToScroll = 10] - How many scrolls until the maximum scroll\n * position is reached\n * @return {Scroller}\n */\nfunction createScroller(timesToScroll) {\n  const scrollAmount = createScrollAmount(timesToScroll || 10);\n  return Object.assign(\n    {\n      canScrollDownMore,\n      canScrollUpMore,\n      scrollDown() {\n        scrollWindowBy(0, this.scrollUpDownAmount);\n      },\n      scrollUp() {\n        scrollWindowBy(0, -this.scrollUpDownAmount);\n      },\n      scrollLeft() {\n        scrollWindowBy(-this.scrollLeftRightAmount, 0);\n      },\n      scrollRight() {\n        scrollWindowBy(this.scrollLeftRightAmount, 0);\n      },\n    },\n    scrollAmount\n  );\n}\n\n\n//# sourceURL=webpack://lib/./lib/scrolls.js?");

/***/ }),

/***/ "./lib/strings.js":
/*!************************!*\
  !*** ./lib/strings.js ***!
  \************************/
/*! exports provided: camelCase, capitalize, camelCaseToDashDelim, camelCaseToUnderscoreDelim, titleCase, collapseWhitespace, stringBetween, isAlpha, isAlphaNumeric, isEmptyString, isLower, isNumeric, isUpper, toBoolean, toFloat, toInt, substringFromLastIndexOf, substringFromIndexOf */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"camelCase\", function() { return camelCase; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"capitalize\", function() { return capitalize; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"camelCaseToDashDelim\", function() { return camelCaseToDashDelim; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"camelCaseToUnderscoreDelim\", function() { return camelCaseToUnderscoreDelim; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"titleCase\", function() { return titleCase; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"collapseWhitespace\", function() { return collapseWhitespace; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"stringBetween\", function() { return stringBetween; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isAlpha\", function() { return isAlpha; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isAlphaNumeric\", function() { return isAlphaNumeric; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isEmptyString\", function() { return isEmptyString; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isLower\", function() { return isLower; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isNumeric\", function() { return isNumeric; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isUpper\", function() { return isUpper; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"toBoolean\", function() { return toBoolean; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"toFloat\", function() { return toFloat; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"toInt\", function() { return toInt; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"substringFromLastIndexOf\", function() { return substringFromLastIndexOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"substringFromIndexOf\", function() { return substringFromIndexOf; });\n/** @ignore */\nconst __camelCaseRe = /(-|_|\\s)+(.)?/g;\n/** @ignore */\nconst __camelCaseReplacer = (match, sep, c) => (c ? c.toUpperCase() : '');\n\n/**\n * Attempts to convert the supplied string into camel case\n * @param {string} str\n * @return {string}\n */\nfunction camelCase(str) {\n  return str.replace(__camelCaseRe, __camelCaseReplacer);\n}\n\n/**\n * Attempts to capitalize the supplied string\n * @param {string} str\n * @return {string}\n */\nfunction capitalize(str) {\n  return str.substr(0, 1).toUpperCase() + str.substring(1).toLowerCase();\n}\n\n/** @ignore */\nconst __ccToDashDelimRe1 = /[_\\s]+/g;\n/** @ignore */\nconst __ccToDashDelimRe2 = /([A-Z])/g;\n/** @ignore */\nconst __ccToDashDelimRe3 = /-+/g;\n\n/**\n * Attempts to convert the supplied camel case string into dash delimited string\n * @param {?string} str\n * @return {?string}\n */\nfunction camelCaseToDashDelim(str) {\n  if (str) {\n    return str\n      .trim()\n      .replace(__ccToDashDelimRe1, '-')\n      .replace(__ccToDashDelimRe2, '-$1')\n      .replace(__ccToDashDelimRe3, '-')\n      .toLowerCase();\n  }\n  return str;\n}\n\n/** @ignore */\nconst __ccToUnderDelimRe1 = /([a-z\\d])([A-Z]+)/g;\n/** @ignore */\nconst __ccToUnderDelimRe2 = /([A-Z\\d]+)([A-Z][a-z])/g;\n/** @ignore */\nconst __ccToUnderDelimRe3 = /[-\\s]+/g;\n\n/**\n * Attempts to convert the supplied camel case string into an underscore delimited string\n * @param {?string} str\n * @return {?string}\n */\nfunction camelCaseToUnderscoreDelim(str) {\n  if (str) {\n    str\n      .trim()\n      .replace(__ccToUnderDelimRe1, '$1_$2')\n      .replace(__ccToUnderDelimRe2, '$1_$2')\n      .replace(__ccToUnderDelimRe3, '_')\n      .toLowerCase();\n  }\n  return str;\n}\n\n/** @ignore */\nconst __titleCaseRe = /(^[a-z]| [a-z]|-[a-z]|_[a-z])/g;\n/** @ignore */\nconst __titleCaseReplacer = value => value.toUpperCase();\n\n/**\n * Attempts to convert the supplied string into title case\n * @param {?string} str\n * @return {?string}\n */\nfunction titleCase(str) {\n  if (str) {\n    return str.replace(__titleCaseRe, __titleCaseReplacer);\n  }\n  return str;\n}\n\n/** @ignore */\nconst __collapseWhitespaceRe1 = /[\\s\\xa0]+/g;\n/** @ignore */\nconst __collapseWhitespaceRe2 = /^\\s+|\\s+$/g;\n\n/**\n * Attempts to collapse all extra white space into a single white space character\n * @param {string} str\n * @return {string}\n */\nfunction collapseWhitespace(str) {\n  return str\n    .replace(__collapseWhitespaceRe1, ' ')\n    .replace(__collapseWhitespaceRe2, '');\n}\n\n/**\n * Returns the string between the left and right string\n * @param {string} str\n * @param {string} left\n * @param {string} right\n * @return {string}\n */\nfunction stringBetween(str, left, right) {\n  const startPos = str.indexOf(left);\n  const endPos = str.indexOf(right, startPos + left.length);\n  if (endPos === -1 && right != null) {\n    return '';\n  } else if (endPos === -1 && right == null) {\n    return str.substring(startPos + left.length);\n  }\n  return str.slice(startPos + left.length, endPos);\n}\n\n/** @ignore */\nconst __isAlphaRe = /[^a-z\\xDF-\\xFF]|^$/;\n\n/**\n * Returns T/F indicating if the supplied string is only alpha characters\n * @param {?string} str\n * @return {boolean}\n */\nfunction isAlpha(str) {\n  if (str) {\n    return !__isAlphaRe.test(str.toLowerCase());\n  }\n  return false;\n}\n\n/** @ignore */\nconst __isAlphaNumericRe = /[^0-9a-z\\xDF-\\xFF]/;\n\n/**\n * Returns T/F indicating if the supplied string is only alpha numeric characters\n * @param {?string} str\n * @return {boolean}\n */\nfunction isAlphaNumeric(str) {\n  if (str) {\n    return !__isAlphaNumericRe.test(str.toLowerCase());\n  }\n  return false;\n}\n\n/** @ignore */\nconst __isEmptyRe = /^[\\s\\xa0]*$/;\n\n/**\n * Returns T/F indicating if the supplied string is truly an empty string\n * @param {?string} str\n * @return {boolean}\n */\nfunction isEmptyString(str) {\n  return str == null ? true : __isEmptyRe.test(str);\n}\n\n/**\n * Returns T/F indicating if the supplied string is only lowercase alpha characters\n * @param {string} str\n * @return {boolean}\n */\nfunction isLower(str) {\n  return isAlpha(str) && str.toLowerCase() === str;\n}\n\n/** @ignore */\nconst __isNumericRe = /[^0-9]/;\n\n/**\n * Returns T/F indicating if the supplied string only contains numeric characters\n * @param {?string} str\n * @return {boolean}\n */\nfunction isNumeric(str) {\n  return str != null ? !__isNumericRe.test(str) : false;\n}\n\n/**\n * Returns T/F indicating if the supplied string is only uppercase alpha characters\n * @param {?string} str\n * @return {boolean}\n */\nfunction isUpper(str) {\n  return isAlpha(str) && str.toUpperCase() === str;\n}\n\n/**\n * Attempts to convert the supplied string to boolean (true, yes, on, 1)\n * @param {string|boolean|number} str\n * @return {boolean}\n */\nfunction toBoolean(str) {\n  if (typeof str === 'string') {\n    const s = str.toLowerCase();\n    return s === 'true' || s === 'yes' || s === 'on' || s === '1';\n  }\n  return str === true || str === 1;\n}\n\n/**\n * Attempts to convert the supplied string to a float\n * @param {string} str\n * @param {number} [precision]\n * @return {number}\n */\nfunction toFloat(str, precision) {\n  const num = parseFloat(str);\n  if (precision) {\n    return parseFloat(num.toFixed(precision));\n  }\n  return num;\n}\n\n/**\n * Attempts to convert the supplied string to a integer\n * @param {string} str\n * @param {number} [base = 10]\n * @return {number}\n */\nfunction toInt(str, base = 10) {\n  return parseInt(str, base);\n}\n\n/**\n * Returns the substring from the last index of the supplied needle\n * @param {string} str\n * @param {string} needle - The substring to be searched for\n * @param {SubstringArgs} [opts]\n * @return {string}\n */\nfunction substringFromLastIndexOf(\n  str,\n  needle,\n  { searchFrom, include } = {}\n) {\n  const idx = str.lastIndexOf(needle, searchFrom);\n  return str.substring(idx + (include ? 0 : needle.length));\n}\n\n/**\n * Returns the substring from the index of the supplied needle\n * @param {string} str\n * @param {string} needle - The substring to be searched for\n * @param {SubstringArgs} [opts]\n * @return {string}\n */\nfunction substringFromIndexOf(\n  str,\n  needle,\n  { searchFrom, include } = {}\n) {\n  const idx = str.indexOf(needle, searchFrom);\n  return str.substring(idx + (include ? 0 : needle.length));\n}\n\n/**\n * @typedef {Object} SubstringArgs\n * @property {number} [searchFrom] - Index to search from for the supplied substring\n * @property {boolean} [include] - Should the supplied substring be included in the returned substring\n **/\n\n\n//# sourceURL=webpack://lib/./lib/strings.js?");

/***/ }),

/***/ "./lib/traversals.js":
/*!***************************!*\
  !*** ./lib/traversals.js ***!
  \***************************/
/*! exports provided: traverseChildrenOfLoaderParent, traverseChildrenOfLoaderParentRemovingPrevious, traverseChildrenOf, traverseChildrenOfRemovingPrevious, traverseChildrenOfLoaderParentGenFn, walkChildrenOfCustom, traverseChildrenOfCustom, WalkEndedReasons, DisconnectingWalk */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfLoaderParent\", function() { return traverseChildrenOfLoaderParent; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfLoaderParentRemovingPrevious\", function() { return traverseChildrenOfLoaderParentRemovingPrevious; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOf\", function() { return traverseChildrenOf; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfRemovingPrevious\", function() { return traverseChildrenOfRemovingPrevious; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfLoaderParentGenFn\", function() { return traverseChildrenOfLoaderParentGenFn; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"walkChildrenOfCustom\", function() { return walkChildrenOfCustom; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"traverseChildrenOfCustom\", function() { return traverseChildrenOfCustom; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"WalkEndedReasons\", function() { return WalkEndedReasons; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"DisconnectingWalk\", function() { return DisconnectingWalk; });\n/* harmony import */ var _delays__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./delays */ \"./lib/delays.js\");\n/* harmony import */ var _dom__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./dom */ \"./lib/dom.js\");\n/* harmony import */ var _general__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./general */ \"./lib/general.js\");\n\n\n\n\n/**\n * Applies a function to the children of the supplied parent element yielding the return value of the function.\n *\n * When a child of the parent element does not have a next element sibling a wait\n * for additional child elements to be added is done for a maximum of 15 seconds and\n * if the current child element still does not have a next element sibling the generator ends.\n *\n * @param {SomeElement} parentElement - The parent element who's children are to be traversed\n * @param {function(element: SomeElement, additionalArgs: *): *} fn - The function to be applied to each child element\n * @param {*} [additionalArgs] - Any additional arguments to be supplied to the function applied to each child element\n * @return {AsyncIterableIterator<*>}\n */\nasync function* traverseChildrenOfLoaderParent(\n  parentElement,\n  fn,\n  additionalArgs\n) {\n  if (parentElement == null) return;\n  for await (const child of walkChildrenOfCustom({\n    parentElement,\n    loader: true,\n  })) {\n    const nextValue = fn(child, additionalArgs);\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(nextValue)) {\n      for await (const next of Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"noExceptGeneratorWrap\"])(nextValue)) {\n        yield next;\n      }\n    } else {\n      yield Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(nextValue) ? await nextValue : nextValue;\n    }\n  }\n}\n\n/**\n * Applies a function to the children of the supplied parent element yielding the return value of the function\n * and then removing the previously considered child element from the DOM.\n *\n * When a child of the parent element does not have a next element sibling a wait\n * for additional child elements to be added is done for a maximum of 15 seconds and\n * if the current child element still does not have a next element sibling the generator ends.\n *\n * @param {SomeElement} parentElement - The parent element who's children are to be traversed\n * @param {function(element: SomeElement, additionalArgs: *): *} fn - The function to be applied to each child element\n * @param {*} [additionalArgs] - Any additional arguments to be supplied to the function applied to each child element\n * @return {AsyncIterableIterator<*>}\n */\nasync function* traverseChildrenOfLoaderParentRemovingPrevious(\n  parentElement,\n  fn,\n  additionalArgs\n) {\n  if (parentElement == null) return;\n  for await (const child of walkChildrenOfCustom({\n    parentElement,\n    loader: true,\n    nextChild(parent, currentChild) {\n      return Object(_dom__WEBPACK_IMPORTED_MODULE_1__[\"getElemSiblingAndRemoveElem\"])(currentChild);\n    },\n  })) {\n    const nextValue = fn(child, additionalArgs);\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(nextValue)) {\n      for await (const next of Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"noExceptGeneratorWrap\"])(nextValue)) {\n        yield next;\n      }\n    } else {\n      yield Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(nextValue) ? await nextValue : nextValue;\n    }\n  }\n}\n\n/**\n * Applies a function to the children of the supplied parent element yielding the return value of the function.\n *\n * When a child of the parent element does not have a next element sibling the generator ends.\n *\n * @param {SomeElement} parentElement - The parent element who's children are to be traversed\n * @param {function(element: SomeElement, additionalArgs: *): *} fn - The function to be applied to each child element\n * @param {*} [additionalArgs] - Any additional arguments to be supplied to the function applied to each child element\n * @return {AsyncIterableIterator<*>}\n */\nasync function* traverseChildrenOf(parentElement, fn, additionalArgs) {\n  if (parentElement == null) return;\n  for (const child of Object(_dom__WEBPACK_IMPORTED_MODULE_1__[\"childElementIterator\"])(parentElement)) {\n    const nextValue = fn(child, additionalArgs);\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(nextValue)) {\n      for await (const next of Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"noExceptGeneratorWrap\"])(nextValue)) {\n        yield next;\n      }\n    } else {\n      yield Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(nextValue) ? await nextValue : nextValue;\n    }\n  }\n}\n\n/**\n * Applies a function to the children of the supplied parent element yielding the return value of the function\n * and then removing the previously considered child element from the DOM.\n *\n * When a child of the parent element does not have a next element sibling the generator ends.\n *\n * @param {SomeElement} parentElement - The parent element who's children are to be traversed\n * @param {function(element: SomeElement, additionalArgs: *): *} fn - The function to be applied to each child element\n * @param {*} [additionalArgs] - Any additional arguments to be supplied to the function applied to each child element\n * @return {AsyncIterableIterator<*>}\n */\nasync function* traverseChildrenOfRemovingPrevious(\n  parentElement,\n  fn,\n  additionalArgs\n) {\n  if (parentElement == null) return;\n  for await (const child of walkChildrenOfCustom({\n    parentElement,\n    loader: false,\n    nextChild(parent, currentChild) {\n      return Object(_dom__WEBPACK_IMPORTED_MODULE_1__[\"getElemSiblingAndRemoveElem\"])(currentChild);\n    },\n  })) {\n    const nextValue = fn(child, additionalArgs);\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(nextValue)) {\n      for await (const next of Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"noExceptGeneratorWrap\"])(nextValue)) {\n        yield next;\n      }\n    } else {\n      yield Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(nextValue) ? await nextValue : nextValue;\n    }\n  }\n}\n\n/**\n * Applies a function to the children of the supplied parent element yielding the return value of the function.\n *\n * When a child of the parent element does not have a next element sibling, the supplied genFun is called and a wait\n * for additional child elements to be added is done for a maximum of 15 seconds.\n * After the wait ends and if the current child element still does not have a next element sibling the generator ends.\n *\n * @param {SomeElement} parentElement - The parent element who's children are to be traversed\n * @param {function(): BoolOrPromiseBool} genFn - Function used to generator more elements returning T/F indicating if\n * a wait should be done\n * @param {function(element: SomeElement, additionalArgs: *): *} fn - The function to be applied to each child element\n * @param {*} [additionalArgs] - Any additional arguments to be supplied to the function applied to each child element\n * @return {AsyncIterableIterator<*>}\n */\nasync function* traverseChildrenOfLoaderParentGenFn(\n  parentElement,\n  fn,\n  genFn,\n  additionalArgs\n) {\n  if (parentElement == null) return;\n  for await (const child of walkChildrenOfCustom({\n    parentElement,\n    loader: true,\n    async shouldWait(parent, currentChild) {\n      if (currentChild.nextElementSibling != null) return false;\n      const fromGen = genFn();\n      return Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(fromGen) ? await fromGen : fromGen;\n    },\n  })) {\n    const nextValue = fn(child, additionalArgs);\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(nextValue)) {\n      for await (const next of Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"noExceptGeneratorWrap\"])(nextValue)) {\n        yield next;\n      }\n    } else {\n      yield Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(nextValue) ? await nextValue : nextValue;\n    }\n  }\n}\n\n/**\n * Walks the children of the supplied parent element yielding its children.\n *\n * If the loader option is true or shouldWait and or wait function(s) are supplied then the walk\n * behaves as if the loader was set to true.\n *\n * When operating in loader mode and a child of the parent element does not have a next element sibling, a wait\n * for additional child elements to be added is done, if no custom wait function was supplied the wait happens for a maximum of 15 seconds.\n * After the wait ends and if the current child element still does not have a next element sibling the generator ends.\n *\n * When not operating in loader mode and the current child element does not have a next element sibling the traversal ends.\n *\n * See the documentation for {@link WalkChildrenOfCustomOpts} for the customization options available\n * @param {WalkChildrenOfCustomOpts} opts - Options for configuring the walk\n * @return {AsyncIterableIterator<SomeElement>}\n */\nasync function* walkChildrenOfCustom(opts) {\n  if (!opts) return;\n  const {\n    parentElement,\n    waitOptions,\n    selector,\n    filter,\n    nextChild = (parent, child) => child.nextElementSibling,\n  } = opts;\n  let isLoader = !!opts.loader;\n  if (!isLoader && (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.shouldWait) || Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.wait))) {\n    isLoader = true;\n  }\n  let shouldWait;\n  let wait;\n  if (isLoader) {\n    shouldWait = Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.shouldWait)\n      ? opts.shouldWait\n      : (parent, child) => child.nextElementSibling == null;\n    wait = Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.wait)\n      ? opts.wait\n      : (parent, child) => Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"waitForAdditionalElemChildren\"])(parent, waitOptions);\n  }\n  let curChild = parentElement.firstElementChild;\n  let useChild;\n  let nextChildValue;\n  while (curChild != null) {\n    useChild = filter ? filter(curChild) : true;\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(useChild) ? await useChild : useChild) {\n      yield selector ? selector(curChild) : curChild;\n    }\n    if (isLoader) {\n      const shouldWaitValue = shouldWait(parentElement, curChild);\n      if (\n        Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(shouldWaitValue) ? await shouldWaitValue : shouldWaitValue\n      ) {\n        await wait(parentElement, curChild);\n      }\n    }\n    nextChildValue = nextChild(parentElement, curChild);\n    curChild = Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(nextChildValue)\n      ? await nextChildValue\n      : nextChildValue;\n  }\n}\n\n/**\n * Traverses the children of the supplied element applying the supplied function to each child and yielding\n * the return value of the function.\n *\n * If the loader option is true or shouldWait and or wait function(s) are supplied then the walk\n * behaves as if the loader was set to true.\n *\n * When operating in loader mode and a child of the parent element does not have a next element sibling, a wait\n * for additional child elements to be added is done, if no custom wait function was supplied the wait happens for a maximum of 15 seconds.\n * After the wait ends and if the current child element still does not have a next element sibling the generator ends.\n *\n * When not operating in loader mode and the current child element does not have a next element sibling the traversal ends.\n *\n * See the documentation for {@link TraversalOpts} for the customization options available.\n * @param {TraversalOpts} opts - Options for configuring the traversal\n * @return {AsyncIterableIterator<*>}\n */\nasync function* traverseChildrenOfCustom(opts) {\n  if (!opts) return;\n  if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.preTraversal)) {\n    const preValue = opts.preTraversal();\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(preValue)) {\n      for await (const preNext of Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"noExceptGeneratorWrap\"])(preValue)) {\n        yield preNext;\n      }\n    } else if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(preValue)) {\n      await preValue;\n    }\n  }\n  let parentElement;\n  if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.setup)) {\n    const parentElemOrPromise = opts.setup();\n    parentElement = Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(parentElemOrPromise)\n      ? await parentElemOrPromise\n      : parentElemOrPromise;\n  } else {\n    parentElement = opts.parentElement;\n  }\n  if (!parentElement) {\n    if (opts.setupFailure) {\n      const handlingFailure = Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.setupFailure)\n        ? opts.setupFailure()\n        : opts.setupFailure;\n      if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(handlingFailure)) {\n        for await (const failureNext of Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"noExceptGeneratorWrap\"])(\n          handlingFailure\n        )) {\n          yield failureNext;\n        }\n      } else {\n        yield handlingFailure;\n      }\n    }\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.postTraversal)) {\n      const postValue = opts.postTraversal(true);\n      if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(postValue)) {\n        try {\n          let next;\n          let nv;\n          while (true) {\n            next = postValue.next();\n            nv = Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(next) ? await next : next;\n            if (nv.done) {\n              if (nv.value) return nv.value;\n              break;\n            } else {\n              yield nv.value;\n            }\n          }\n        } catch (e) {}\n      } else if (postValue) return postValue;\n    }\n    return;\n  }\n  for await (const child of walkChildrenOfCustom({\n    parentElement,\n    loader: opts.loader,\n    nextChild: opts.nextChild,\n    shouldWait: opts.shouldWait,\n    wait: opts.wait,\n    waitOptions: opts.waitOptions,\n    filter: opts.filter,\n    selector: opts.selector,\n  })) {\n    const nextValue = opts.handler(child, opts.additionalArgs);\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(nextValue)) {\n      for await (const next of Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"noExceptGeneratorWrap\"])(nextValue)) {\n        yield next;\n      }\n    } else if (nextValue) {\n      yield nextValue;\n    }\n  }\n  if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(opts.postTraversal)) {\n    const postValue = opts.postTraversal(false);\n    if (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isGenerator\"])(postValue)) {\n      try {\n        let next;\n        let nv;\n        while (true) {\n          next = postValue.next();\n          nv = Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isPromise\"])(next) ? await next : next;\n          if (nv.done) {\n            if (nv.value) return nv.value;\n          } else {\n            yield nv.value;\n          }\n        }\n      } catch (e) {}\n    } else if (postValue) {\n      return postValue;\n    }\n  }\n}\n\n/**\n * Reasons why the walk performed by {@link DisconnectingWalk} has ended\n * @type {{failedToRefindParent: number, noMoreChildren: number, failedToRefindChild: number, failedToFindFirstParent: number, failedToFindFirstChild: number}}\n */\nconst WalkEndedReasons = {\n  failedToFindFirstParent: 1,\n  failedToFindFirstChild: 2,\n  failedToRefindParent: 3,\n  failedToRefindChild: 4,\n  noMoreChildren: 0,\n};\n\nclass DisconnectingWalk {\n  /**\n   *\n   * @param {DisconnectingWalkConfig} config\n   */\n  constructor(config) {\n    config.loader = !!config.loader;\n    // if is loader is not set to true but a custom shouldWait or wait\n    // function was supplied, then it is assumed that loader should be true\n    if (\n      !config.loader &&\n      (Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(config.shouldWait) || Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(config.wait))\n    ) {\n      config.loader = true;\n    }\n    // check if we need to use default functions\n    if (config.loader && !Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(config.shouldWait)) {\n      config.shouldWait = (parent, child) => child.nextElementSibling == null;\n    }\n    if (config.loader && !Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(config.wait)) {\n      config.wait = (parent, child) =>\n        Object(_delays__WEBPACK_IMPORTED_MODULE_0__[\"waitForAdditionalElemChildren\"])(parent, config.waitOptions);\n    }\n    if (!Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"isFunction\"])(config.nextChild)) {\n      config.nextChild = (parent, child) => child.nextElementSibling;\n    }\n\n    /**\n     * @type {DisconnectingWalkConfig}\n     */\n    this.opts = config;\n\n    /**\n     * Set to one of the {@link WalkEndedReasons} when a walk is ended\n     * @type {?number}\n     */\n    this.walkEndedReason = null;\n    // perform the initial setup and set the walk end reason if setup fails\n    const parent = this.opts.findParent();\n    let child;\n    if (!parent) {\n      this.walkEndedReason = WalkEndedReasons.failedToFindFirstParent;\n    } else {\n      child = parent.firstElementChild;\n      if (!child) {\n        this.walkEndedReason = WalkEndedReasons.failedToFindFirstChild;\n      }\n    }\n\n    /**\n     * The parent element containing the children to be walked\n     * @type {?SomeElement}\n     */\n    this._curParent = parent;\n\n    /**\n     * The current child element of the walk\n     * @type {?SomeElement}\n     */\n    this._curChild = child;\n    Object(_general__WEBPACK_IMPORTED_MODULE_2__[\"autobind\"])(this);\n  }\n\n  /**\n   * Returns the parent element who's children are being walked.\n   * The element returned maybe disconnected\n   * @return {?SomeElement}\n   */\n  get parent() {\n    return this._curParent;\n  }\n\n  /**\n   * Returns the current child.\n   * The element returned maybe disconnected\n   * @return {?SomeElement}\n   */\n  get child() {\n    return this._curChild;\n  }\n\n  /**\n   * Swap the current child element, the yielded element, of the walk to the supplied element.\n   *\n   * This function should be used in the cases when the child element yield'd\n   * is removed or will be removed from the document and never replaced after\n   * performing some action.\n   *\n   * In that case when before performing the action that would remove the yield'd child,\n   * the child's previous element sibling should be supplied to this method in order\n   * to ensure that the walk continues as expected.\n   *\n   * @param {SomeElement} newChild - The element to replace the current child element\n   */\n  swapChild(newChild) {\n    this._curChild = newChild;\n  }\n\n  /**\n   * Initiate re-finding the parent and or current child element of the walk,\n   * returning true to indicate success (elements re-found or no need to re-found)\n   * and false if the parent or current child element could not be re-found.\n   *\n   * The parent and or child elements are re-found if it is detected that they\n   * have been removed from the document\n   *\n   * @return {boolean}\n   */\n  refind() {\n    if (!this._curParent || !this._curParent.isConnected) {\n      this._curParent = this.opts.findParent();\n      if (!this._curParent) {\n        this.walkEndedReason = WalkEndedReasons.failedToRefindParent;\n        return false;\n      }\n      this._curChild = this.opts.refindChild(this._curParent, this._curChild);\n      if (!this._curChild) {\n        this.walkEndedReason = WalkEndedReasons.failedToRefindChild;\n        return false;\n      }\n    }\n    if (!this._curChild || !this._curChild.isConnected) {\n      this._curChild = this.opts.refindChild(this._curParent, this._curChild);\n      if (!this._curChild) {\n        this.walkEndedReason = WalkEndedReasons.failedToRefindChild;\n        return false;\n      }\n    }\n    return true;\n  }\n\n  /**\n   * Perform the walk.\n   * @return {AsyncIterableIterator<SomeElement>}\n   */\n  async *walk() {\n    if (this.walkEndedReason) return;\n    while (this._curChild != null && this._curChild.isConnected) {\n      yield this._curChild;\n      if (!this.refind()) break;\n      if (\n        this.opts.loader &&\n        this.opts.shouldWait(this._curParent, this._curChild)\n      ) {\n        await this.opts.wait(this._curParent, this._curChild);\n      }\n      this._curChild = this.opts.nextChild(this._curParent, this._curChild);\n    }\n    if (!this.walkEndedReason) {\n      this.walkEndedReason = WalkEndedReasons.noMoreChildren;\n    }\n  }\n\n  /**\n   * Same as {@link walk}\n   * @return {AsyncIterableIterator<SomeElement>}\n   */\n  [Symbol.asyncIterator]() {\n    return this.walk();\n  }\n}\n\n/**\n * @typedef {SomeElement|Promise<SomeElement>} ElemOrPromiseElem\n */\n\n/**\n * @typedef {boolean|Promise<boolean>} BoolOrPromiseBool\n */\n\n/**\n * @typedef {Object} WalkChildrenOfCustomOpts\n * @property {SomeElement} parentElement - The element who's children are to\n * be visited, if not supplied it is expected that a setup function is\n * @property {function(parent: SomeElement, child: SomeElement): ElemOrPromiseElem} [nextChild] - Function used to get the next child to be visited\n * @property {function(child: SomeElement): Element} [selector] - Function used to select a more specific element to be used\n * @property {function(child: SomeElement): BoolOrPromiseBool} [filter] - Function used to determine if a specific child is to be handled\n * @property {function(parent: SomeElement, child: SomeElement): BoolOrPromiseBool} [shouldWait] -\n * Function used to determine if a wait for additional children should be done\n * @property {function(parent: SomeElement, child: SomeElement): Promise<*>} [wait] - Function used\n * to perform the wait for additional children\n * @property {boolean} [loader = false] - Should the walk expect that the parent element\n * could have additional child elements added e.g. infinite loading.\n * If loader is falsy and either or both shouldWait and wait function(s) are supplied then the walk\n * behaves as if the loader was set to true\n * @property {WaitForOptions} [waitOptions] - Options controlling\n * the wait for more children {@link waitForAdditionalElemChildren} when no custom\n * wait function is supplied\n */\n\n/**\n * @typedef {Object} TraversalOpts\n * @property {function(element: SomeElement, additionalArgs: *): *} handler - The function to be called for each child\n * @property {?SomeElement} [parentElement] - The element who's children are to\n * be visited, if not supplied it is expected that a setup function is\n * @property {function(): ElemOrPromiseElem} [setup] - Function\n * used to receive the parent element who's children are to be traversed.\n * @property {function(): *} [setupFailure] - Function called if the supplied\n * setup function fails to return a parent element or the supplied parent\n * element is falsy\n * @property {function(parent: SomeElement, child: SomeElement): ElemOrPromiseElem} [nextChild] - Function used to get the\n * next child to be visited\n * @property {function(child: SomeElement): BoolOrPromiseBool} [filter] -\n * Function used to determine if a specific child is to be handled\n * or not, returning true indicates the child element is to be handled false otherwise\n * @property {function(child: SomeElement): Element} [selector] - Function\n * used to select a more specific element to be used\n * @property {function(parent: SomeElement, child: SomeElement): BoolOrPromiseBool} [shouldWait] -\n * Function used to determine if a wait for additional children should be done\n * @property {function(parent: SomeElement, child: SomeElement): Promise<*>} [wait] - Function used\n * to perform the wait for additional children\n * @property {function(): *} [preTraversal] - Function used to perform\n * some action before traversal begins. If this function returns an (async) generator\n * the values of the generator are yielded otherwise no value this function returns\n * is yielded\n * @property {function(setupFailure: boolean): *} [postTraversal] - Function called after the traversal\n * is complete with T/F indicating if the function is being called if setup failed or the traversal is complete.\n * The value of this function yield'd if it is truthy\n * @property {*} [additionalArgs] - Optional additional arguments to be supplied\n * to the handler function\n * @property {boolean} [loader = false] - Should the traversal expect that the parent element\n * could have additional child elements added e.g. infinite loading.\n * If loader is falsy and either or both shouldWait and wait function(s) are supplied then the walk\n * behaves as if the loader was set to true\n * @property {WaitForOptions} [waitOptions] - Options controlling\n * the wait for more children {@link waitForAdditionalElemChildren} when no custom\n * wait function is supplied\n */\n\n/**\n * @typedef {Object} DisconnectingWalkConfig\n * @property {function(): ?SomeElement} findParent - function used to find the parent element\n * @property {function(parent: SomeElement, child: SomeElement): ?SomeElement} refindChild - function used to re-find the current child when the parent element has become disconnected\n * @property {function(parent: SomeElement, child: SomeElement): ?SomeElement} [nextChild] - function used to get the next child element\n * @property {boolean}  [loader = false] - Should the traversal expect that the parent element\n * could have additional child elements added e.g. infinite loading. When true the shouldWait and wait functions must be supplied\n * @property {function(parent: SomeElement, child: SomeElement): boolean} [shouldWait] - function used to determine if the wait function should be called. Only required when loader is true\n * @property {function(parent: SomeElement, child: SomeElement): void} [wait] - function used to wait once `shouldWait` returns true. Only required when loader is true\n * @property {WaitForOptions} [waitOptions] - Options controlling\n * the wait for more children {@link waitForAdditionalElemChildren} when no custom\n * wait function is supplied\n */\n\n\n//# sourceURL=webpack://lib/./lib/traversals.js?");

/***/ })

/******/ });